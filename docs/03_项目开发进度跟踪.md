# GT Office 项目开发进度跟踪

> 基线日期：2026-02-06

## 1. 状态定义

- `TODO`：未开始
- `IN_PROGRESS`：进行中
- `BLOCKED`：受阻
- `DONE`：完成

## 2. 里程碑看板

| 里程碑 | 目标 | 计划周期 | 状态 | 完成度 |
|---|---|---|---|---|
| M0 需求冻结 | 完成需求/架构/规范文档 | W1 | DONE | 100% |
| M1 MVP 内核 | Workspace+文件树+Terminal+基础Git | W2-W5 | DONE | 100% |
| M2 MVP 完整 | 多窗口+Tool Adapter+发布候选 | W6-W8 | TODO | 0% |
| M3 V1 体验增强 | Change Feed+会话恢复+安全完善 | W9-W12 | TODO | 0% |

## 3. 当前任务清单

| ID | 模块 | 任务 | 优先级 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|---|---|
| T-170 | UI | StationCard 小窗终端输入不可见 + 顶部按钮 tooltip 层级修复 | P1 | DONE | AI+你 | xterm 在小尺寸初始化时补齐最小尺寸与字体就绪后的 fit/refresh；提升 StationCard 按钮 tooltip z-index 并移除原生 title 避免叠层与双提示 |
| T-171 | Core/Infra | 多 Agent MCP 桥接（任务中心解耦派发 + 多 CLI 自动安装） | P0 | IN_PROGRESS | AI+你 | 已修复 Codex 启动超时与 Claude 新版配置兼容（`.claude.json` project-scope）；开发环境 runtime/gto_health/e2e（health-only）已通过，待补全 dispatch/handover 真流与四 CLI 真机回归 |
| T-172 | Core/Infra | 外部通道适配基线（Feishu + Telegram 路由/准入/幂等/诊断） | P0 | IN_PROGRESS | AI+你 | 已完成内置 webhook runtime、任务中心可视化、设置页“添加 Channel -> 分步向导弹窗”配置闭环；当前默认仅展示已添加 channel，路由目标改为岗位选择（`role:<role_key>`），后端解析岗位 agent 档案并吸收 `agent_runtime_register.roleKey` 在线实例进行派发；新增绑定元数据字段 `createdAtMs/botName`，其中 `botName` 在绑定时回填一次并可由健康检查变更回写；设置页外部通道绑定列表支持“bot-agent-createdAt”摘要、图标化操作与行级健康检查；当前外部入站主链路仍以“直接写入已绑定的 live terminal session 并提交执行”为准；本轮已将外部回复提取主事实源切换为前端 `xterm.buffer.active` 派生的 `RenderedScreenSnapshot`，新增 `terminal.report_rendered_screen` 命令与 Station xterm 采样上报链路，后端回复会话优先使用 rendered-screen 提取并保留 legacy VT100 作为遗留 fallback；待补 Telegram 真机回归与更多误判样本验证 |
| T-173 | DevX/Infra | Tauri 开发启动稳定性修复（端口对齐 + 旧路径构建缓存自修复） | P1 | DONE | AI+你 | `scripts/run-tauri-with-env.cjs` 已支持动态端口注入与 stale build cache 自动清理（`tauri-*` / `gtoffice-desktop-tauri-*`），避免 `devUrl` 漂移与仓库迁移后 `plugin permissions` 读取旧路径失败 |
| T-174 | UI | 设置页移除“文件系统运行时设置”面板 | P2 | DONE | AI+你 | 该面板仅暴露 watcher/preview 运行时参数，非核心路径且无契约依赖；已从设置弹窗移除并清理文案/样式与组件文件，保留底层 settings API 供系统内部读取 |
| T-175 | UI | 样式系统工程化重构（CSS -> SCSS 分层治理） | P1 | IN_PROGRESS | AI+你 | 已完成 SCSS 架构落地（tokens/base/features/utilities）、入口切换、Git 样式单一来源迁移与构建验证；待继续按 feature 拆分 `_legacy-foundations.scss` 以降低样式体积与层叠复杂度 |
| T-176 | Core/Infra | 规范测试目录结构 (Integration Test) | P2 | DONE | AI+你 | 将所有测试文件迁移到独立的 `tests/` 目录下，保证测试代码与被测代码严格解耦，提升了依赖分界线与架构一致性 |
| T-177 | UI | Channel 多 Bot 绑定分层管理 + StationCard 绑定可视化 | P1 | DONE | AI+你 | 已将 Channel 管理收敛为 `channel -> bot(accountId) -> route(target)` 分层展示，并在 StationCard 展示命中的 channel/bot 绑定摘要；抽离通用聚合与 connector 账户解析工具，去除 Telegram-only 逻辑 |
| T-178 | UI | 设置页与 Channel 管理中心重构（VSCode 布局 + Apple 风格） | P1 | DONE | AI+你 | 重构 `SettingsModal` 为左侧分类栏 + 右侧内容区的 VSCode 分栏布局；将 Channel 管理拆分为 `ChannelManagerPane` / `ChannelOverview` / `ChannelWizard` / `ChannelBotCard` 4 个独立组件；删除旧版 `SettingsChannelManagerModal` / `SettingsChannelOnboardingPane` / `SettingsChannelEntryCard`；新增 `_settings.scss` 专属样式并导入分层体系；验证通过 `npm run typecheck` |
| T-179 | UI | 设置页字号字体与 i18n 兼容性修复 | P2 | DONE | AI+你 | 修复 `settingsModal.title` i18n key 显示为英文字符串的问题；为桌面应用补充 Google Fonts 外部资源并在 `CodeMirror` 及 `Terminal` 编辑器应用动态字号配置 |
| T-180 | UI/Core | 非 Git 工作区降噪（轻提示 + 优雅降级） | P1 | DONE | AI+你 | 修复“目录无 `.git` 时 Git 协作面板报错风暴”：后端统一返回 `GIT_REPO_INVALID`，前端识别后改为轻提示文案、禁用 Git 操作按钮，并避免连接状态进入 `git-read-failed` |

## 4. 每周节奏模板

### Week N 目标

1. 本周交付：
2. 关键依赖：
3. 风险预警：

### Week N 复盘

1. 已完成：
2. 未完成与原因：
3. 下周调整：

## 5. 风险与阻塞登记

| 风险ID | 描述 | 影响 | 应对策略 | 状态 |
|---|---|---|---|---|
| R-001 | PTY 在不同平台行为差异 | 终端稳定性 | 做平台适配层+回归用例 | OPEN |
| R-002 | 大仓库文件树性能波动 | 使用体验 | 虚拟列表+懒加载+缓存 | OPEN |
| R-003 | Git 复杂场景兼容性 | 功能可靠性 | git2 + system git 双通道 | OPEN |
| R-004 | 本机缺少 Linux 系统库导致 Tauri 无法编译 | 开发阻塞 | 安装 pkg-config/webkit2gtk/rsvg2 等依赖 | CLOSED |
| R-005 | Agent 协作功能扩展导致架构复杂度上升 | 研发节奏与质量 | 采用阶段化落地（先本地权威存储，后 Redis 加速） | OPEN |

## 6. 变更记录

| 日期 | 变更内容 | 影响范围 | 执行人 |
|---|---|---|---|
| 2026-03-08 | 继续修复 `T-172` Telegram 回传丢失：在外部回复调度状态机中改为“Finalize 成功后再删除会话”，避免发送超时时候选被提前移除导致消息永久丢失；新增 preview 失败回滚（重置节流状态）以允许下轮重发；修复“会话无输出阶段被 idle 误清理”导致后续真实输出无法回传的问题。同步在 `flush_external_reply_candidates` 中接入 finalize 成功清理与 preview 失败回滚调用，并补充重试相关回归测试。验证通过 `cargo test -p gtoffice-desktop-tauri external_reply_finalize_candidate_retries_until_marked_delivered -- --nocapture`、`cargo test -p gtoffice-desktop-tauri external_reply_preview_failure_resets_throttle_for_retry -- --nocapture`、`cargo test -p gtoffice-desktop-tauri external_reply_binding_kept_when_no_output_yet -- --nocapture`、`cargo test -p gtoffice-desktop-tauri external_reply_flush_is_single_shot_after_idle -- --nocapture`、`cargo check -p gtoffice-desktop-tauri`。 | `T-172` Telegram 出站重试可靠性、外部回复会话生命周期、超时场景消息保真 | AI |
| 2026-03-08 | 继续修复 `T-172` 回传失败链路：针对 `external/channel_error` 中 `CHANNEL_REPLY_SEND_FAILED -> curl (28) timed out after 15005ms`，在 Telegram connector API 层补齐传输容错。`run_curl_json` 新增可重试网络错误识别（timeout/connect reset/empty reply/TLS 等），失败时自动走 `--connect-timeout 8 --retry 2 --retry-delay 1 --retry-all-errors -4 --http1.1` 回退重试（Windows 额外 `--ssl-no-revoke`）；同时将 `sendMessage/editMessageText/deleteMessage` 超时窗口从 `15s` 提升到 `25s`，降低抖动下误失败。 | `T-172` Telegram 出站稳定性、channel reply 传输容错、外部事件错误率 | AI |
| 2026-03-08 | 修复 `T-172`“Telegram 入站可达但 Agent 回复不回传”的复合根因：1) 前端 `StationXtermTerminal` 将 `› Implement {feature}` 误判为最新 prompt 锚点，导致快照从该行开始截断，真实 assistant 文本未上报；现已将该模式归类为 placeholder prompt，避免误截断。2) `app_state` 的 rendered-screen 锚点定位从“仅 injected_input 精确回显”升级为“精确回显 -> 折行前缀拼接 -> prompt 锚点回退”，解决长输入折行/换行后无法命中锚点导致正文被静默丢弃。3) 增加“折行输入续行吸收”逻辑，避免把用户输入第二行误当 assistant 正文。4) tool block 提取中新增“遇到新的 assistant 标记且非工具行即退出工具块”，避免 `• Ran ...` 后的真实回答被吞。5) 交互菜单识别新增标题语义约束，避免把普通 `1./2.` 回答误判为 Telegram inline menu 导致正文被剔除。新增回归测试 `rendered_screen_reply_snapshot_falls_back_when_injected_echo_is_wrapped` 与 `rendered_screen_reply_snapshot_extracts_real_telegram_dom_sample`。验证通过 `cargo test -p gtoffice-desktop-tauri rendered_screen_reply_snapshot_ -- --nocapture`、`cargo test -p gtoffice-desktop-tauri rendered_screen_reply_snapshot_extracts_real_telegram_dom_sample -- --nocapture`、`cargo check -p gtoffice-desktop-tauri`、`npm run typecheck`。另：`cargo test -p gtoffice-desktop-tauri codex_bound_reply_session_emits_candidate_after_rendered_snapshot -- --nocapture` 当前仍失败（期望 Preview，实得 Finalize），本轮未调整该既有断言。 | `T-172` Telegram 回传完整性、前后端 rendered-screen 锚点一致性、工具输出与交互菜单误判抑制 | AI |
| 2026-03-06 | 以第一性原理修正 `T-172` rendered-screen 事实源：前端 `StationXtermTerminal` 不再只采集当前 viewport 的可视区文本，而是基于 `xterm.buffer.active` 逆向扫描最近 prompt 锚点，采集“从本轮 prompt 到 buffer 末尾”的整轮 turn 快照后再上报。这样后端提取和累计的输入从“当前屏幕一小块”升级为“当前整轮回答的 rendered transcript”，用于解决长回答滚屏、段落换页后可视区丢失导致 channel 内容不完整的问题。 | `T-172` rendered-screen 事实源、长回答完整性、前后端 turn 级提取语义 | AI |

