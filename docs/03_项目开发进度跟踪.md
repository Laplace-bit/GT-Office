# GT Office 项目开发进度跟踪

> 基线日期：2026-02-06

## 1. 状态定义

- `TODO`：未开始
- `IN_PROGRESS`：进行中
- `BLOCKED`：受阻
- `DONE`：完成

## 2. 里程碑看板

| 里程碑 | 目标 | 计划周期 | 状态 | 完成度 |
|---|---|---|---|---|
| M0 需求冻结 | 完成需求/架构/规范文档 | W1 | DONE | 100% |
| M1 MVP 内核 | Workspace+文件树+Terminal+基础Git | W2-W5 | DONE | 100% |
| M2 MVP 完整 | 多窗口+Tool Adapter+发布候选 | W6-W8 | TODO | 0% |
| M3 V1 体验增强 | Change Feed+会话恢复+安全完善 | W9-W12 | TODO | 0% |

## 3. 当前任务清单

| ID | 模块 | 任务 | 优先级 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|---|---|
| T-170 | UI | StationCard 小窗终端输入不可见 + 顶部按钮 tooltip 层级修复 | P1 | DONE | AI+你 | xterm 在小尺寸初始化时补齐最小尺寸与字体就绪后的 fit/refresh；提升 StationCard 按钮 tooltip z-index 并移除原生 title 避免叠层与双提示 |
| T-171 | Core/Infra | 多 Agent MCP 桥接（任务中心解耦派发 + 多 CLI 自动安装） | P0 | IN_PROGRESS | AI+你 | 已修复 Codex 启动超时与 Claude 新版配置兼容（`.claude.json` project-scope）；开发环境 runtime/gto_health/e2e（health-only）已通过，待补全 dispatch/handover 真流与四 CLI 真机回归 |
| T-172 | Core/Infra | 外部通道适配基线（Feishu + Telegram 路由/准入/幂等/诊断） | P0 | IN_PROGRESS | AI+你 | 已完成内置 webhook runtime、任务中心可视化、设置页“添加 Channel -> 分步向导弹窗”配置闭环；当前默认仅展示已添加 channel，路由目标改为岗位选择（`role:<role_key>`），后端解析岗位 agent 档案并吸收 `agent_runtime_register.roleKey` 在线实例进行派发；已修复 tunnel connector 的 `thread::spawn` Send 约束编译错误（E0277）并清理 Windows 平台无效导入 warning；待补真实机器人联调证据后收敛 DONE |
| T-173 | DevX/Infra | Tauri 开发启动稳定性修复（端口对齐 + 旧路径构建缓存自修复） | P1 | DONE | AI+你 | `scripts/run-tauri-with-env.cjs` 已支持动态端口注入与 stale build cache 自动清理（`tauri-*` / `gtoffice-desktop-tauri-*`），避免 `devUrl` 漂移与仓库迁移后 `plugin permissions` 读取旧路径失败 |
| T-174 | UI | 设置页移除“文件系统运行时设置”面板 | P2 | DONE | AI+你 | 该面板仅暴露 watcher/preview 运行时参数，非核心路径且无契约依赖；已从设置弹窗移除并清理文案/样式与组件文件，保留底层 settings API 供系统内部读取 |
| T-175 | UI | 样式系统工程化重构（CSS -> SCSS 分层治理） | P1 | IN_PROGRESS | AI+你 | 已完成 SCSS 架构落地（tokens/base/features/utilities）、入口切换、Git 样式单一来源迁移与构建验证；待继续按 feature 拆分 `_legacy-foundations.scss` 以降低样式体积与层叠复杂度 |

## 4. 每周节奏模板

### Week N 目标

1. 本周交付：
2. 关键依赖：
3. 风险预警：

### Week N 复盘

1. 已完成：
2. 未完成与原因：
3. 下周调整：

## 5. 风险与阻塞登记

| 风险ID | 描述 | 影响 | 应对策略 | 状态 |
|---|---|---|---|---|
| R-001 | PTY 在不同平台行为差异 | 终端稳定性 | 做平台适配层+回归用例 | OPEN |
| R-002 | 大仓库文件树性能波动 | 使用体验 | 虚拟列表+懒加载+缓存 | OPEN |
| R-003 | Git 复杂场景兼容性 | 功能可靠性 | git2 + system git 双通道 | OPEN |
| R-004 | 本机缺少 Linux 系统库导致 Tauri 无法编译 | 开发阻塞 | 安装 pkg-config/webkit2gtk/rsvg2 等依赖 | CLOSED |
| R-005 | Agent 协作功能扩展导致架构复杂度上升 | 研发节奏与质量 | 采用阶段化落地（先本地权威存储，后 Redis 加速） | OPEN |

## 6. 变更记录

| 日期 | 变更内容 | 影响范围 | 执行人 |
|---|---|---|---|

| 2026-03-03 | 推进 `T-175` 增量修复（任务中心 Flex 主布局重构）：1) 任务中心容器与宿主层收敛为 `flex` 纵向主布局（`task-center-scroll-host -> task-center-pane`），滚动统一在宿主层；2) 非历史区块固定 `flex: 0 0 auto`，`task-center-history` 改为 `flex: 1 1 auto` + 列表内部 `flex:1/overflow:auto`，由剩余空间驱动；3) 移除历史区 `grid-template-rows` 高度分配，避免小高度窗口下栅格分配导致的重叠。验证：任务中心核心段 `rg "px"` 无输出，`npm run typecheck` 通过。 | 任务中心布局范式（grid -> flex）、小高度窗口重叠修复、滚动行为一致性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（任务中心无 px 响应式单位改造）：任务中心核心样式段（`task-center-pane` 至 `task-center-history-actions`）统一由 `px` 切换为 `rem/svh/%`（含字体、间距、圆角、边框、列表高度、断点）；滚动区高度改为 `svh + clamp/minmax` 自适应；并同步收敛任务中心相关滚动条尺寸单位。验证：`sed -n '640,1430p' ... | rg \"px\"` 无输出，`npm run typecheck` 通过。 | 任务中心高度响应式一致性、低高度窗口重叠风险、单位体系一致性（无 px） | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（小高度响应式收敛）：1) `ShellRoot` 在 tasks 场景增加 `shell-left-pane is-task-center` 类，任务中心模式下左侧布局改为单行 `minmax(0,1fr)`，消除三行 grid 在低高度下的约束冲突；2) 任务中心关键区块高度改用 `svh` 维度收缩（描述框、历史区、外部事件区），并新增 `@media (max-height: 860px/720px)` 分级规则，在小高度窗口下自动减小间距与可视区高度；3) 保持滚动链路在宿主层统一，避免低高度下内容重叠。验证通过 `npm run typecheck`。 | 任务中心低高度窗口响应式、重叠问题、滚动与可视区稳定性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（任务中心重叠硬约束）：1) `task-center-scroll-host` 从跨行 `grid-row: 1 / -1` 改为固定 `grid-row: 3`（左侧 `1fr` 行）并承担唯一纵向滚动；2) `task-center-pane` 改为 `overflow: visible`，避免与宿主双滚动竞争；3) 任务描述 textarea 改为固定高度 `clamp(182px, 28vh, 320px)` + `resize: none` + 内部滚动，彻底消除拖拽导致的布局挤压/重叠。验证通过 `npm run typecheck`。 | 任务中心重叠问题根治、滚动层级稳定性、描述输入区高度行为一致性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（任务中心 UI 重叠收敛）：1) `task-center-editor` 增加 `overflow: hidden`，防止编辑区内部内容越界覆盖下方模块；2) 任务描述 textarea 增加 `max-height: clamp(220px, 34vh, 360px)` 与 `overflow: auto`，限制拖拽拉高上限并在框内滚动，避免继续挤压导致历史区重叠。验证通过 `npm run typecheck`。 | 任务中心编辑区与派发历史区重叠问题、文本框 resize 场景稳定性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（派发历史高度策略）：将 `task-center-history` 收敛为“标题 + 列表”双行布局，列表区高度改为 `minmax(132px, clamp(180px, 28vh, 320px))` 并恢复内部滚动，避免历史区塌陷或无限拉伸。验证通过 `npm run typecheck`。 | 任务中心派发历史区可读性、稳定高度与滚动体验 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（任务中心滚动宿主重构）：1) 在 `ShellRoot` 的 tasks 分支新增 `.task-center-scroll-host` 包裹层，使任务中心在左侧 `shell-left-pane` 内占满高度并形成稳定滚动宿主；2) `task-center-pane` 去除 `grid-row/height/max-height` 的跨布局耦合，仅保留内容纵向滚动职责；3) 宿主层通过 `grid-row: 1 / -1 + height:100% + min-height:0 + overflow:hidden + flex` 保证文本框拉高后可继续滚动到派发历史。验证通过 `npm run typecheck`。 | 任务中心最外层滚动稳定性、文本框 resize 场景下历史可达性、左侧面板布局鲁棒性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（任务中心外层滚动二次收敛）：1) `task-center-pane` 补充 `height/max-height: 100%` 强约束，固定为左侧面板内滚动视口；2) 任务历史区从“内部列表滚动”改为“外层容器统一滚动”（`task-center-history` 与其 `ul` 改为 `overflow: visible`）；3) 解除任务中心内部列表 `overscroll-behavior: contain` 对外层滚动链路的阻断（改为 `auto`）。验证通过 `npm run typecheck`。 | 任务中心最外层滚动可用性、滚轮滚动链路一致性、侧栏收缩场景下交互稳定性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（任务中心容器滚动与宽度自适应）：1) `task-center-pane` 外层容器改为纵向 `flex`，显式开启 `overflow-y: auto` 并补齐 `overflow-x: hidden`；2) 为任务中心子块补齐 `min-width: 0`，并在目标操作区/外部状态头部与控制区启用 `flex-wrap`，保证侧栏宽度变化时内容可收缩；3) 为任务历史文本增加 `overflow-wrap: anywhere`，避免长文本横向溢出。验证通过 `npm run typecheck`。 | 任务中心左侧面板滚动体验、侧栏宽度变更下的布局自适应与文本防溢出 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（Git 文件行操作约束）：已暂存文件仅显示“取消暂存（undo）”，移除“丢弃”按钮；未暂存文件显示“暂存（check）+ 丢弃（trash）”，与操作风险语义对齐。验证通过 `npm --workspace apps/desktop-web run build`。 | Git 文件行按钮可用性与语义一致性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（撤销箭头风格）：新增 `undo` 图标并将“丢弃变更（git.action.discard）”按钮从 `x-mark` 切换为 `undo`，与“撤销”语义对齐；验证通过 `npm --workspace apps/desktop-web run build`。 | Git 文件操作图标语义一致性、可用性 | AI |
| 2026-03-03 | 推进 `T-175` 增量修复（Git 管理模块图标语义）：将“丢弃变更（git.action.discard）”按钮图标从 `trash` 调整为 `x-mark`，避免与“删除”语义混淆；保留“删除分支（git.action.deleteBranch）”使用 `trash`。验证通过 `npm --workspace apps/desktop-web run build`。 | Git 文件行操作可理解性、风险操作语义一致性 | AI |
| 2026-03-03 | 启动并推进 `T-175` 样式系统工程化重构：1) `apps/desktop-web` 引入 `sass` 并将样式入口切换为 `src/styles/index.scss`；2) 将原 `index.css/shell-layout.css/git-pane.css/diff-viewer.css` 迁移至 `src/styles/{foundations,features}` 分层目录，新增 `tokens/_theme-compat.scss` 补齐历史缺失令牌（`--vb-primary`、`--vb-bg-secondary`、`--vb-surface-elevated` 等）；3) 移除 `ShellRoot.tsx`、`GitPane.tsx` 对局部 css 文件的直接导入，改为全局分层入口统一加载；4) 删除 shell 内重复 `.git-*` 规则，统一以 `features/git` 为单一来源；5) `apps/desktop-tauri` 模板样式同步切换为 `styles.scss`。验证通过 `npm run typecheck`、`npm run build:web`、`cargo check --workspace`。 | Web 样式工程结构、主题令牌兼容性、Git 视图样式来源一致性、Tauri 模板样式一致性 | AI |
| 2026-03-03 | 推进 `T-172` 第三十一轮修复（长消息截断与流式噪音体验）：1) finalize 静默窗口从 `20s` 回调为 `8s`，并移除“prompt 回归即立即 finalize”策略，避免流式过程中被中途提示符误触发提前收敛；2) 回传净化新增 CLI prompt 行过滤（`›/❯/$`），并增强运行态噪音过滤（`100%`、`Wng/Wog/Wlen/Lent` 等碎片）以消除流式开头噪音；3) 保持多行 assistant 正文提取不被 interleaved prompt 截断。验证通过 `cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml app_state::tests::should_skip_cli_prompt_lines -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml app_state::tests::normalize_reply_text_keeps_multiline_block_with_interleaved_prompts -- --nocapture`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | 长消息回传完整性、流式预览噪音抑制、终端通用提取稳定性 | AI |
| 2026-03-03 | 推进 `T-172` 第三十轮修复（通用命令行响应提取稳定性）：1) 外部回传 finalize 静默窗口从 `2.2s` 调整到 `20s`，避免长回答生成中途被提前收敛；2) `extract_last_assistant_block` 改为“提示符/状态行跳过而非截断”，修复长文本被 `›` 等 CLI 提示符打断后仅保留首行问题；3) 新增“提示符回归判定”用于完成态检测（与模型无关），在检测到 assistant 输出后出现命令行 prompt 时优先 finalize。验证通过 `cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml app_state::tests::normalize_reply_text_keeps_multiline_block_with_interleaved_prompts -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml app_state::tests::prompt_recovered_after_assistant_reply_is_detected -- --nocapture`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | 长消息回传完整性、通用 CLI 响应提取鲁棒性、TG 输出正确率 | AI |
| 2026-03-03 | 推进 `T-172` 第二十九轮重构（对齐 openclaw Telegram 模块化与流式回传）：1) 连接器目录从单文件升级为 `connectors/telegram/{mod,api,inbound,offset_store}.rs`，将 API 调用、入站解析、polling offset 持久化拆分；2) 新增 Telegram `editMessageText` 出站能力并接入“节流预览 + 最终收敛”流式回传链路（同一消息编辑，失败回退 send）；3) `app_state` 外部回传会话升级为流式派发状态机（`Preview/Finalize`），支持预览消息 ID 回写；4) polling offset 持久化落盘到 `app_data/channel/telegram/update-offset-<account>.json`，重启后续拉取更稳定。验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml app_state::tests::external_reply_dispatch_emits_preview_then_finalize -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml app_state::tests::normalize_reply_text_extracts_last_assistant_block -- --nocapture`。 | Telegram 消息处理架构可维护性、流式回传用户体验、polling 重启一致性、外部通道稳定性 | AI |
| 2026-03-03 | 推进 `T-172` 第二十八轮修复：1) 针对“英文仍仅换行”在 Telegram 外部入站派发链路加入二次硬提交（在首个 submit 后追加一次 `\\r`），覆盖首个 Enter 被补全/菜单消费的场景；2) 回传净化升级为“全行输入回显过滤 + 最后 assistant 回答块提取”，剥离重复 prompt 与碎片状态噪音（含 `Wng1/Wog/...`）。验证通过 `cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml normalize_reply_text_extracts_last_assistant_block -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml normalize_reply_text_skips_runtime_noise_lines -- --nocapture`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | Telegram 英文入站提交稳定性、回传内容纯净度、外部通道端到端鲁棒性 | AI |
| 2026-03-03 | 推进 `T-172` 第二十七轮修复：1) 修复“英文消息仅换行不提交”隐患，将 submit 序列归一化策略收敛为 `\\r/\\n/\\r\\n -> \\r`（前端采集与后端派发双侧一致），避免 runtime 学到 `\\n` 后导致仅换行；2) 修复 Telegram 回传噪音，新增终端运行态噪音过滤（`Working...`、`Implement {feature}`、模型状态行、spinner 碎片）并保留有效回复内容。验证通过 `cargo test -p vb-task dispatch_batch_runtime_lf_submit_is_canonicalized_to_cr -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml normalize_reply_text_skips_runtime_noise_lines -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml should_skip_runtime_noise_line_filters_status_and_spinner -- --nocapture`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`npm run typecheck`。 | Telegram 英文入站提交稳定性、回传内容纯净度、跨 CLI Enter 兼容性 | AI |
| 2026-03-02 | 推进 `T-172` 第二十六轮热修复：针对仍出现的 `WORKSPACE_RESOLVE_FAILED: workspace not found: ws:<uuid>`，在外部入站处理链路增加运行时兜底映射：当路由命中旧 `workspace_id` 且解析失败时，自动回退到“当前 active workspace（优先）或唯一已打开 workspace（次选）”继续派发，避免旧绑定阻断消息流。验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | 外部入站鲁棒性、旧 workspace_id 兼容性、故障自恢复能力 | AI |
| 2026-03-02 | 推进 `T-172` 第二十五轮修复：修复 `WORKSPACE_RESOLVE_FAILED: workspace not found: ws:<uuid>`。根因是 Channel 路由绑定按运行时 `workspace_id` 持久化，而 `workspace_id` 在重启后可能变化。现将通道状态持久化升级为 `route_binding + workspace_root`，启动恢复时优先按 `workspace_root` 自动 `workspace.open` 并重映射到新 `workspace_id`；兼容旧状态文件（无 `workspace_root`）时，在仅有一个已打开工作区的场景自动回落映射。验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | Channel 路由恢复稳定性、重启后外部通道可达性、旧状态文件兼容迁移 | AI |
| 2026-03-02 | 推进 `T-172` 第二十四轮修复（基于 CLI 提交键差异资料分析）：1) 新增“提交序列学习”机制：前端从 xterm `onData` 捕获真实 Enter 序列（`\r`/`\n`/`\x1bOM`/CSI-u 等），通过 `agent_runtime_register.submitSequence` 上报后端 runtime；2) `vb-task` 派发 submit 选择升级为“优先 runtime.submitSequence -> request.submitSequences -> 默认 `\r`”，避免不同 CLI 提交键差异导致“仅换行不提交”；3) 回传回显抑制增强：支持带提示符前缀（如 `> input`）的输入回显清洗；4) 继续保持 Channel 状态持久化（`app_data/channel/state.json`）与启动恢复。验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml normalize_reply_text_suppresses_injected_input_echo -- --nocapture`、`npm run typecheck`。 | 外部通道到 Agent 的跨 CLI 提交兼容性、TG 回传净化稳定性、重启后配置可恢复性 | AI |
| 2026-03-02 | 推进 `T-172` 第二十三轮修复：1) 外部通道命令提交增强：默认 submit 序列从 `CRLF` 调整为 `CR`，并在 `task_dispatch_batch` / `channel_adapter` / `mcp_bridge` 三处改为“先写命令、再写 submit”两段写入，降低“仅换行未提交”概率；2) 外部回传抑制输入回显：`ExternalReplyRelayTarget` 绑定注入原文，回传规范化阶段新增 injected-input echo suppression，避免“原始消息被原样回传 TG”；3) 修复重启丢 Channel 配置：新增 `app_data/channel/state.json` 持久化（route bindings / access policy / allowlist），并在应用启动时恢复到 `TaskService`；4) 设置页向导优化：Telegram 在“已有账户且 `hasBotToken=true`”时允许空 token 保存，不再强制每次重填。验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`cargo test -p vb-task dispatch_batch_terminal_command_appends_real_crlf_enter -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml normalize_reply_text_suppresses_injected_input_echo -- --nocapture`、`npm run typecheck`。 | Telegram 入站执行一致性、回传内容准确性、Channel 配置持久化与重启恢复、设置页可用性 | AI |
| 2026-03-02 | 推进 `T-172` 第二十二轮修复：1) `vb-task` 派发命令从固定 `echo [vb-task] assigned ...` 调整为写入任务原文（外部通道即 Telegram 入站原始消息），避免消息内容丢失；2) 修复 `submit_sequence` 未落到实际终端写入的问题，在 `task_dispatch_batch` / `channel_adapter` / `mcp_bridge` 三处统一改为 `command + submit_sequence`，确保自动回车执行；3) 外部回传文本规范化新增 `vb-task` 系统提示行过滤，防止系统派发提示被误回传到 Telegram。验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`cargo test -p vb-task dispatch_batch_terminal_command_appends_real_crlf_enter -- --nocapture`、`cargo test -p vb-task dispatch_batch_honors_target_submit_sequence_override -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml normalize_reply_text_skips_vb_task_assignment_lines -- --nocapture`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml should_skip_external_reply_line_only_for_vb_task_markers -- --nocapture`。 | Telegram 入站到 Agent 投递语义、终端执行一致性（自动回车）、外部回传内容纯净度 | AI |
| 2026-03-02 | 推进 `T-172` 第二十一轮修复：1) 修复设置页“打开管理中心”黑屏卡住问题，根因是 `SettingsChannelOnboardingPane` 渲染阶段引用未定义变量 `botConfigSaving`（触发运行时 `ReferenceError`）；现已移除该无效引用，管理中心可正常渲染。2) 收敛 Windows 编译告警：为 `credential_store.rs` 的进程内内存回退实现添加 `#[cfg(not(target_os = \"windows\"))]`，避免 Windows 目标下 `MEMORY_FALLBACK/memory_*` 死代码告警。验证通过 `npm run typecheck`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | Channel 管理中心可用性、前端运行时稳定性、Windows 构建告警收敛 | AI |
| 2026-03-02 | 推进 `T-172` 第二十轮收敛：针对“消息入站但未派发”高频误配，将 Channel 向导默认准入策略从 `pairing` 调整为 `open`（仅影响新建/重新应用配置），降低首次接入门槛；同时增强 `CHANNEL_PAIRING_REQUIRED` 错误详情，返回 identity 与 pairingCode 及处理指引，便于快速定位并修复旧配置。验证通过 `npm run typecheck`、`cargo check -p gtoffice-desktop-tauri`。 | Channel 首次可用性、故障可诊断性、配对策略可运维性 | AI |
| 2026-03-02 | 推进 `T-172` 第十九轮实现：设置页 Channel 向导 Step2 从“仅岗位绑定”升级为“绑定 Agent / 绑定岗位”双模式。新增目标类型切换、Agent 列表加载（`agent.list`）、目标摘要展示与绑定列表显示优化（识别 `role:<key>` 与 `agentId`），保存时按目标类型写入 `targetAgentId`（direct agent id 或 `role:<role_key>`）。同步新增 i18n key，避免新增多语言文本与代码耦合。验证通过 `npm run typecheck`。 | Channel 配置可用性、绑定语义完整性（Agent+Role）、设置页信息架构与 i18n 规范一致性 | AI |
| 2026-03-02 | 推进 `T-172` 第十八轮实现：新增“外部通道响应回传”闭环（Telegram）：1) 入站派发成功后按 `targetAgentId -> runtime sessionId` 绑定外部上下文；2) 终端输出接入 channel reply 缓冲（去 ANSI、压缩空行、32KB 上限）；3) 新增后台 flush worker，按“静默窗口（2.2s）或会话结束”触发一次性回传，避免流式逐 chunk 刷屏；4) Telegram connector 新增 `sendMessage` 出站能力并发射 `external/channel_outbound_result` 成功/失败回执。补充 `app_state` 单测覆盖“单次 flush / 无输出不提前移除”。验证通过 `cargo check -p gtoffice-desktop-tauri`、`cargo test -p gtoffice-desktop-tauri app_state::tests -- --nocapture`。 | 外部通道回传闭环、Telegram 出站能力、terminal 流式回传策略、事件可观测性 | AI |
| 2026-03-01 | 推进 `T-172` 第十七轮实现：1) Telegram 入站调试弹窗补充消息内容展示（后端 `external/channel_inbound` 事件新增 `senderName/text`，前端弹窗展示 sender/peer/messageId/content/account）；2) 设置页重构为“入口卡片 + 独立 Channel 管理弹窗”，并抽离 `SettingsChannelEntryCard` 与 `SettingsChannelManagerModal` 组件，降低设置主页复杂度；3) 修复 Windows token 持久化缺口：`credential_store` 接入 Windows Credential Manager（`CredWriteW/CredReadW`），并移除 Windows 上静默内存回退，避免“保存成功但重启丢失”。验证通过 `npm run typecheck`、`cargo check --workspace`。 | Telegram 联调可观测性、设置页信息架构（入口/管理分离）、组件边界清晰、Windows 凭据持久化稳定性 | AI |
| 2026-03-01 | 推进 `T-172` 第十六轮实现：新增 Telegram 入站调试弹窗（前端订阅 `external/channel_inbound` 时命中 `channel=telegram` 触发，展示 sender/peer/message/account 与接收时间，6 秒自动消失可手动关闭），用于快速验证 polling 入站是否到达 GT Office；保持既有外部事件列表不变。验证通过 `npm run typecheck`。 | Telegram 联调可观测性、用户自助排障效率、接入验证路径清晰度 | AI |
| 2026-03-01 | 推进 `T-172` 第十五轮实现（对齐 openclaw polling-first）：Telegram connector 默认模式切换为 `polling`，并新增后台 polling worker（启动时自动拉起，`getUpdates` 拉取、首次 `deleteWebhook` 预清理、按 account 维护 update offset）；轮询入站消息统一走 `process_external_inbound_message`，复用既有路由/准入/幂等/派发链路；设置页保存 Telegram 账户默认写入 `mode=polling`，向导流程不再依赖 webhook 同步，用户只需 token 即可完成基础连接。验证通过 `cargo check --workspace`、`npm run typecheck`。 | Telegram 接入默认路径简化（token-only）、公网 webhook 依赖降低、入站编排一致性、用户配置复杂度下降 | AI |
| 2026-03-01 | 推进 `T-172` 第十四轮修复：修复 `apps/desktop-tauri/src-tauri/src/connectors/tunnel.rs` 中线程读取 cloudflared 输出时捕获 `*const AtomicBool` 导致 `E0277`（`*const AtomicBool` 非 `Send`）的编译失败，改为 `Arc<AtomicBool>` 在线程间共享状态并移除 unsafe 指针访问；同时将 `credential_store.rs` 的 `Command` 导入调整为仅在 `macos/linux` 编译目标启用，消除 Windows 下 `unused import` warning。验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | Windows `npm run dev:tauri` 编译可用性、tunnel connector 线程安全、连接器模块告警收敛 | AI |
| 2026-03-01 | 推进 `T-172` 第十三轮修复：针对 Windows `curl` 在 Telegram API 调用中出现 `schannel ... SSL/TLS connection failed`，在 Telegram connector 增加握手失败自恢复重试（命中 schannel 错误时自动追加 `-4 --http1.1 --ssl-no-revoke` 进行一次重试），降低 `channel_connector_health` 与 `channel_connector_webhook_sync` 的 TLS 握手脆弱性。验证通过 `cargo check --workspace`。 | Windows 环境 Telegram 健康检查稳定性、connector 网络鲁棒性、联调成功率 | AI |
| 2026-03-01 | 推进 `T-172` 第十二轮实现（对齐 openclaw gateway 的 webhook 配置思路）：`channel_connector_webhook_sync` 新增可选参数 `webhookUrl`（支持以公网 HTTPS 地址覆盖 runtime 本地地址），后端增加非 HTTPS 预校验并返回 `CHANNEL_CONNECTOR_WEBHOOK_INVALID`；设置页 Telegram 机器人配置新增“公网 Webhook URL（可选）”输入，`同步 Webhook` 按覆盖值执行；向导内 Telegram webhook 同步/健康检查改为“后置非阻塞”，避免因本地 `127.0.0.1` webhook 被 Telegram 拒绝而导致整单配置失败。验证通过 `npm run typecheck`、`cargo check --workspace`。 | Telegram webhook 联调可达性（公网 HTTPS）、设置页机器人配置可运维性、向导配置容错性、前后端契约一致性 | AI |
| 2026-03-01 | 推进 `T-172` 第十一轮实现：设置页新增“机器人配置”区块，支持 Telegram 机器人账户直接配置与运维动作（保存 Bot Token、刷新账户、手动 webhook 同步、健康检查），并在同区块展示账户快照（token 是否存在）与最近健康状态（status/botUsername/webhookMatched/checkedAt）；Feishu 入口保持可见并明确标记“connector 待实现”，避免误配。 | Channel 设置页机器人配置可达性、Telegram 接入联调效率、运维可观测性 | AI |
| 2026-03-01 | 修复 `T-173` 后续问题：`npm run dev:tauri` 在仓库目录迁移（如 `C:\\project\\vbCode -> C:\\personal\\vbCode`）后复用旧 `target/<platform>/debug/build` 产物，导致 `failed to read plugin permissions ... app_hide.toml`。在 `scripts/run-tauri-with-env.cjs` 增加启动前自检：扫描 `root-output` 是否落在当前 `CARGO_TARGET_DIR`，命中则定向清理 `debug/build` 与 `debug/.fingerprint` 下 `tauri-*` / `gtoffice-desktop-tauri-*` 缓存后再启动。验证通过 `node --check scripts/run-tauri-with-env.cjs`、`npm run dev:tauri`（可完成编译并运行 `gtoffice-desktop-tauri`）。 | Tauri 本地开发启动稳定性、跨目录迁移后的构建可恢复性、开发排障成本 | AI |
| 2026-03-01 | 推进 `T-172` 第十轮实现：新增 Telegram connector 基线能力（账户 upsert/list、健康检查、runtime webhook 同步），落地连接器配置持久化（`~app_data/channel/connectors.json`）与凭据引用字段（`botTokenRef/webhookSecretRef`）；`channel_adapter_status` 输出 connector 账户快照；新增事件 `external/channel_outbound_result` 与 `external/channel_connector_health_changed`；前端 `desktop-api` 同步新增 connector 契约与订阅类型，并将设置向导接入 Telegram connector 命令（保存账户 -> 自动 webhook 同步 -> 健康检查）。验证通过 `cargo check --workspace`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml channel_adapter_runtime::tests -- --nocapture`、`npm run typecheck`、`npm run build:web`。 | 外部通道连接器管理能力、设置向导可执行闭环、Telegram 健康闭环起点、事件可观测性、前后端契约一致性 | AI |
| 2026-03-01 | 推进 `T-172` 第九轮：基于 `openclaw - 快捷方式.lnk` 指向源码（`/mnt/c/personal/openclaw`）与官方文档完成 Telegram/Feishu channel 功能对标分析，明确插件契约、网关生命周期、路由匹配优先级、pairing/allowlist 准入、幂等去重与连接模式实现；同步产出 GT Office 分阶段落地计划（连接器拆分、凭据管理、状态持久化、出站回复闭环、联调验收路径）。本轮为方案收敛，未变更运行时代码。 | `T-172` 实施边界与里程碑拆解、后续开发顺序、风险识别 | AI |
| 2026-02-28 | 推进 `T-172` 第八轮收敛：按产品反馈移除 Channel 设置页顶部“刷新”按钮；Step1 增强 Telegram 配置引导，新增 Bot Token 输入并实时生成可复制的 `setWebhook/getWebhookInfo` 地址；补充 `Base URL` 与 webhook token 语义提示（区分 GT Office 通道密钥与 Telegram Bot Token） | 设置页配置引导可理解性、Telegram 接入操作闭环、误配风险降低 | AI |
| 2026-02-28 | 推进 `T-172` 第七轮收敛：为岗位路由补齐在线 runtime 映射链路，`agent_runtime_register` 新增 `roleKey`，外部入站在 `role:<role_key|role_id>` 解析时合并“岗位 agent 档案 + 在线 runtime(roleKey)”目标，避免仅依赖 agent 建档导致的岗位投递缺口；前端 runtime 注册同步上报岗位。验证通过 `npm run typecheck`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml channel_adapter_runtime::tests` | 岗位路由稳定性、外部通道到岗位实例的可达性、前后端运行时契约一致性 | AI |
| 2026-02-28 | 推进 `T-172` 第六轮实现：按产品要求将 channel 配置目标从“agent”升级为“岗位（role）”；设置页默认仅展示已添加 channel，向导 Step2 改为岗位选择（支持手动 role key 与刷新岗位列表）；后端新增 `role:<role_key|role_id>` 目标解析并在入站派发前展开岗位下 agent 集合，未命中岗位或岗位无可投递 agent 时返回结构化失败；同步更新 `docs/05`、`docs/06` 契约描述。验证通过 `npm run typecheck`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml` | 外部通道路由语义（agent -> role）、设置页展示策略、岗位派发编排一致性、文档契约一致性 | AI |
| 2026-02-28 | 完成 `T-174`：移除设置页“文件系统运行时设置”面板（`SettingsRuntimePane`），同步清理 `settingsRuntime.*` i18n 文案与对应 CSS，保留底层 settings 命令链路供 ShellRoot 读取 UI 偏好；补充排障：复现并处理 `npm run dev:tauri` 的 `os error 5`（`target/windows/debug/gtoffice-desktop-tauri.exe` 被残留进程占用），通过 `taskkill /f /im gtoffice-desktop-tauri.exe` 解除占用。验证通过 `npm run typecheck` | 设置弹窗信息架构简化、前端编译稳定性、Windows 开发态启动排障路径 | AI |
| 2026-02-28 | 推进 `T-172` 第五轮修复：按产品反馈修复向导可用性问题（1. 长文本撑开容器 -> 全链路 code 文本改为可换行；2. 向导滚动串扰外层弹窗 -> `overscroll-behavior` + 弹窗滚轮事件隔离 + body lock；3. Agent 不可选 -> 增加“刷新 Agent 列表”与手动输入 Agent ID；4. 第二步无法下一步 -> 放宽步骤门禁为仅校验目标 Agent；5. 补充 openclaw 风格具体配置提示（Telegram `setWebhook/getWebhookInfo`、Feishu `url_verification`）；6. 统一字号与友好提示文案）。验证通过 `npm run typecheck` | 设置页向导可用性、稳定性与可读性、机器人接入成功率 | AI |
| 2026-02-28 | 修复 `T-173`：`npm run dev:tauri` 在前端端口被占用时卡死 180s 的问题。`run-tauri-with-env.cjs` 新增动态端口解析（默认从 `5173` 起探测），并在 `tauri dev` 时通过 `--config` 同步覆盖 `build.beforeDevCommand` 与 `build.devUrl`，确保 Tauri 等待地址与 Vite 实际监听端口一致；保留显式 `--config/-c` 优先级。验证通过 `node --check scripts/run-tauri-with-env.cjs`、`node scripts/run-tauri-with-env.cjs dev --help`、`node scripts/run-tauri-with-env.cjs dev -c '{\"build\":{\"devUrl\":\"http://localhost:9999\"}}' --help` | Tauri 本地开发启动稳定性、端口冲突场景可用性、开发反馈速度 | AI |
| 2026-02-28 | 推进 `T-172` 第四轮实现：对标 openclaw 增强 webhook runtime 稳定性（请求读取超时、json content-type 校验、按通道限流、运行时指标计数与最近错误快照）并在 `system_gto_doctor` 增加稳定性检查项；设置页新增 Telegram/Feishu 接入能力并按产品反馈重构为“添加 Channel -> 弹窗分步向导（4 Step）”交互，向导内完成 webhook 绑定引导、路由绑定、准入策略与 identity 授权提交；UI 继续沿用苹果风格 token；同步更新 `docs/05`、`docs/06` 契约描述；验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml channel_adapter_runtime::tests`、`npm run typecheck` | 外部通道接入稳定性、设置页可运维能力、向导化配置闭环、诊断可观测性、文档契约一致性 | AI |
| 2026-02-28 | 推进 `T-172` 第三轮实现：任务中心新增“外部通道”可视化面板（沿用现有苹果风格 glass/token 体系），展示 runtime running 状态、Feishu/Telegram webhook 地址、路由/准入/幂等快照与最近外部事件（inbound/routed/dispatch/reply/error）；`ShellRoot` 接入 `channel_adapter_status + system_gto_doctor` 轮询刷新，并订阅 `external/*` 事件流；验证通过 `npm --workspace apps/desktop-web run build`、`npm run typecheck` | 任务中心可观测性、外部通道运行态可视化、问题定位效率、UI 一致性 | AI |
| 2026-02-28 | 推进 `T-172` 第二轮实现：新增桌面端内置 webhook runtime（`channel_adapter_runtime`），启动时自动生成 `feishu/telegram` 回调地址并写入 `~/.gtoffice/channel/runtime.json`；实现 `POST /webhook/feishu/<token>` 与 `POST /webhook/telegram/<token>` 入站解析与 token 鉴权，复用 `channel_external_inbound` 核心编排（路由/准入/幂等/派发）；`channel_adapter_status` 与 `system_gto_doctor` 增加 runtime 状态与诊断建议；前端 `desktop-api` 补充 runtime 字段类型；验证通过 `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml channel_adapter_runtime::tests`、`npm --workspace apps/desktop-web run build` | 外部通道 webhook 实接能力、Telegram/Feishu 入站统一链路、诊断可观测性、前后端契约一致性 | AI |
| 2026-02-28 | 启动 `T-172` 并完成首轮实现：`vb-task` 新增外部通道能力（路由绑定、准入策略、pairing、幂等缓存、doctor 快照）；Tauri 新增 `channel_adapter_status` / `channel_binding_upsert` / `channel_binding_list` / `channel_access_policy_set` / `channel_access_approve` / `channel_access_list` / `channel_external_inbound` / `system_gto_doctor` 命令；前端 `desktop-api` 同步新增通道管理与外部入站接口，补充 `external/*` 事件订阅类型；验证通过 `cargo check --workspace`、`cargo test -p vb-task`、`npm --workspace apps/desktop-web run build` | 外部通道接入基线、任务中心外部入站编排、准入与幂等防重、诊断与可观测事件链路、前后端接口一致性 | AI |
