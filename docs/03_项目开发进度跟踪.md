# GT Office 项目开发进度跟踪

> 基线日期：2026-02-06

## 1. 状态定义

- `TODO`：未开始
- `IN_PROGRESS`：进行中
- `BLOCKED`：受阻
- `DONE`：完成

## 2. 里程碑看板

| 里程碑 | 目标 | 计划周期 | 状态 | 完成度 |
|---|---|---|---|---|
| M0 需求冻结 | 完成需求/架构/规范文档 | W1 | DONE | 100% |
| M1 MVP 内核 | Workspace+文件树+Terminal+基础Git | W2-W5 | DONE | 100% |
| M2 MVP 完整 | 多窗口+Tool Adapter+发布候选 | W6-W8 | TODO | 0% |
| M3 V1 体验增强 | Change Feed+会话恢复+安全完善 | W9-W12 | TODO | 0% |

## 3. 当前任务清单

| ID | 模块 | 任务 | 优先级 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|---|---|
| T-135 | UI | xterm 自绘滚动条精细化（`.xterm-scrollable-element` 苹果风格） | P0 | DONE | AI+你 | 针对 xterm 自绘滚动条结构（`.scrollbar.vertical > .slider`）做局部覆盖：轨道半透明圆角、滑块细条渐变、hover 光泽与阴影过渡，并处理 `fade/invisible` 状态 |
| T-136 | UI | Workbench Canvas 顶部精简 + 布局预设 + 搜索定位滚动 | P0 | DONE | AI+你 | 删除画布顶部标题/描述；新增 `auto/1*1/1*2/2*1/2*2/3*2/4*2` 预设控制 `station-grid` 展示；搜索弹窗选择工位后自动滚动定位目标卡片 |
| T-137 | UI | StationCard 命令行预览/聚焦内容一致性修复 | P0 | DONE | AI+你 | 在 `StationCard` 增加本地终端快照缓存并在 `renderTerminal -> preview` 切换时优先使用最新快照生成预览，修复预览态落后于聚焦态的问题 |
| T-138 | UI | Workbench station-grid 在 terminal 区域的滚动链修复 | P0 | DONE | AI+你 | 在 `StationXtermTerminal` 增加 wheel capture 边界桥接：terminal 滚动到顶/底（或无内部滚动）时将滚动量转发到最近 `station-grid`，修复鼠标停留 terminal 内时偶发无法滚动工位网格的问题 |
| T-139 | Infra | Tauri 跨平台目标产物隔离（Windows/WSL target 污染修复） | P0 | DONE | AI+你 | 在 `run-tauri-with-env` 默认注入平台隔离的 `CARGO_TARGET_DIR`（`target/windows|linux|macos`），避免 Windows 构建误用 WSL 生成的 Linux `rlib` 触发 `E0461` |
| T-140 | UI | 顶部标题栏拖拽卡顿回归修复 | P0 | DONE | AI+你 | 在 `ShellRoot` 增加 `data-tauri-drag-region` 指针监听，拖动中切换 `body.vb-window-dragging`；在 `shell-layout.css` 拖动态禁用标题栏玻璃/阴影/动画并弱化背景光效，降低窗口移动时重绘负载 |
| T-141 | UI | Agent IDE 底栏信息重构（单行高价值信息 + 超长截断） | P1 | DONE | AI+你 | 重构 `StatusBar` 为单行状态带，移除 `Active Workspace`，并将 Git 分支放在最左侧；保留 Agent 在线/终端会话/Git 变更文件数；新增分支下拉切换能力（复用 Git 管理 checkout 链路）与超长文本截断，在窄宽度按优先级隐藏次要项以避免换行 |
| T-142 | Core | Workspace 生命周期闭环（会话持久化 + 恢复） | P0 | DONE | AI+你 | 完成 `.gtoffice/session.snapshot.json` 最小快照链路：Rust `restore_session` 真读取快照；前端接入 `workspace_restore_session` 恢复视图/文件/终端；新增防抖持久化与分批恢复策略，兼顾启动速度与交互流畅性 |
| T-143 | UI | 标题栏拖拽性能防回归（窗口移动事件兜底） | P0 | DONE | AI+你 | 在 `desktop-api` 增加 `subscribeWindowMoved`；`ShellRoot` 以窗口 `moved` 事件驱动 `body.vb-window-moving`（不再只依赖 `pointerdown`）；`shell-layout.css` 将 moving 态纳入降级样式并降低 xterm 自绘滚动条在移动期间的视觉开销 |
| T-144 | UI | 终端高频输出下拖拽卡顿抑制（未读计数合批） | P0 | DONE | AI+你 | 将后台终端未读计数从“每 chunk 直接 `setState`”改为短周期合批 flush，降低 `ShellRoot` 高频重渲染对窗口拖拽帧率的影响；切换工位时同步清理待刷增量避免误加 |
| T-145 | UI | 标题栏拖拽链路再收敛（移除高频 moved 桥接） | P0 | DONE | AI+你 | 回滚 `window.onMoved -> class` 高频桥接链路，改为 Windows 原生标题栏模式常驻轻量渲染（禁用重过渡/重合成）+ `pointer` 拖动态补充；同步移除 `desktop-api` 的 `subscribeWindowMoved`，避免再次因事件桥接导致拖拽掉帧 |
| T-146 | Core | 任务中心 2.0（多选派发 + Agent 传输通道） | P0 | DONE | AI+你 | Rust `vb-task` 新增无持久化内存通道（`channel/message` + `channel/ack`）与批量派发编排；任务中心升级为多选工位派发，前端接入 `task_dispatch_batch`，并用后端事件驱动工位“已接收”反馈 |
| T-147 | UI | 任务中心交互重构（Agent 下拉多选 + `@` 文件引用） | P0 | DONE | AI+你 | 目标选择改为 Agent 下拉多选并支持清空不回填；任务标题与内容融合为单 Markdown 输入；移除附件 UI；新增 `fs_search_files` + `@` 引用候选插入；重排任务中心布局并修复重叠体验问题 |
| T-148 | UI | 文件编辑器快捷键与内搜体验对齐（CodeMirror + VSCode 常规能力） | P0 | DONE | AI+你 | 完成 CodeMirror 搜索能力系统适配：`Ctrl/Cmd+F` 文件内搜索优先、`Ctrl/Cmd+Shift+F` 保留全局全文搜索、信息栏新增查找/替换/上一项/下一项快捷操作、搜索面板样式统一 |
| T-149 | UI | 文件管理入口误触发文件搜索弹窗修复 + 文件搜索弹窗滚动体验优化 | P0 | DONE | AI+你 | 修复 `fileSearchRequest` 残留导致点击“文件管理”偶发自动弹出搜索框；文件搜索弹窗改为固定工具栏高度，仅结果列表内部滚动，避免整窗滚动抖动 |
| T-151 | UI | 全局文件搜索快捷键切换（`Ctrl/Cmd+F` -> `Ctrl/Cmd+P`）并改为可配置路由 | P0 | DONE | AI+你 | `ShellRoot` 移除 `Ctrl/Cmd+F` 全局文件搜索回退，仅保留编辑器内查找；引入 `shortcut-bindings` 配置解析，默认键位+`keybindings.overrides` 覆盖，避免硬编码按键判断 |
| T-152 | UI/Core | 任务中心派发回执去重（气泡单次展示）+ 终端自动回车确认 | P0 | DONE | AI+你 | `ShellRoot` 为 `stationTaskSignals` 增加自动过期清理，避免切页重进后同一任务气泡重复弹出；`vb-task` 修复派发命令末尾回车，从字面量 `\\r` 改为真实回车字符，任务下发后终端自动执行 |
| T-153 | UI | StationCard terminal hover 手型指针与列表滚动受阻修复 | P0 | DONE | AI+你 | 在 `StationXtermTerminal` 中将未聚焦 terminal 的滚轮优先转发给 `station-grid`，并为 terminal 区域显式设置 `cursor: text`；跟进补齐未激活预览态（`station-terminal-preview-shell`）的光标与滚轮转发，修复“未聚焦 terminal 仍手型且列表无法滚动” |
| T-154 | UI | StationCard 单击直达终端输入（一次点击激活并聚焦命令行） | P0 | DONE | AI+你 | `StationCard` 新增“激活+确保终端会话+焦点”统一入口，卡片主体单击直接触发终端会话拉起与聚焦，避免需要二次点击才能进入命令行 |
| T-155 | UI | StationCard 取消预览模式（工位卡片统一实时终端渲染） | P0 | DONE | AI+你 | 移除 `StationCard` preview 分支，`WorkbenchCanvas/ShellRoot` 不再传递 preview 文本与渲染开关，工位卡片始终渲染 `StationXtermTerminal`；terminal 区点击同样走“激活+会话确保+焦点”，彻底消除 preview/live 内容不一致 |
| T-156 | UI | StationCard 点击语义重构（卡片激活 vs 命令行首击聚焦） | P0 | DONE | AI+你 | 卡片主体点击仅激活工位，不自动聚焦命令行；terminal 区点击改为 `pointerdown capture` 首帧触发“激活+会话确保+聚焦”，修复命令行首击需两次点击的问题 |
| T-157 | UI | 多终端切换聚焦闪烁修复（仅聚焦终端显示闪烁光标） | P0 | DONE | AI+你 | `StationXtermTerminal` 改为基于 `focusin/focusout` 控制 `cursorBlink`，非聚焦终端禁用闪烁；CSS 将非聚焦终端光标设为完全隐藏并取消过渡，修复两个终端切换时同时闪烁的问题 |
| T-158 | UI | 终端点击/焦点链路调试日志注入 + 终端重建抖动收敛 | P0 | DONE | AI+你 | 在 `StationCard`/`StationXtermTerminal`/`ShellRoot` 注入结构化调试日志（点击、focusin/out、cursorBlink、activeStation 变化）；修复 `StationCard.handleBindSink` 依赖 `active` 导致 `onBindSink` 引用抖动并触发 xterm effect 重建的问题，降低首击失焦与切换闪烁不稳定性 |
| T-159 | UI | Station terminal 首击输入与双闪终版修复（去竞态） | P0 | DONE | AI+你 | `StationXtermTerminal` 移除 `pointerdown capture` 事件阻断并增加微任务 refocus 兜底；`StationCard` 终端点击链路改为“仅激活（必要时拉起会话）+ 立即焦点兜底”，避免已存在会话时重复 launch/focus 竞争；`ShellRoot` 改为所有会话常显并移除 active 切换 snapshot 重置，消除首击输入延迟与双终端切换闪烁 |
| T-160 | UI | Station terminal 调试日志回收清理 | P0 | DONE | AI+你 | 清理 `StationCardDebug`/`StationTerminalDebug`/`StationFocusDebug` 日志函数与调用，保留 T-159 的焦点/可见性修复逻辑，降低控制台噪音 |
| T-161 | UI | Station terminal 滚轮链路优化（命令行优先滚动） | P0 | DONE | AI+你 | 调整 `StationXtermTerminal` wheel capture：移除“未聚焦即转发 `station-grid`”，改为“终端可滚动时优先消费；仅在顶/底边界或无内部滚动时转发外层”；并在二次修复中于 capture 阶段显式调用 `terminal.scrollLines` + 阻断冒泡，避免默认滚轮链路落到外层列表导致终端历史不滚动 |
| T-162 | UI | StationCard 命令行渲染稳定性加固（抖动/闪烁去竞态） | P0 | DONE | AI+你 | 在 `StationCard` 引入单通道 terminal focus 调度器（可取消 RAF + 有界重试 + DOM focus 校验），移除分散重复聚焦触发；并将“启动终端”按钮改为已有会话不重复拉起，降低会话切换时命令行闪烁与抖动 |
| T-163 | UI | StationCard 终端滚动条箭头去除（上下箭头隐藏） | P1 | DONE | AI+你 | 在 `shell-layout.css` 隐藏 xterm 自绘滚动条 `scra` 上下箭头，并隐藏 webkit `scrollbar-button`，保留滑块与滚动行为不变，消除滚动条区域上下箭头视觉噪音 |
| T-164 | UI/Core | 任务中心运行中会话自动提交兼容（多提交序列+即时下发） | P0 | DONE | AI+你 | 保持 `task_dispatch_batch.submitSequences` 契约并扩展提交序列兼容；批量派发确保“后端同 session 两段写入（命令+提交）”；重发链路复用提交策略并带 xterm 兜底；`vb-terminal/vb-daemon` 写入补 `flush`，修复 Codex/Claude 等运行中会话“仅输入不提交” |
| T-165 | UI | 小窗终端输出渲染缺失修复（可见尺寸就绪 + 刷新补偿） | P0 | DONE | AI+你 | `StationXtermTerminal` 初始化改为“可见尺寸就绪后 fit+refresh”，并在 `cols/rows<=0` 时持续重试 fit，输出写入做帧级 refresh 合并；修复小尺寸工位下启动工具有输出但文本不可见、放大后才出现的问题 |
| T-166 | UI/Core | StationCard 小窗终端稳定渲染 + 任务中心 xterm 自动提交收敛 | P0 | DONE | AI+你 | `StationCard` 增加小窗 compact 布局阈值（降低终端最小高度约束）以稳定小尺寸渲染；任务派发改为“后端仅写命令文本 + 前端 xterm `terminal.input('\\r')` 提交”，并强制顺序为“写命令成功后再 submit”，修复任务命令到达但未自动执行 |

## 4. 每周节奏模板

### Week N 目标

1. 本周交付：
2. 关键依赖：
3. 风险预警：

### Week N 复盘

1. 已完成：
2. 未完成与原因：
3. 下周调整：

## 5. 风险与阻塞登记

| 风险ID | 描述 | 影响 | 应对策略 | 状态 |
|---|---|---|---|---|
| R-001 | PTY 在不同平台行为差异 | 终端稳定性 | 做平台适配层+回归用例 | OPEN |
| R-002 | 大仓库文件树性能波动 | 使用体验 | 虚拟列表+懒加载+缓存 | OPEN |
| R-003 | Git 复杂场景兼容性 | 功能可靠性 | git2 + system git 双通道 | OPEN |
| R-004 | 本机缺少 Linux 系统库导致 Tauri 无法编译 | 开发阻塞 | 安装 pkg-config/webkit2gtk/rsvg2 等依赖 | CLOSED |
| R-005 | Agent 协作功能扩展导致架构复杂度上升 | 研发节奏与质量 | 采用阶段化落地（先本地权威存储，后 Redis 加速） | OPEN |

## 6. 变更记录

| 日期 | 变更内容 | 影响范围 | 执行人 |
|---|---|---|---|

| 2026-02-12 | 完成 `T-136`：优化 `workbench-canvas` 顶部信息层级，移除标题/描述并新增预设布局胶囊栏；新增 `auto/1*1/1*2/2*1/2*2/3*2/4*2` 动态布局预设，支持固定行列展示与虚拟列表行高联动；搜索弹窗选择工位后自动切换到工位视图并滚动定位至目标卡片；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 中央画布布局、搜索交互链路、虚拟化滚动体验 | AI |
| 2026-02-12 | 完成 `T-137`：修复 `StationCard` 命令行预览与聚焦展示不一致问题；在卡片内部对 sink `write/reset` 做本地快照同步，并在从聚焦态回到预览态时基于最新快照生成预览文本，避免预览使用滞后 `previewText`；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位卡片终端预览一致性、聚焦/非聚焦切换体验 | AI |
| 2026-02-12 | 完成 `T-138`：修复 station-grid 在 terminal hover 下滚动链偶发中断；新增 xterm wheel capture 边界逻辑，在 terminal 到达滚动边界或无可滚动内容时显式将滚动量转发给 `station-grid`，并在转发时阻断 terminal 继续消费事件；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位网格滚动交互、terminal 与父容器滚动协同 | AI |
| 2026-02-12 | 完成 `T-139`：修复 Windows 编译 `E0461`（误引用 WSL/Linux 目标产物）；在 `scripts/run-tauri-with-env.cjs` 中默认设置平台隔离 `CARGO_TARGET_DIR`，Windows/WSL/macOS 分别写入 `target/windows|linux|macos`，从根源避免跨平台 `target` 污染；`node --check scripts/run-tauri-with-env.cjs` 通过 | Tauri 构建链路稳定性、跨平台开发一致性 | AI |
| 2026-02-12 | 完成 `T-140`：修复顶部标题栏拖拽卡顿回归；在 `ShellRoot` 为 `data-tauri-drag-region` 增加拖动态 class 切换（`pointerdown -> body.vb-window-dragging`，`pointerup/cancel/blur` 清理），并在 `shell-layout.css` 拖动中禁用顶部栏高开销特效与过渡、降低背景光效层开销；进一步在 `TopControlBar` 与 Windows 样式中将 `vb-workspace-badge` 移出拖拽区、扩大纯净 `titlebar-drag-region` 并关闭标题栏动画过渡；同时在 Windows 原生标题栏模式禁用 `AmbientBackgroundLighting`，减少整窗移动时背景层重绘压力；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 原生标题栏拖拽体验、顶部样式渲染性能 | AI |
| 2026-02-12 | 完成 `T-141`：重构底栏为“单行高价值信息带”，移除低价值静态指标，仅展示激活工作区、Agent 在线、终端会话、Git 分支、Git 变更文件数；新增超长文本处理（单行省略 + `title` 完整提示），并在窄宽度按优先级隐藏次要字段以保持单行；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 底栏信息架构、Agent 协作态可读性、响应式稳定性 | AI |
| 2026-02-12 | 完成 `T-141` 细化：按产品要求移除底栏 `Active Workspace`；将 Git 分支移动到最左并改为可切换下拉；复用 `useGitWorkspaceController` 的 checkout 链路，新增 `checkoutTo(target)` 供底栏与 Git 面板共享；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 底栏信息优先级、Git 分支切换可达性、Git 能力复用一致性 | AI |
| 2026-02-12 | 完成 `T-141` 样式精修：优化底栏分支下拉视觉质感，新增胶囊容器层、自定义箭头、hover/focus 光效与禁用态，并保持紧凑高度与单行稳定；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 底栏分支控件精致度、一致性与可用性 | AI |
| 2026-02-12 | 完成 `T-141` 视觉问题修复：修复底栏分支下拉“重影/发虚”观感；移除分支控件双重焦点光圈叠加（覆盖全局 `select:focus` 光效），并禁用该控件 `translateZ(0)` 强制合成层，降低文字边缘虚化；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 底栏分支下拉清晰度、焦点反馈一致性 | AI |
| 2026-02-12 | 完成 `T-141` 重复箭头修复：修复底栏分支下拉出现两个向下箭头的问题；为分支下拉增加 `custom-select` 标识，避开全局 `select:not([class*=\"custom\"])` 自动 SVG 箭头注入，并在局部样式强制 `background-image: none`；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 底栏分支控件视觉正确性 | AI |
| 2026-02-12 | 完成总体需求评估：确认下一步核心功能为 `T-142`（Workspace 会话持久化与恢复闭环）；依据 `docs/01` 的 `FR-01`、`docs/05` 的 `AC-001` 及当前代码现状（`vb-workspace` 仍为内存态且 `restore_session` 返回默认空快照）确定优先级；本轮未改动业务代码，仅更新任务与交接文档 | 里程碑收尾决策、后续开发入口统一 | AI |
| 2026-02-12 | 完成 `T-142`：实现 Workspace 会话持久化与恢复闭环。后端 `vb-workspace` 从 `.gtoffice/session.snapshot.json` 读取 `windows/tabs/terminals`；前端新增 workspace session model（parse/serialize/build），接入 `workspace_restore_session` 恢复 active view/文件 tab/终端会话，并采用“活跃 tab 优先 + 其余分批恢复 + 快照防抖写入”策略以提升启动与交互性能；`cargo test -p vb-workspace`、`cargo check --workspace`、`npm --workspace apps/desktop-web run build` 全部通过（web build 仅 chunk size warning） | Workspace 生命周期、前后端会话恢复链路、启动性能与交互流畅性 | AI |
| 2026-02-12 | 完成 `T-143`：修复标题栏拖拽再次回归卡顿。定位到 `app-region: drag` 下 `pointerdown` 触发不稳定导致拖动态降级样式失效；新增窗口 `moved` 事件兜底（`subscribeWindowMoved -> body.vb-window-moving`），并将 moving 态纳入顶部栏/背景层/xterm 自绘滚动条降级规则；同步在 `docs/05` 增加标题栏拖拽性能防回归约束与 `AC-029` 验收用例；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 标题栏拖拽跟手性、防回归策略、文档化验收基线 | AI |
| 2026-02-12 | 完成 `T-144`：继续修复“仍卡”场景。定位到后台终端高频输出时未读计数按 chunk 触发 `setStationTerminals`，造成 `ShellRoot` 高频重渲染；改为未读增量短周期合批 flush（约 84ms）并在清空未读时剔除待刷增量；同步在 `docs/05` 增加 US-13 与 WF-13 的“高频输出合批”防回归约束与 `AC-030` 用例；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 高频输出场景下拖拽帧率稳定性、终端未读反馈链路 | AI |
| 2026-02-12 | 完成 `T-145`：处理“仍卡”最终回归。确认 `window.onMoved` 高频事件桥接本身会放大拖拽期间主线程负担，移除 `subscribeWindowMoved` 与 `vb-window-moving` 链路；Windows 原生标题栏模式改为常驻轻量渲染（禁用高成本合成/广域过渡/标题栏 tooltip 动画）并保留 `pointer` 拖动态补充；更新 `docs/05` 防回归条款，明确禁止将高频窗口移动事件作为拖拽样式主触发链路；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 标题栏拖拽主链路稳定性、Windows 合成负载控制、防回归策略 | AI |
| 2026-02-12 | 完成 `T-146`：任务中心升级为多选 Agent 批量派发，Rust `vb-task` 落地无持久化通道抽象与在线必达语义（离线失败）；Tauri 新增 `task_dispatch_batch` / `channel_publish` / `agent_runtime_register` / `agent_runtime_unregister`；前端接入批量派发接口、Agent runtime 注册同步与 `channel/message` 订阅，工位卡片按后端事件展示“已接收”反馈；`cargo check --workspace`、`cargo test -p vb-task`、`npm --workspace apps/desktop-web run build` 通过 | 任务中心主流程、Agent 通信抽象、前后端契约与实时反馈链路 | AI |
| 2026-02-12 | 完成 `T-147`：任务中心交互重构为 Agent 下拉多选（支持清空且不自动回选）+ 单输入 Markdown 编辑；移除附件输入与附加当前文件入口；新增 Rust `fs_search_files` 与前端 `@` 文件引用候选链路，插入工作区相对路径；同步重排任务中心布局样式，修复输入与按钮重叠；`cargo check --workspace`、`cargo test -p gtoffice-desktop-tauri fs_search_files_payload_keeps_contract_fields`、`npm --workspace apps/desktop-web run build` 通过 | 任务中心交互效率、文件引用效率、前后端搜索契约 | AI |
| 2026-02-12 | 完成 `T-147` 跟进修复：将目标 Agent 选择改为原生 `select multiple`，彻底消除下拉浮层与任务描述区重叠；发送按钮改为任务描述框内右下角内嵌小按钮，缩短操作动线并优化尺寸；`npm --workspace apps/desktop-web run build` 通过 | 任务中心目标选择稳定性、编辑发送动线与可用性 | AI |
| 2026-02-12 | 完成 `T-147` 二次跟进：目标 Agent 选择升级为“UI 库风格 tag 多选”（标签展示 + 搜索下拉 + 点击即选），下拉改为 `portal` 挂载避免与任务描述区重叠；保留描述框内右下角小号发送按钮；`npm --workspace apps/desktop-web run build` 通过 | 任务中心多选交互可用性、视觉一致性、浮层层级稳定性 | AI |
| 2026-02-12 | 完成 `T-148`：针对现有 `CodeMirror 6` 文件编辑器完成交互对齐与系统适配；新增编辑器命令通道（查找/替换/下一项/上一项），修复全局快捷键抢占导致 `Ctrl/Cmd+F` 无法文件内搜索的问题，保留 `Ctrl/Cmd+Shift+F` 全局全文搜索；文件编辑信息栏新增快捷操作按钮并统一搜索面板视觉；同步补充 `docs/05` 的 US-25/WF-20/AC-032；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | 文件编辑器快捷键体系、搜索交互一致性、文件页可用性与视觉一致性 | AI |
| 2026-02-12 | 完成 `T-148` 跟进：按产品反馈移除文件编辑器顶部“查找/替换/上一项/下一项”按钮与信息栏左侧文件名/路径展示，仅保留状态提示；同步清理无效透传参数；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | 文件编辑器顶部信息层级简化、交互噪音收敛 | AI |
| 2026-02-12 | 完成 `T-149`：修复点击左侧“文件管理”偶发触发文件搜索弹窗的问题；在 `FileTreePane` 增加搜索请求消费回执并由 `ShellRoot` 在消费后清空 `fileSearchRequest`，避免旧请求在面板重挂载时重复触发；同时优化文件搜索弹窗布局，搜索工具栏高度固定，滚动仅在文件结果列表内部发生；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | 文件管理入口稳定性、文件搜索弹窗滚动与布局稳定性 | AI |
| 2026-02-12 | 完成 `T-148` 国际化跟进：将 CodeMirror 搜索面板文案（Find/Replace/next/previous/replace all 等）接入 `locale`，中英文切换时可显示对应文案；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | 文件编辑器内搜索面板 i18n、双语一致性 | AI |
| 2026-02-12 | 完成 `T-153`：修复 `StationCard` terminal hover 时出现手型并导致 station-grid 滚动受阻的问题；在 `StationXtermTerminal` 的 wheel capture 中新增“未聚焦 terminal 优先转发至 `station-grid`”逻辑，保留聚焦态下终端边界滚动桥接；在 `shell-layout.css` 为 terminal 区域显式设置 `cursor: text`，避免继承卡片 `cursor: pointer`；`npm --workspace apps/desktop-web run build` 未通过（受既有 `src/shell/layout/ShellRoot.tsx(2278,53): TS6133` 阻塞，与本次改动无关） | WebUI 工位卡片终端 hover 交互、station-grid 滚动连续性 | AI |
| 2026-02-12 | 完成 `T-153` 跟进修复：覆盖未激活工位预览态（`station-terminal-preview-shell`）场景；在 `StationCard` 预览区新增 wheel capture 并把滚轮转发到 `station-grid`，避免预览 `pre` 吞滚轮；在 `shell-layout.css` 为预览区显式设置 `cursor: default`，彻底去除未聚焦 terminal 手型；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位卡片未聚焦预览态交互、station-grid 滚动连续性 | AI |
| 2026-02-12 | 完成 `T-154`：优化 StationCard 点击链路为“单击直达命令行”；新增 `activateStationAndOpenTerminal` 统一触发激活/会话确保/焦点，卡片主体单击即可进入可输入终端，终端按钮复用同一入口避免重复点击；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位卡片点击效率、终端会话拉起与聚焦一致性 | AI |
| 2026-02-12 | 完成 `T-155`：取消 StationCard 终端预览模式；`StationCard` 始终渲染 `StationXtermTerminal`，删除 preview 文本与 `renderTerminal` 分支；`WorkbenchCanvas`/`ShellRoot` 同步移除 `terminalPreviewByStation` 与 `buildTerminalPreview` 链路；`StationXtermTerminal` 新增 `onActivateStation`，点击 terminal 区域也会触发“激活+会话确保+焦点”；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位卡片终端内容一致性、点击行为一致性 | AI |
| 2026-02-12 | 完成 `T-156`：按产品期望重构 StationCard 点击语义；卡片主体点击改为“仅激活工位”（不自动命令行聚焦），terminal 区点击改为 `pointerdown capture` 首次点击即触发“激活+会话确保+焦点”，并阻断冒泡避免被卡片点击语义覆盖；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位卡片点击行为一致性、命令行首击聚焦可靠性 | AI |
| 2026-02-12 | 完成 `T-157`：修复多终端切换聚焦时双光标闪烁；`StationXtermTerminal` 改为监听宿主 `focusin/focusout` 动态切换 `cursorBlink`（仅焦点终端启用），并在 `shell-layout.css` 将 `.station-terminal-shell:not(:focus-within) .xterm .xterm-cursor` 设为 `opacity: 0` 且移除过渡，避免切换瞬间旧终端光标残留闪动；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 终端聚焦视觉稳定性、工位间切换体验 | AI |
| 2026-02-12 | 完成 `T-158`：按反馈加入终端点击/焦点链路调试日志并继续稳定性修复；`StationCard` 新增 `StationCardDebug` 日志（卡片点击、终端启动请求、sink 绑定与 raf 聚焦），`StationXtermTerminal` 新增 `StationTerminalDebug` 日志（pointerdown/click、focusin/out、cursorBlink、wheel 转发）并引入全局 `station-terminal-shell-focused` 独占类；`ShellRoot` 新增 `activeStation` 变化日志；同时将 `handleBindSink` 改为不依赖 `active`（使用 ref），避免 active 切换触发 `onBindSink` 引用变化导致 xterm effect 重建；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 焦点问题可观测性、终端切换稳定性、后续定点修复效率 | AI |
| 2026-02-13 | 完成 `T-159`：基于用户日志定点修复“terminal 首击无法立即输入 + 两终端切换双闪”；`StationXtermTerminal` 取消 `onPointerDownCapture` 的 `stopPropagation` 并增加 `queueMicrotask` refocus 兜底，避免拦截 xterm 首击内部事件；`StationCard` 新增 `activateStationFromTerminal`（终端区点击只激活工位，已有会话不重复 launch）并在 `requestTerminalFocus` 增加 immediate focus；`ShellRoot` 取消 active-only `terminalSetVisibility` 与 active 切换 `terminalReadSnapshot` 重置，改为会话常显；`shell-layout.css` 改为 `.station-terminal-shell:focus-within` 控制光标显示并将卡片默认 cursor 调整为 `default`；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 终端首击聚焦可靠性、切换光标稳定性、station-grid hover 交互一致性 | AI |
| 2026-02-13 | 完成 `T-160`：按用户确认“问题已解决”后清理调试日志；移除 `StationCard.tsx`、`StationXtermTerminal.tsx`、`ShellRoot.tsx` 中所有 `StationCardDebug/StationTerminalDebug/StationFocusDebug` 输出与辅助函数，保留首击聚焦、终端会话常显、cursor `:focus-within` 等修复逻辑；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 控制台噪音收敛、终端交互修复稳定保留 | AI |
| 2026-02-13 | 完成 `T-161`：修复“鼠标悬停命令行滚轮时外层列表抢滚，终端历史不可滚动”；在 `StationXtermTerminal` 的 `onWheelCapture` 中新增终端可消费滚动量判断（依据 viewport `scrollTop/scrollHeight/clientHeight`），终端可滚动时不再转发；仅在终端到达边界或无内部滚动时桥接到 `station-grid`；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位终端滚轮优先级、命令行历史可达性、外层滚动边界桥接稳定性 | AI |
| 2026-02-13 | 完成 `T-161` 二次修复：针对“仍然无法在命令行内滚动”场景，将滚轮消费判断切换为 xterm buffer 指标（`viewportY/baseY`），并在 terminal 可消费滚轮时由 `StationXtermTerminal` 主动执行 `terminal.scrollLines`、同时 `preventDefault + stopPropagation`，彻底阻断滚轮落到外层 `station-grid`；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI 工位终端滚轮消费确定性、xterm 自绘滚动条场景稳定性 | AI |
| 2026-02-12 | 完成 `T-151`：按产品要求取消 `Ctrl/Cmd+F` 全局文件搜索入口并统一为 `Ctrl/Cmd+P`；新增 `apps/desktop-web/src/shell/state/shortcut-bindings.ts`，将全局/编辑器快捷键改为“默认值 + `keybindings.overrides` 覆盖”的可配置匹配；同步修正文件搜索文案与契约文档，且顺带清理 `ShellRoot` 中阻塞构建的 `TS6133` 未使用类型导入；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | 全局快捷键路由、文件搜索入口一致性、快捷键配置扩展性、前端构建基线 | AI |
| 2026-02-12 | 完成 `T-152`：修复任务中心回执重复冒泡与终端确认问题；`ShellRoot` 新增 `stationTaskSignals` 定时清理机制（信号展示后自动删除），避免在切换导航后重新进入页面时重复弹出历史“任务收到”气泡；`vb-task` 将派发命令末尾从字面量 `\\r` 改为真实回车字符，任务消息写入终端后自动执行；新增 `dispatch_batch_terminal_command_appends_real_enter` 单测防回归；`npm --workspace apps/desktop-web run build`、`cargo test -p vb-task`、`cargo check --workspace` 通过；`cargo fmt --all --check` 未通过（仓库既有未格式化文件，与本次改动无关） | 任务中心派发反馈一致性、StationCard 交互体验、任务下发终端执行链路 | AI |
| 2026-02-13 | 完成 `T-162`：修复 `StationCard` 命令行渲染抖动/闪烁；将焦点请求改为单通道调度（`pending + cancelable RAF + bounded retry + :focus-within` 校验），移除原先分散在 `requestTerminalFocus/handleBindSink/active effect` 的重复聚焦触发，避免焦点竞态导致命令行闪烁；并将“启动终端”按钮改为已存在会话时仅聚焦不重复拉起；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI StationCard 命令行渲染稳定性、焦点链路一致性、会话切换抖动控制 | AI |
| 2026-02-13 | 完成 `T-163`：按需求微调 StationCard 终端滚动条区域，去除上下箭头；在 `shell-layout.css` 中隐藏 xterm 自绘滚动条箭头节点（`.scrollbar.vertical > .scra`）并屏蔽 webkit `::-webkit-scrollbar-button`，保留滑块拖动与滚轮滚动行为；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | WebUI StationCard 终端滚动条视觉简化、滚动交互连续性 | AI |
| 2026-02-13 | 完成 `T-164`：修复任务中心向“运行中且等待输入”的 CLI 会话派发任务后未自动提交问题；`task.dispatch_batch` 新增 `submitSequences`（按目标工位提交键序列），`ShellRoot` 记录用户真实 Enter 序列并按工位/工具回传；后端仅接受白名单序列并回退默认；派发与重发链路改为“命令写入 + 提交键序列写入”两次发送，不再把回车控制字符拼接在任务命令文本中；`vb-terminal` 与 `vb-daemon` 写入后补 `flush` 保障提交字节即时下发；新增 `dispatch_batch_honors_target_submit_sequence_override` 单测防回归；`cargo test -p vb-task`、`npm --workspace apps/desktop-web run build`、`cargo check --workspace` 全部通过（web build 仅 chunk size warning） | 任务中心派发自动提交兼容性、运行中 CLI（Codex/Claude）接收任务执行链路、前后端一致性 | AI |
| 2026-02-13 | 完成 `T-165`：修复工位小窗终端“有输出但不显示，放大后才可见”；`StationXtermTerminal` 启动阶段改为尺寸就绪后 `fit+refresh`，`ResizeObserver` 在尺寸变化时补偿重排，输出写入通过 `requestAnimationFrame` 合并刷新，避免小窗渲染丢帧；`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning） | 小尺寸终端渲染稳定性、工具启动日志可见性、工位窗口缩放一致性 | AI |
| 2026-02-13 | 完成 `T-164/T-165` 回归修复：针对“任务仅输入不提交”与“小窗光标下移但内容不显示”复现问题，批量派发改为“后端只写命令，前端优先 xterm 仿真 Enter（失败回退 submitSequence）”，并扩展提交序列兼容（`CR/LF/CRLF/CSI-u/CSI~ /modifyOtherKeys/SS3 Enter`）；`StationXtermTerminal` 增加 `cols/rows<=0` 持续 fit 重试与写入前补偿；`cargo test -p vb-task`、`npm --workspace apps/desktop-web run build`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml` 通过 | 任务自动提交可靠性、小窗终端渲染稳定性、跨协议兼容性 | AI |
| 2026-02-13 | 完成 `T-164/T-165` 二次回归修复：定位到会话竞态导致“命令与提交可能落到不同 session”；`ShellRoot` 在创建终端会话时即时同步 `stationTerminalsRef`，避免提交阶段二次建会话；批量派发恢复为后端同 session 两段写入（命令+提交）确保顺序一致；保留提交序列兼容扩展与小窗 `cols/rows<=0` 重试；`cargo test -p vb-task`、`npm --workspace apps/desktop-web run build`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml` 通过 | 任务自动提交顺序一致性、会话映射稳定性、小窗终端渲染稳定性 | AI |
| 2026-02-13 | 完成 `T-166`：修复 `StationCard` 小窗终端渲染不稳定与任务中心自动提交失效；`StationCard` 引入基于尺寸阈值的 compact 布局并下调 terminal 最小高度约束，提升小窗口下可见区域稳定性；`StationXtermTerminal.submit` 改为 xterm 原生 `terminal.input('\\r', true)`；`ShellRoot` 改为“命令写入成功后再 submit”，并在 submit sink 不可用时返回 `XTERM_SUBMIT_FAILED`；Tauri `task_dispatch_batch` 改为只写命令文本，不再追加提交序列；`npm --workspace apps/desktop-web run build`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml` 通过（web build 仅 chunk size warning） | StationCard 小窗终端稳定性、任务中心自动执行确定性、前后端提交链路一致性 | AI |
