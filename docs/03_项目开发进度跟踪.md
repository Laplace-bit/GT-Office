# GT Office 项目开发进度跟踪

> 基线日期：2026-02-06

## 1. 状态定义

- `TODO`：未开始
- `IN_PROGRESS`：进行中
- `BLOCKED`：受阻
- `DONE`：完成

## 2. 里程碑

| 里程碑 | 目标 | 计划周期 | 状态 | 完成度 |
|---|---|---|---|---|
| M0 需求冻结 | 完成需求/架构/规范文档 | W1 | DONE | 100% |
| M1 MVP 内核 | Workspace + 文件树 + Terminal + 基础 Git | W2-W5 | DONE | 100% |
| M2 MVP 完整 | 多窗口 + Tool Adapter + 发布候选 | W6-W8 | IN_PROGRESS | 85% |
| M3 V1 体验增强 | Change Feed + 会话恢复 + 安全完善 | W9-W12 | TODO | 0% |

## 3. 当前任务（精简视图）

| ID | 模块 | 任务 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|---|
| T-171 | Core/Infra | 多 Agent MCP 桥接（任务中心解耦派发 + 多 CLI 手动安装） | P0 | DONE | 已落地进阶 Agent 配置中心、Claude 深配、Codex/Gemini 轻配与安全预览/应用/审计闭环；MCP bridge 改为用户在 Agent 管理卡片中单独手动安装。 |
| T-172 | Core/Infra | 外部通道适配基线（Feishu/Telegram 路由、准入、幂等、回传） | P0 | IN_PROGRESS | 已切到 session-log finalize + rendered-screen preview/interaction 双源链路；本轮补齐 Codex provider session 显式绑定、session-log delta finalize 与后台 blocking 读取；待 Telegram 真机回归与更多 provider session log 扩展 |
| T-185 | Core/Infra | Workspace / 文件工作区初始化性能治理（目录 I/O 去阻塞 + watcher 去风暴 + 会话懒恢复） | P0 | IN_PROGRESS | 已完成首轮链路治理：文件系统命令 blocking worker 化、watcher 事件后端合批、文件树目录加载 singleflight、非活动 tab 懒恢复；仍待真实超大仓库 profile 与 terminal restore 进一步收口 |
| T-190 | Core/Infra/UI | Agent 供应商扫描增强与安装/卸载动作收口 | P0 | DONE | 已将 CLI 扫描扩展为 PATH + 常见安装目录（fnm/nvm/volta/homebrew 等）综合检测，并补齐 Codex/Gemini 卸载能力；前端不再因缺 Node/npm 直接禁用安装按钮，而是展示可执行错误与诊断。 |
| T-194 | UI/Settings | Agent 供应商设置页打开性能治理（首帧优先 + 局部 loading） | P0 | DONE | 已将设置页已访问 tab 保活，Agent 供应商页改为首帧立即渲染卡片框架，CLI/MCP 检测延后到首帧后后台执行；Rust 侧 `ai_config_read_snapshot` 与 agent 检测改走 blocking worker，避免设置页交互被重 I/O 阻塞。 |
| T-191 | UI/Shell | 启动时误自动打开默认目录修复 | P1 | DONE | 已移除 `workspacePathInput` 的硬编码默认路径 `/mnt/c/project/vbCode`，避免首次启动时被“路径变更自动打开”逻辑误触发；现在仅在存在真实记忆工作区时执行恢复。 |
| T-192 | Core/Infra | 终端会话继承已扫描 Agent CLI 路径 | P0 | DONE | 已在 `terminal.create` 的环境增强链路中，将扫描到的 Claude/Codex/Gemini 可执行文件目录 prepend 到终端 `PATH`，并写入 `GTO_*_COMMAND` override，避免应用内新终端会话因未加载用户登录 shell 而找不到已安装 CLI。 |
| T-189 | Core/Infra | macOS Tauri 打包链路修复（DMG bundling 递归污染与错误透传） | P0 | DONE | 已修复 `run-tauri-with-env.cjs` 对 Tauri CLI 对象错误的二次崩溃；macOS `build` 改为 `--bundles app` 后自管 `hdiutil` 生成 DMG，避开内置 `bundle_dmg.sh` 将中间 `rw.*.dmg` 写入源目录导致的递归打包/空间耗尽。 |
| T-193 | UI/File Explorer | 文件管理列表 Apple 风格重构与格式图标语义统一 | P1 | DONE | 已完成文件树、搜索结果、编辑器标签三处统一文件视觉语义（Lucide 分类 + 类型色），并保留虚拟列表与快捷键交互稳定性。 |
| T-187 | UI | 响应式样式单位重构（px -> rem/clamp/fluid） | P1 | DONE | 已完成 Shell、Git、Settings、Task Center、Workspace Hub、File Explorer、Tool Adapter 及 foundations/tokens 的全量迁移；业务源码 `px`/`px-to-rem(...)` 命中归零，构建通过。 |
| T-184 | Core/Infra | Git 管理模块性能治理（状态刷新去风暴 + 后端阻塞隔离） | P0 | DONE | 已切断 `git.status -> git/updated -> git.status` 回环，按操作类型缩减前端全量刷新，并补齐 Windows GUI 下 system git 静默启动，避免 workspace 绑定/启动刷新 Git 时弹出控制台 |
| T-175 | UI | 样式系统工程化重构（CSS -> SCSS 分层治理） | P1 | IN_PROGRESS | 已补齐 AI provider 弹窗类型治理、清除 `any` 滥用，并将通信通道侧栏重新归并回 `tool-adapter`；已完成“通信通道”聊天记录化改造（苹果风左右气泡）；待继续拆分 legacy foundations 与补手工 UI 回归 |
| T-186 | UI/Shell | macOS 原生窗口首击与触控板点击兼容性修复 | P1 | DONE | 已开启 WebView `acceptFirstMouse`，并把顶栏拖拽/双击最大化的 Windows 语义从 macOS 隔离，恢复单击即可触发按钮 |
| T-183 | Core/Infra | Tauri 后端 command 层按前端 feature 模块化 | P1 | DONE | `src/commands` 已改为 `workspace/file_explorer/terminal/git/task_center/tool_adapter/settings/keybindings` 目录结构，并完成内部引用迁移 |
| T-182 | Core/Infra | Rust 后端目录整理（测试归位 + 辅助脚本出源码树） | P1 | DONE | 已将 Tauri Rust 测试文件归到 `src/tests` / `src/commands/tests`，并把 `split_tests.py` 迁到 `scripts/dev/rust/` |
| T-181 | Docs | 文档瘦身与 AGENTS 规范重构（降 token 成本） | P1 | DONE | 已重写 AGENTS 与 docs 关键链路文档 |

## 4. 风险与阻塞

| 风险ID | 描述 | 影响 | 应对策略 | 状态 |
|---|---|---|---|---|
| R-001 | PTY 跨平台行为差异 | 终端稳定性 | platform适配层 + 回归用例 | OPEN |
| R-002 | 大仓库文件树性能波动 | 使用体验 | 虚拟列表 + 懒加载 + 缓存 + watcher 合批 | OPEN |
| R-003 | Git 复杂场景兼容性 | 功能可靠性 | `git2 + system git` 双通道 | OPEN |
| R-005 | Agent 协作扩展导致复杂度上升 | 研发节奏与质量 | 分阶段落地，先本地权威存储后加速层 | OPEN |

## 5. 变更记录（最近）

| 日期 | 任务ID | 变更内容 | 影响范围 | 执行人 |
|---|---|---|---|---|
| 2026-03-21 | T-175 | 按用户反馈回调 `StationXtermTerminal` 终端背景配色：恢复 `--vb-terminal-bg / --vb-terminal-bg-top` 到 2026-03-20 改动前的暖色浅底与经典深色底，并移除 `StationCard` 中终端舱体新加的冷色渐变，保留其余选择态与滚动条增强。验证：`cd apps/desktop-web && npm run build`。 | Terminal 视觉主题、Station 卡片终端背景层 | AI |
| 2026-03-20 | T-194 | 优化设置页 Agent 供应商 tab 的打开体验与检测性能：1. `SettingsModal` 为已访问 tab 保持挂载，避免在“通用/快捷键/通道/Agent 供应商”之间切换时重复冷启动；2. `AiProvidersSection` 改为首帧先渲染专业卡片框架与顶部后台检查提示，再在 `requestAnimationFrame + setTimeout(0)` 后异步拉取 snapshot；3. provider 卡片新增局部 loading 状态，CLI/MCP 检查期间仅在卡片内部展示“正在检查本机环境”，不再整页阻塞；4. `ai_config_read_snapshot`、`agent_install_status`、`agent_mcp_install_status` 改为 `spawn_blocking`，`vb-ai-config` 内部对 3 个 agent 的安装态与 MCP 检查并行化。验证：`cd apps/desktop-web && npm run build`、`cargo check -p gtoffice-desktop-tauri`。 | 设置页切换体验、Agent 供应商卡片首帧、CLI/MCP 检测路径、Tauri 后台阻塞隔离 | AI |
| 2026-03-20 | T-172 | 收口 channel agent 消息解析准确率与前端不卡顿约束：1. `AgentRuntimeRegistration` 新增 `providerSession`，支持回传 `providerSessionId/logPath/sessionStartedAtMs/discoveryConfidence`；2. `vb-session-log` 为 Codex 增加显式 session 元数据优先绑定、`cwd + 时间窗 + prompt anchor` 回退发现、增量 `delta` 与 truncate 容错；3. `app_state` 将发现到的 provider session 回写 runtime，并把 Codex `finalize` 改为只发送相对 preview 的新增尾段；4. channel flush worker 的 session-log 刷新改走 `spawn_blocking`，避免阻塞前端线程；5. 为便于肉眼调试，外发消息末尾临时追加 `[source=... confidence=... phase=...]` 标签。验证：`cargo check -p vb-session-log`、`cargo check -p vb-task`、`cargo check -p gtoffice-desktop-tauri`、`cargo test -p vb-session-log -- --nocapture`、`cargo test -p gtoffice-desktop-tauri session_log_finalize_delta_returns_only_unseen_tail -- --nocapture`。 | 外部通道 reply relay、Codex session log 绑定、runtime 元数据、channel finalize 语义、后台 I/O 调度、消息来源调试 | AI |
| 2026-03-20 | T-193 | 按用户三轮反馈继续收口：1. Git 状态关联文件的文件名颜色同步为状态色；2. Git 状态向上汇总到父目录（递归到根层级目录），目录名也按聚合状态着色；3. 保留文件尾标 `M/A/D/R/?/U` 与既有交互性能路径。验证：`cd apps/desktop-web && npx eslint src/features/file-explorer/FileTreePane.tsx src/features/file-explorer/FileSearchModal.tsx src/features/file-explorer/FileEditorPane.tsx src/features/file-explorer/file-visuals.ts`、`cd apps/desktop-web && npm run build`。 | 文件树 Git 状态可见性、目录级状态感知、文件名颜色语义一致性 | AI |
| 2026-03-20 | T-193 | 按用户二轮反馈并结合 `ui-ux-pro-max` 收口文件树细节：1. 去掉文件图标边框（文件树/搜索结果/编辑器标签一致）；2. 提升文件名文字对比度，避免“发灰”；3. 文件树新增 VSCode 风格 Git 状态尾标（`M/A/D/R/?/U`）并用状态色区分，状态由 `git_status` + `git/updated` + 文件系统变更节流刷新驱动；4. 保持虚拟列表与快捷键交互不变。验证：`cd apps/desktop-web && npx eslint src/features/file-explorer/FileTreePane.tsx src/features/file-explorer/FileSearchModal.tsx src/features/file-explorer/FileEditorPane.tsx src/features/file-explorer/file-visuals.ts`、`cd apps/desktop-web && npm run build`。 | 文件树可读性、文件图标视觉纯净度、Git 状态感知、搜索/标签视觉一致性 | AI |
| 2026-03-20 | T-193 | 使用 `ui-ux-pro-max` 完成文件管理模块视觉增强：1. 新增 `file-visuals.ts`，按扩展名/特殊文件名映射统一图标语义（code/document/data/image/video/audio/archive/config/lock/binary/unknown）；2. `FileTreePane` 重做行态层级（34 行高、图标胶囊、格式 badge、更清晰 selected/hover/focus），并将目录/文件图标改为语义化渲染；3. `FileSearchModal` 结果列表补齐同源图标与 badge；4. `FileEditorPane` 标签补齐格式图标并优化已修改标记。验证：`cd apps/desktop-web && npm run build`、`cd apps/desktop-web && npx eslint src/features/file-explorer/FileTreePane.tsx src/features/file-explorer/FileSearchModal.tsx src/features/file-explorer/FileEditorPane.tsx src/features/file-explorer/file-visuals.ts`。 | 文件树、搜索结果、编辑器标签视觉一致性；文件格式识别可读性；文件管理交互视觉品质 | AI |
| 2026-03-19 | T-175 | 按用户“效果仍差”的反馈继续收口可靠性：1. `StationXtermTerminal` 新增运行时输入行打标，基于 `.xterm-cursor` 为当前行注入 `vb-terminal-input-row` 类名；2. `StationCard.scss` 的输入高亮选择器改为 `vb-terminal-input-row + :has(.xterm-cursor)` 双通道，确保即便 `:has` 在个别 WebView 不稳定也能生效；3. 继续保持“当前输入行 span 背景透明”以清除 Codex 默认灰底叠层。验证：`cd apps/desktop-web && npm run build`。 | 终端输入高亮跨 WebView 生效可靠性、输入行视觉冲突清理、工作站终端样式可见性 | AI |
| 2026-03-19 | T-175 | 按用户截图继续修正“输入效果不明显 + 样式重叠”：1. 移除基于 `.xterm-screen::after` 的输入伪层，改为直接命中 `.xterm-rows > div:has(.xterm-cursor)` 的“当前输入行”高亮（边框/背景/发光）；2. 对当前输入行内联 span 强制 `background-color: transparent !important`，清理 Codex TUI 默认灰底与自定义高亮重叠；3. 维持 `xterm-selection` 高层覆盖与滚动条白边修复。验证：`cd apps/desktop-web && npm run build`。 | 终端输入行层级可见性、默认输入底色冲突清理、xterm 行级覆盖可靠性 | AI |
| 2026-03-19 | T-175 | 按用户反馈继续修正“样式未生效/层级不够/旧样式重叠”：1. 将输入提示层从 `.xterm-screen::after` 改为真实 DOM 覆盖层 `.station-terminal-input-lane`，由 `StationXtermTerminal` 动态注入 `top/height`，并提升为“文本上层、selection 下层”的固定 z-index 体系；2. 清理终端舱体旧叠加层（移除 `station-terminal-shell::before`）减少材质重叠；3. 强化 DOM renderer 下 `xterm-selection`/`::selection` 覆盖选择器，确保选中态在亮暗主题可见。验证：`cd apps/desktop-web && npm run build`。 | 工作站终端输入视觉层级、selection 可读性、终端材质叠层冲突清理 | AI |
| 2026-03-19 | T-175 | 按用户给定 xterm DOM 与 `ui-ux-pro-max` 继续深化终端样式：1. `StationXtermTerminal` 新增输入锚点同步（读取 `.xterm-helper-textarea` 的 `top/height`），下发 `--vb-terminal-input-lane-top/height` 与 `data-input-anchor`，用于“输入框位置感”可控覆盖；2. `StationCard.scss` 新增按锚点定位的输入行高亮层、IME/composition 偏移与材质强化、selection 强弱态覆盖（含选中文本色回退）并保留 `xterm-decoration-overview-ruler` 隐藏，避免白边回归；3. `legacy foundations` 补齐 selection/composition/input-lane 设计 token，亮/暗模式同步。验证：`cd apps/desktop-web && npm run build`。 | 工作站终端输入区定位感、鼠标选中文本可读性、IME 输入视觉、xterm 亮/暗主题一致性 | AI |
| 2026-03-19 | T-175 | 按用户最新反馈回滚“底部输入带”视觉层：1. 删除 `StationCard.scss` 中 `station-terminal-shell::after` 与对应焦点态动画，清除“底部多出类滚动条”的误加样式；2. `xterm-screen` padding 恢复，不再为输入带预留额外底部空间；3. 为进一步压掉滚动条左侧白边，新增 `overviewRulerBorder: transparent` 并将滚动条轨道设为透明、移除滑块/轨道阴影与 shadow 残留。验证：`cd apps/desktop-web && npm run build`。 | 终端底部视觉、xterm 滚动条边线、工作站终端样式回滚稳定性 | AI |
| 2026-03-19 | T-175 | 按用户反馈继续使用 `ui-ux-pro-max` 深化终端样式定制：1. 修复细 Y 滚动条左侧白边（移除 `slider` 边框并降低轨道不透明度）；2. 补齐 xterm `selectionForeground/selectionBackground/selectionInactiveBackground`，实现鼠标选中文本前景与背景在亮/暗模式下的对比一致性；3. 引入“输入带”视觉层与 IME `composition-view` 苹果风输入胶囊（不改 PTY 输入语义，仅增强输入位置感）；4. 终端 token 新增 selection/composition/input-lane 变量并同步亮暗主题。验证：`cd apps/desktop-web && npm run build`。 | 工作站终端滚动条细节、终端选中态可读性、输入体验视觉、亮暗主题终端 token | AI |
| 2026-03-19 | T-175 | 按用户要求使用 `ui-ux-pro-max` 研究并收口终端样式覆盖链路：1. 确认 xterm 滚动条由内部 `xterm-scrollable-element` 管理，纯 `::-webkit-scrollbar` 覆盖不可靠；2. `StationXtermTerminal` 新增 `overviewRuler.width` + `theme.scrollbarSlider*` 注入，滚动条宽度改由 design token `--vb-terminal-scrollbar-size` 驱动，实现“强制细化 Y 轴滚动条”；3. `StationCard.scss` 增加作用域覆盖，重塑终端舱体材质/高光/滚动条轨道与滑块；4. `legacy foundations` 终端 token 更新为苹果风专业配色与滚动条色阶。验证：`cd apps/desktop-web && npm run build`。 | 工作站终端视觉、xterm 内部滚动条控制、终端主题 token、样式覆盖可靠性 | AI |
| 2026-03-19 | T-175 | 继续按用户反馈收口通信通道分组体验：1. 顶部会话分组从胶囊气泡改为扁平 tab 导航；2. tab 文案不再展示 channel+长 id，改为基于绑定关系解析 `channel bot 名称 - agent 名称`；3. 前端新增 `bindings + stationNameMap` 组合映射逻辑，以 `ChannelRouteBinding.botName + targetAgentId -> station.name` 生成显示名。验证：`cd apps/desktop-web && npm run build`。 | 通信通道 tab 导航样式、绑定关系显示语义、agent 名称映射可读性 | AI |
| 2026-03-19 | T-175 | 按用户要求使用 `ui-ux-pro-max` 重构“通信通道”外部事件视图：1. `ShellRoot` 为外部事件补齐 `traceId/accountId/peerId/targetAgentId` 等上下文，并用“外部端点 + 内部 target agent”生成 `conversationKey`，在 routed/dispatch 事件到达时回填关联；2. `CommunicationChannelsPane` 改为按 `conversationKey` 分组并在顶部 Tab 切换，不再把多通道消息串在一个流里；3. 删除“最近外部事件”标题与单条时间/channel/status 元信息，仅保留消息气泡正文（失败附详情）；4. 深色模式下调暗面板和气泡材质，避免背景过亮。验证：`cd apps/desktop-web && npm run build`。 | 外部事件聚合逻辑、通信通道会话分组体验、tool-adapter 面板样式（亮/暗主题） | AI |
| 2026-03-19 | T-175 | 按用户要求使用 `ui-ux-pro-max` 将“通信通道”消息列表改为社交聊天记录界面：1. `CommunicationChannelsPane` 从横向行列表重构为“最新在底部”的单流聊天视图；2. 按约定实现“入站靠右、出站靠左”，右侧入站使用主色气泡，左侧出站使用中性色气泡；3. 元信息改为时间常显 + channel/status 图标常显，窄屏自动折叠标签；4. `ShellRoot` 修复 `appendExternalChannelEvent` 对 `channel/status` 的丢失，保证消息元信息可直出，不依赖文案反推。验证：`cd apps/desktop-web && npm run build`。 | 通信通道侧栏、外部消息元数据透传、tool-adapter 样式体系、聊天流可读性与响应式表现 | AI |
| 2026-03-19 | T-190 | 将 Agent CLI 安装提升为“系统终端可直接使用”的全局落地：1. Codex/Gemini 改为 `npm --prefix ~/.local` 安装/卸载，避免落到临时 fnm multishell 路径；2. 安装后自动确保 `~/.local/bin` 存在并写入 `~/.zshrc/.zprofile/.bashrc/.bash_profile/.profile` 的 PATH 导出块；3. 保留 shell 暴露不足的 warning，但不再把它作为安装失败。验证：`cargo check -p vb-tools`、`cargo check -p gtoffice-desktop-tauri`。 | Agent 全局安装稳定性、系统终端可用性、PATH 持久化 | AI |
| 2026-03-19 | T-190 | 收敛安装后复检误判：1. `vb-tools::agent_installer` 在校验 `codex/gemini --version` 时会自动补齐 Node runtime PATH，修复 GUI 进程环境缺失 `node` 导致“已安装 CLI 被误判未安装”；2. `install_agent` 对 “shell exposure incomplete” 改为进度警告，不再阻断安装成功返回。验证：`cargo check -p vb-tools`、`cargo check -p gtoffice-desktop-tauri`。 | Codex/Gemini 检测稳定性、Claude 安装回执语义、设置页安装状态可信度 | AI |
| 2026-03-19 | T-190 | 修复 Agent 供应商卡片中 Claude 卸载按钮不可点击：前端 `ProviderAgentCard` 不再因 `uninstallAvailable=false` 直接禁用卸载 CTA，而是允许点击后走统一 `uninstall_agent` 链路，由后端返回真实卸载结果或明确指导信息。验证：`cd apps/desktop-web && npm run build`。 | 设置页 AI Providers 卡片交互、Claude/Codex/Gemini 卸载可达性、错误提示链路 | AI |
| 2026-03-19 | T-190 | 修复 Claude Code“安装完成但命令行仍不可用”假成功问题，并把同类治理同步到 Codex/Gemini：1. `vb-tools::agent_installer` 的安装判定从“扫到可执行文件”升级为“可执行文件 + `--version` 真实校验”，同时补充 login shell 探测、`asdf/fnm current/.local/bin` 等常见目录与 shell 可见性告警；2. `install_agent` / `uninstall_agent` 改为安装后、卸载后都做后置复检，不再把脚本退出码直接当最终状态；3. Claude 新增受管卸载能力，当前可识别 native/npm/homebrew 来源并执行对应移除；4. 该诊断链路对 Codex/Gemini 同步生效。验证：`cargo check -p vb-tools`、`cargo check -p gtoffice-desktop-tauri`。 | Agent CLI 扫描/安装/卸载判定、Claude/Codex/Gemini 一致性、设置页安装结果可信度 | AI |
| 2026-03-19 | T-192 | 修复“Agent 已扫描到，但 GT Office 自带终端里 `codex` 仍 command not found”问题：定位到 CLI 扫描与 `terminal.create` 使用的 shell 环境脱节，应用内 PTY 不会自动加载用户登录 shell 中的 fnm/nvm/volta PATH。现已在 `augment_terminal_env_for_agent` 中把扫描到的可执行文件绝对路径写入 `GTO_CLAUDE_COMMAND/GTO_CODEX_COMMAND/GTO_GEMINI_COMMAND`，并将其父目录 prepend 到新终端会话 `PATH`。验证：`cargo check -p gtoffice-desktop-tauri`。 | 终端环境注入、应用内 PTY CLI 可见性、Agent 启动稳定性 | AI |
| 2026-03-19 | T-191 | 修复 Shell 首次启动误自动打开目录：`ShellRoot.tsx` 原先在没有记忆工作区时也把 `workspacePathInput` 初始化为硬编码 `/mnt/c/project/vbCode`，随后被“路径输入变化自动打开工作区”的 debounce 逻辑误当作用户输入执行 `workspace_open`；现已改为空字符串，仅在 `loadRememberedWorkspacePath()` 返回真实历史路径时恢复。验证：`cd apps/desktop-web && npm run build`。 | Shell 启动工作区绑定、首次启动体验、工作区输入默认值 | AI |
| 2026-03-19 | T-190 | 修复 Agent 供应商“扫描不稳 + 未安装不可安装 + 无卸载”问题：1. `vb-tools::agent_installer` 扩展为 PATH + 常见安装目录联合扫描，覆盖 `fnm multishell`、`fnm node-versions`、`nvm`、`volta`、`~/.local/bin`、`homebrew`、`/usr/local/bin` 等来源，并返回 `detectedBy/issues/installAvailable/uninstallAvailable/npmReady` 等状态；2. Tauri 新增 `uninstall_agent` command，当前支持 Codex/Gemini 走 `npm uninstall -g` 回收，Claude 明确返回“暂不支持自动卸载”的指导；3. 设置页 AI Providers 不再因缺 Node/npm 直接禁用安装按钮，而是允许触发并把后端诊断展示给用户；4. Provider 卡片补充检测来源展示，并在已安装场景提供卸载按钮。验证：`cd apps/desktop-web && npm run build`、`cargo check -p gtoffice-desktop-tauri`。 | Agent 供应商扫描、安装/卸载 command、设置页 AI Providers、前后端安装状态模型 | AI |
| 2026-03-19 | T-189 | 修复 macOS Tauri 打包失败：1. `run-tauri-with-env.cjs` 对 Tauri CLI 抛出的对象错误先归一化为字符串，避免 `logError` 二次抛 `StringExpected` 覆盖真实失败；2. 构建前自动清理 `target/<platform>/(debug|release)/bundle/macos/` 残留 `rw.*.dmg` 中间文件；3. macOS `npm run build` 改为先让 Tauri 只产出 `.app`，再由脚本基于 `hdiutil create -srcfolder <staging>` 自管生成最终 `.dmg`，避开内置 `bundle_dmg.sh` 把中间镜像写进源目录造成的递归打包与“设备上无剩余空间”；4. 非沙箱验证 `cd apps/desktop-tauri && npm run build` 已通过，成功产出 `target/macos/release/bundle/dmg/GT Office_0.1.0_aarch64.dmg`。 | Tauri 构建包装脚本、macOS DMG 产物生成链路、构建日志可诊断性 | AI |
| 2026-03-19 | T-188 | 响应 review 收口文件管理边界缺陷：1. `fs_copy` 新增目录后代目标拒绝，避免把目录复制到自身子目录时递归爆炸；2. `fs_create_dir` 对“同名已存在文件”改为返回冲突错误，不再误报创建成功；3. `FileTreePane` 键盘快捷键改为从聚焦树节点最近的 `data-kind` / `data-path` 解析上下文，恢复目录焦点下的粘贴与重命名语义；4. 为目录创建冲突与递归复制保护补充 Rust 回归测试。验证：`cd apps/desktop-web && npm run build`、`cd apps/desktop-web && npx eslint src/features/file-explorer/FileTreePane.tsx`、`cargo test -p gtoffice-desktop-tauri create_dir_rejects_existing_file_target -- --nocapture`、`cargo test -p gtoffice-desktop-tauri copy_rejects_directory_target_inside_source_tree -- --nocapture`。 | Tauri 文件系统命令、文件树键盘交互、前端构建稳定性、后端回归测试 | AI |
| 2026-03-19 | T-188 | 收口文件管理与 Shell 编译残留：1. 修复 `FileTreePane.tsx` 中目录加载错误 key、粘贴冲突判重缓存与剪贴板分支的 TS 报错；2. 将 `ShellRoot.tsx` 丢失的 workspace/layout import 改回正确模块来源，并补齐 workspace session 类型引用；3. 清理 `FileTreeModals.tsx` 的 React 19 effect lint 与相关未使用导入。验证：`cd apps/desktop-web && npm run build`、`npx eslint src/shell/layout/ShellRoot.tsx src/features/file-explorer/FileTreePane.tsx src/features/file-explorer/FileTreeModals.tsx`。 | Shell 主布局、文件树、文件管理弹窗、前端构建稳定性 | AI |
| 2026-03-19 | T-188 | 修复右键菜单体验反馈：1. 使用 `createPortal` 将菜单直接挂载到 `document.body` 解决由于相对容器导致的定位偏差问题；2. 在 `icons.tsx` 中扩充了缺失的 `LucideIcon`（`Scissors`, `Copy`, `FilePlus` 等），并为右键菜单项全面配备专属图标；3. 增强 `ui-ux-pro-max` 苹果风格视觉，将背景调浅后增加 `blur(48px)` 和 `saturate(200%)`。验证：`npm run build` 通过。 | 右键菜单定位稳定性、苹果风格视觉体验、前端图标包依赖 | AI |
| 2026-03-19 | T-188 | 深度增强文件树 IDE 体验：1. Tauri 增加 `fs_create_dir`, `fs_copy`, `fs_show_in_folder` (集成 `open` v5.3)；2. `FileTreePane` 支持完整剪贴板 (复制、剪切、粘贴)、重命名快捷与基于 `ui-ux-pro-max` 的现代化右键级联菜单，完成全链路前后端打通与修复编译。验证：`cargo check`、`npm run build`。 | 文件树页面交互、Rust 后端文件系统命令、前端快捷键捕获系统、资源管理器原生唤起。 | AI |
| 2026-03-19 | T-187 | 全量完成响应式样式单位重构：1. Git `operations/history/diff/graph` 收口为 `rem`/token/container query 体系，`GitGraphView` 改为容器感知列模板；2. Settings / AI Providers、Task Center / Global Overlay、Workspace Hub、File Explorer / Editor / CodeMirror、Tool Adapter、Shell 与 foundations/tokens 的残余 `px` / `px-to-rem(...)` 全部清零；3. 删除 `_responsive.scss` 中最后的 `px-to-rem` 兼容别名。验证：`rg "\b\d+(\.\d+)?px\b|px-to-rem\(" apps/desktop-web/src` 返回 0；`cd apps/desktop-web && npm run build` 通过。 | 前端业务样式系统、Git/Settings/TaskCenter/Workspace/FileExplorer/ToolAdapter、全局 tokens/foundations、响应式布局基线 | AI |
| 2026-03-19 | T-187 | 继续收口 Git 字体缩放回归：1. 新增 `features/git/git-font-scale.ts`，区分“设计尺寸 -> rem”和“虚拟列表测量 -> 实际像素”，修复此前 GitPane / GitGraphView 把设计尺寸按当前根字号反算成固定像素的问题；2. `GitPane.tsx`、`GitGraphView.tsx` 的虚拟列表行高、translateY、SVG 尺寸与内联宽高已改为跟随应用字体设置；3. `styles/features/git/_pane.scss` 与 `_diff-viewer.scss` 为变更文件列表、历史图和 diff 视图补齐统一字体/行高变量，并覆盖第三方 diff wrapper 的继承字号。验证：`cd apps/desktop-web && npm run build` 通过。 | Git 变更文件列表、Git 历史图、Diff Viewer、应用字体大小跟随 | AI |
| 2026-03-19 | T-187 | 第二轮尾部清理继续推进：1. 按写集收口 `ShellRoot.scss`、`ActivityRail.scss`、`TopControlBar.scss`、`StatusBar.scss`、`LeftBusinessPane.scss`、`SettingsModals.scss`、`TaskDispatchPreferences.scss`、`AiProvidersSection.scss` 以及 `SettingsModal.tsx`、`ClaudeConfigModal.tsx`、`LightAgentConfigModal.tsx` 中残留的 `px` / `px-to-rem(...)`；2. TSX 内联尺寸统一改为 `rem` 字符串输出，避免继续拼接 `px`；3. 定向审计本轮写集内返回 0，`cd apps/desktop-web && npm run build` 通过。 | Shell 布局、Settings 详情页、AI provider 配置弹窗、桌面端响应式样式收口、构建验证 | AI |
| 2026-03-18 | T-187 | 进入 Phase 4 Feature 迁移：1. Git pane 响应式单位替换继续收口，`git/_pane.scss` 已完成 `px-to-rem` 过渡清理并统一改为 `rem` 语义；2. `GitPane.tsx` 与 `GitGraphView.tsx` 的内联像素尺寸改为基于根字号的 `rem` 输出，减少窗口缩放下的跳变；3. Git 专属布局开始收口 container query，补齐窄宽度下的折叠与换行规则；4. `docs/09` 已补充 Git feature 分块迁移、专项验收矩阵与已落地/待落地边界。验证：`cd apps/desktop-web && npm run build` 成功；`rg "\b\d+(\.\d+)?px\b" apps/desktop-web/src/features/git apps/desktop-web/src/styles/features/git/_pane.scss` 返回 0。 | Git feature 样式、Git 历史视图、虚拟列表内联尺寸、container query 收口、响应式实施文档 | AI |
| 2026-03-19 | T-187 | 第二轮尾部清理：1. 收口 Tool Adapter / File Content / utilities / shell layout / platform / legacy foundations 残余 `px`；2. `CommunicationChannelsPane.scss`、`ChannelStudio.scss`、`FileContentPane.scss`、`_helpers.scss`、`_layout.scss`、`_platform.scss`、`_legacy-foundations.scss` 已清零 `px` 与 `px-to-rem(...)`；3. 后续阻塞项 `TaskDispatchPreferences.scss` 已在同轮尾部清理中解除，全量构建最终通过。 | Tool Adapter、File Explorer、Shell utilities、全局 token / foundation、构建验证 | AI |
| 2026-03-18 | T-187 | 完成 Phase 3 Shell 重构：1. 将 ShellRoot, TopControlBar, StatusBar, ActivityRail, LeftBusinessPane 的固定 px 迁移为 rem/semantic tokens；2. 重构 SettingsModal 布局，引入 clamp() 流式尺寸与 container query；3. 升级 _responsive.scss 接入 math.div 消除 Sass 弃用警告。验证：`cd apps/desktop-web && npm run build` 成功且无警告。 | Shell 框架样式、顶部栏/状态栏响应式、设置弹窗布局、Sass 基础函数库 | AI |
| 2026-03-18 | T-175 | 修正 Git 管理界面“变更文件”模块高度方案：确认单靠 `git-section--changes` 的百分比 flex/height 在当前高度链下不稳定后，改为在 `GitPane` 内通过 `ResizeObserver` 读取 `git-pane__content` 实际高度，并把“变更文件” section 直接写成父容器 50% 的像素值；SCSS 侧同步收敛为最小约束，避免与内联高度冲突。验证：`cd apps/desktop-web && npm run build`。 | Git 管理操作页、`GitPane` 高度计算、SCSS 样式治理 | AI |
| 2026-03-18 | T-186 | 修复 macOS 下输入框与代码编辑器无法输入部分字符的问题：将 `ShellRoot` 中桌面端 WebView 浏览器快捷键拦截从“裸键 `r/+/=/-/0` 全部吞掉”收敛为仅拦截真实 `Cmd/Ctrl + r/+/=/-/0` 组合，避免全局捕获阶段误伤普通文本输入。验证：`cd apps/desktop-web && npm run build`。 | Shell 全局键盘监听、macOS 文本输入、文件编辑器与表单输入稳定性 | AI |
| 2026-03-18 | T-171 | 按产品要求把 MCP 安装从自动改为手动，并把卡片入口升级为“增强服务”：1. 移除 bridge 启动自动安装与 `install_agent` 顺带安装 MCP；2. 新增 `agent_mcp_install_status` / `install_agent_mcp`，并让 `gto-agent-mcp-install.mjs` 支持 `--target` 定向安装；3. agent 卡片不再直接暴露 MCP，而是改为苹果风格“增强服务”入口，打开独立弹窗后按 `MCP / Skills` tab 分类；4. 当前 MCP 在 `MCP` tab 中以 `GT Office Agent Bridge` 服务项展示，并区分“未安装 / 已预配置待 CLI / 已安装”；5. snapshot card 增加 `mcpInstalled` 字段。验证：`node --check tools/gto-agent-mcp/src/installer.mjs`、`node --check tools/gto-agent-mcp/bin/gto-agent-mcp-install.mjs`、`npm run typecheck`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。 | MCP 安装入口、桌面端 agent 管理、增强服务弹窗、安装器 CLI 参数 | AI |
| 2026-03-17 | T-171 | 继续补齐 sender 读回回复正文能力：在 `vb-task` 内存态增加最近 `channel/message` 历史，Tauri 新增 `channel_list_messages`，MCP 新增 `gto_list_messages`，默认按当前 agent inbox 拉取最近 `status/handover` 内容；同时明确“纯终端打印但未走 MCP 的回复”不进入 inbox。验证：`cargo test -p vb-task list_messages_returns_recent_filtered_inbox -- --nocapture`、`cargo check --manifest-path tools/gto-agent-mcp-sidecar/Cargo.toml`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`、`node --check tools/gto-agent-mcp/src/mcp-server.mjs`。 | 消息历史、Tauri task_center command、MCP inbox 查询能力、sender 读回回复内容 | AI |
| 2026-03-17 | T-171 | 收敛 `gto-agent-mcp` 正常对话噪音：压缩 `tools/list` 描述与 tool 正常返回，`gto_get_agent_directory` 仅返回最小 agent 目录与 `self`，`gto_health` 在 bridge 正常时不再回显冗长诊断；同时让 `gto_dispatch_task/gto_report_status/gto_handover` 支持 `workspace_id` 自动解析，并把 sender 识别扩成“显式参数 -> `GTO_AGENT_ID` -> 当前 cwd/目录自识别”，避免执行 agent 因缺失 env 被拒绝。另补充安装器受管 communication policy：写入 `GTO_AGENT_COMMUNICATION_POLICY_FILE` 与各 CLI 常见指令文件托管块，减少再靠 manager 临时提示“请用 MCP 回复”。验证：`node --check tools/gto-agent-mcp/src/mcp-server.mjs`、`node --check tools/gto-agent-mcp/src/installer.mjs`、`node --input-type=module -e "... installAll(...)"`、`cargo check --manifest-path tools/gto-agent-mcp-sidecar/Cargo.toml`。 | MCP tool description/response、sidecar parity、CLI 安装器、Agent 默认协作规则 | AI |
| 2026-03-17 | T-175 | 按用户要求使用 `ui-ux-pro-max` 简化“通信通道”侧栏：去除运行状态、Webhook 地址、路由/白名单/幂等等偏运维信息与派发历史折叠区，仅保留面向用户的最近消息流，并将每条消息收敛为“内容 + 时间 + channel + 状态”；同时为 `external/channel_outbound_result` 补发 `channel` 字段，避免前端从描述字符串反推通道。验证：`cd apps/desktop-web && npm run build`。 | communication channels pane、外部消息事件载荷、tool-adapter 侧栏样式层级 | AI |
| 2026-03-17 | T-175 | 将任务中心中的“选择 agent + 发送任务”抽成可全局唤起的 `GlobalTaskDispatchOverlay`：新增默认快捷键 `Mod+Shift+K`、设置中心“快捷键与浮层”页、可录制改键与透明度持久化，并让浮层复用现有任务草稿/多 agent 选择/`@` 文件引用/派发链路。验证：`cd apps/desktop-web && npm run build`；`npm run typecheck` 未提供脚本。 | task-center 全局浮层、keybindings 模型、settings 中心、ShellRoot 全局快捷键接线 | AI |
| 2026-03-17 | T-175 | 使用 `ui-ux-pro-max` 对 `workspace-hub/StationCard` 与 station xterm 终端舱体做视觉重构：把原“玻璃卡片 + 普通终端容器”改为 `OLED cockpit` 语言，新增状态 eyebrow、tool/session pill、终端 stage bar、frame glow、局部 terminal theme 变量、可见 focus ring 与 reduced-motion 收口；同时移除任务确认文案中的 emoji，避免命令行界面出现廉价装饰噪声。验证：`cd apps/desktop-web && npm run build`。 | StationCard、StationXtermTerminal、workspace-hub 终端视觉层级、xterm 局部主题与可访问性反馈 | AI |
| 2026-03-17 | T-175 | 按用户要求用 `ui-ux-pro-max` 收敛 Git 管理界面层级：将 `GitPane`/`GitGraphView`/`DiffViewer` 对应 SCSS 从“多层卡片叠加”改为“单主面板 + 分隔式区块 + 工具条”语言，压平 section/diff/detail/action 的重复圆角、背景和 hover 缩放，统一列表/表格/详情区域为桌面工作台式信息密度。验证：`cd apps/desktop-web && npm run build`。 | Git 管理操作页、历史页、diff viewer、graph detail/action 视觉层级与交互反馈 | AI |
| 2026-03-17 | T-175 | 继续针对 macOS 原生标题栏模式微调顶部中线：`TopControlBar` 的 `native-window-top` 不再直接把 `env(titlebar-area-height)` 全量作为顶部 padding，而是改为“titlebar 高度减去按钮高度”的收敛 inset，并把 macOS 左侧安全区与外层 `agent-shell` 顶部补白再各压一档，减少原生窗口模式下内容下沉感。验证：`cd apps/desktop-web && npm run build`。 | macOS 原生 titlebar 顶部 inset、TopControlBar 中线、ShellRoot 外层补白 | AI |
| 2026-03-17 | T-175 | 按用户反馈继续修正顶部栏垂直对齐：将 `TopControlBar` 改为以左侧三个操作按钮尺寸为统一基准，bar 最小高度、leading 容器、workspace 状态条、拖拽区、窗口控件统一共享同一套按钮高度变量，避免顶部区域在视觉上“按钮不在正中、内容靠 padding 撑开”。验证：`cd apps/desktop-web && npm run build`。 | TopControlBar 垂直节奏、按钮中心线、顶部区域中线对齐 | AI |
| 2026-03-17 | T-175 | 按用户选择的方案 1 继续压缩顶部栏：将 workspace badge 从带背景/边框的胶囊块改为更轻的纯文字状态条，仅保留一枚低对比圆点作为定位提示，移除块级背景和描边，进一步减少顶部视觉重量。验证：`cd apps/desktop-web && npm run build`。 | TopControlBar workspace 状态区、顶部视觉重量、badge 去块化 | AI |
| 2026-03-17 | T-175 | 继续压缩顶部状态栏高度占用：在首轮 `TopControlBar` 紧凑化基础上，再次下调顶栏最小高度、内边距、badge/按钮尺寸与拖拽占位宽度，并同步把 `agent-shell` 与 `shell-main-layout` 的纵向 gap 从 `6px` 收到 `4px`、macOS 顶部补白从 `10px/6px` 收到 `6px`，让“顶部栏本体 + 顶栏下方空档”一起变薄。验证：`cd apps/desktop-web && npm run build`。 | Shell 顶部整体高度、TopControlBar 第二轮紧凑化、ShellRoot 外层纵向留白 | AI |
| 2026-03-17 | T-175 | 优化 Shell 顶部状态栏的纵向密度：基于 `ui-ux-pro-max` 的 compact/professional 检索结果，收紧 `TopControlBar` 的最小高度、内边距、按钮尺寸、badge 高度和 Windows/Linux 窗口控件高度，同时保留 macOS 左侧原生按钮安全区与至少 `6-8px` 的点击间距，避免压缩后变得拥挤难点。验证：`cd apps/desktop-web && npm run build`。 | TopControlBar 顶部状态栏高度、按钮密度、workspace badge、跨平台顶栏视觉节奏 | AI |
| 2026-03-17 | T-186 | 继续收口 macOS 原生窗口按钮不可用问题：1. 复盘确认上一轮虽然把 `tauri.conf.json` 改成 `decorations=true + hiddenTitle + Overlay`，但 `ShellRoot` 启动后仍无差别执行 `window.setDecorations(false)`，在运行时再次把 macOS 原生 traffic lights 关闭，导致“配置看起来对、实际按钮仍不出现”；2. 现改为 `tauri.conf.json` 显式声明 `acceptFirstMouse=true` 与 `trafficLightPosition`，并保留 `create=false + WebviewWindowBuilder::from_config`；3. Rust 侧删除建窗后再手工 `set_title_bar_style/set_decorations/set_background_color` 的 hack，改为完全依赖配置驱动的原生 macOS title bar；4. 前端仅在 Windows/Linux 保持 `decorations=false` 自绘标题栏，macOS 明确恢复 `decorations=true`，同时去掉 macOS 的整块 header drag region、Windows 式双击最大化语义和自绘 traffic lights 样式 hack，只保留左侧原生按钮安全区。验证：`cd apps/desktop-web && npm run build`、`cargo check -p gtoffice-desktop-tauri`。 | macOS 原生 traffic lights、窗口顶栏平台分流、Tauri 建窗路径、Shell 顶栏样式 | AI |
| 2026-03-16 | T-186 | 修复 macOS 触控板点击按钮需双击问题：1. 参考 Tauri 官方对 macOS custom titlebar 的 caveat，停止依赖 `decorations: false` 的自绘顶栏交互；2. 主窗口改为 `tauri.conf.json:create=false + setup 中 WebviewWindowBuilder::from_config` 手动建窗，并仅在 macOS 创建时启用 `decorations + hiddenTitle + Overlay title bar + acceptFirstMouse + trafficLightPosition`，确保原生 traffic lights 可见可用；3. 前端不再为 macOS 渲染自定义窗口控件或挂载拖拽 region，并为标题栏左侧留出原生按钮安全区，Windows/Linux 保持原逻辑；4. 新增 `data-vb-platform='macos'` 平台样式隔离，关闭 macOS 下按钮 hover/active 的位移缩放，只保留颜色与阴影反馈，避免首击先进入 hover 再因命中区域移动丢失 click。验证：`cd apps/desktop-web && npm run build`、`cargo check -p gtoffice-desktop-tauri`。 | Shell 顶栏拖拽、macOS 按钮点击、跨平台窗口交互隔离 | AI |
| 2026-03-15 | T-172 | 落地外部通道回复“双源观察”架构：新增 `vb-session-log` crate，按 provider/cwd/prompt 绑定并轮询 Claude/Codex session log；`app_state` 改为 `session-log > rendered-screen > vt` 的 finalize source 选择，同时保留 rendered-screen 负责 preview、ready/finalize 边界与 interaction prompt。Telegram 交互 prompt 升级为“语义按钮 + 方向键”双模式，callback 新增 `gto-key:*`，并直接回写同一 live terminal session；外部出站统一改走 `channel_sinks`。同步补充方向键菜单提取与 Telegram key callback 回归测试，并更新 T-172/08 设计文档。验证：`cargo check -p gtoffice-desktop-tauri`、`cargo test -p vb-session-log`、`cargo test -p gtoffice-desktop-tauri rendered_screen_extracts_terminal_navigation_menu_prompt -- --nocapture`、`cargo test -p gtoffice-desktop-tauri menu_response_input_does_not_replace_active_reply_session -- --nocapture`、`cargo test -p gtoffice-desktop-tauri permission_response_input_does_not_replace_active_reply_session -- --nocapture`、`cargo test -p gtoffice-desktop-tauri parse_telegram_key_callback_query -- --nocapture`。 | 外部通道 reply relay、Telegram 交互回流、Claude/Codex session log 绑定、channel sink 统一出站、T-172 文档契约 | AI |
| 2026-03-15 | T-171 | 补齐 Claude saved provider 编辑链路：1. 明确 Claude saved provider 仍采用“自动生成 `savedProviderId` + workspace 内 fingerprint 去重”模型，相同配置不会重复落库；2. `preview/apply` 现在支持携带 `savedProviderId`，从已保存列表编辑时会优先复用原记录与托管密钥；3. Claude 已保存供应商列表新增“编辑配置”按钮，直接复用详情参数配置页进入预览/应用；4. 补充后端回归测试覆盖“编辑非当前激活 saved provider 时复用原 ID 和原密钥，不新增重复记录”。验证：`cargo test -p vb-ai-config`、`cd apps/desktop-web && npx tsc -b`。 | Claude saved provider 持久化语义、编辑链路、前端配置弹窗、AI config 契约/文档 | AI |
| 2026-03-15 | T-171 | 修复 Claude 预设详情页表单问题：1. 预设网关模式下的“网关名称”改为可编辑，并打通到后端 preview/apply 保存链路；2. 预设回填时优先展示用户保存过的自定义名称；3. 调整详情页滚动容器与表单字段样式，修复靠左输入框左侧边框被裁剪的问题。验证：`cargo test -p vb-ai-config`、`cd apps/desktop-web && npx tsc -b`。 | Claude 配置向导、预设 providerName 保存语义、详情页表单样式 | AI |
| 2026-03-15 | T-171 | 修复 Claude saved provider 列表切换闪烁：1. Claude saved provider 切换不再额外调用 `read_snapshot` 全量重载，而是直接使用 `ai_config_switch_saved_claude_provider` 返回的 `effective snapshot` 更新当前页状态；2. saved provider 卡片拆为 memo 化子组件，尽量把重绘收敛到激活状态变化的条目；3. saved 列表视图不再在切换成功后插入/清空瞬时 success 文案，减少布局抖动。验证：`cd apps/desktop-web && npx tsc -b`。 | Claude saved provider 列表切换体验、设置页状态更新、前端渲染稳定性 | AI |
| 2026-03-15 | T-171 | 修正 Claude saved provider 列表排序语义：1. 已保存供应商列表改为按保存顺序稳定返回，不再按 `is_active/last_applied` 重排；2. 从列表切换“当前配置”时只更新激活标记与应用结果，不改变卡片在列表中的位置；3. 补充回归测试覆盖切换前后顺序保持不变。验证：`cargo test -p vb-ai-config`。 | Claude saved provider 存储查询、切换行为、列表稳定性 | AI |
| 2026-03-15 | T-171 | 收口 Claude 卡片子入口与官方模式基线：1. Claude 卡片下“配置向导 / 已保存供应商”按钮改为水平两列；2. 参考 `cc-switch` 为 `official` 模式补齐 Anthropic 官方基线，`preview/read_snapshot/savedProviders` 现在都会回填官方 `provider/baseUrl/model/authScheme`，不再显示上一个第三方供应商残留值；3. Claude 官方详情页也显式展示官方 endpoint/model/auth。验证：`cargo test -p vb-ai-config`、`cd apps/desktop-web && npx tsc -b`。 | Claude 配置中心、官方模式预览语义、Claude 卡片入口样式、saved provider/快照一致性 | AI |
| 2026-03-15 | T-171 | 调整 Claude 配置中心入口结构：1. 移除 Claude 弹窗内“配置向导 / 已保存供应商”tab 关系；2. 改为在 Agent 供应商列表的 Claude 卡片内提供两个独立子入口，分别直接打开配置向导和已保存供应商列表；3. 同步清理前端样式与 i18n 文案，保持 saved provider 切换后端契约不变。验证：`cd apps/desktop-web && npx tsc -b`。 | Agent 供应商列表、Claude 配置中心入口交互、前端文案与样式 | AI |
| 2026-03-14 | T-171 | Claude 配置中心新增 saved providers 闭环：1. 官方模式应用时不再只是清理托管 env，而是保存一条可切换的官方档案并将其设为当前配置；2. 每次成功应用 Claude 配置都会 upsert 到数据库 `ai_config_saved_claude_providers`；3. 新增 `ai_config.switch_saved_claude_provider` 命令，支持从历史列表一键切换当前生效配置；4. 前端 Claude 弹窗新增“已保存供应商”列表页，可查看历史配置并直接切换。验证：`cargo test -p vb-ai-config`、`cargo check -p gtoffice-desktop-tauri`、`cd apps/desktop-web && npx tsc -b`。 | Claude 配置中心、Claude saved provider 存储与切换、官方模式切换语义、桌面端设置页交互 | AI |
| 2026-03-14 | T-171 | Claude 配置应用链路升级为系统级 live settings 托管：1. `ai_config.apply_patch` 现在会在保留 `mcpServers`/`permissions`/非受管 env 的前提下同步 `~/.claude/settings.json`，与 GT Office 内新建 Claude 会话保持同一套设置；2. 切回官方模式时会清除 GT Office 托管的 Claude env 覆盖并回退原生 CLI 登录/配置；3. 收口 Windows/Linux/macOS 的 home 路径解析与 live 文件替换兜底，避免 Windows 下已存在文件时覆盖失败；4. 补齐 Claude live sync 回归测试与前端 warning/i18n 文案。验证：`cargo test -p vb-ai-config`、`cd apps/desktop-web && npx tsc -b`。 | Claude 配置中心、系统级 `~/.claude/settings.json` 托管、GT Office Claude 新会话生效链路、跨平台 home/文件写入兼容性 | AI |
| 2026-03-14 | T-171 | 扩充 Claude 供应商预设目录并收口前端交互：1. `vb-ai-config` 预设按模型能力/知名度重排，预设模式不再混入官方项，新增 `DeepSeek / Zhipu GLM / Zhipu GLM en / Qwen Coder / Kimi k2 / Kimi For Coding / MiniMax / MiniMax en / DouBaoSeed / Xiaomi MiMo / ModelScope / OpenRouter / Nvidia / KAT-Coder / Longcat / BaiLing`；2. `ClaudeConfigModal` 只展示可选预设，默认/回填不再落回官方，并允许在预设模式下直接微调 Base URL 以支持 `KAT-Coder` 这类模板端点；3. 补齐中英文引导、provider 说明与 warning 文案；4. 收口跨平台 home 路径解析，为后续系统级 Claude live settings 接管奠定路径兼容基础。`cargo test -p vb-ai-config`、`cd apps/desktop-web && npx tsc -b` 通过。 | Claude 配置中心、AI provider 预设目录、供应商引导文案、跨平台本机配置探测 | AI |
| 2026-03-14 | T-175 | 收口前端类型与目录边界：1. 为 `ui-locale` 增加 `isTranslationKey/translateMaybeKey`，清除 AI provider 弹窗、卡片与设置入口中的 `as any`/`err: any`/未声明 `window` 扩展；2. 新增 `provider-utils.ts` 与 `window-extensions.d.ts`，把 Light Agent 配置流改为显式 `LightAgentDraftInput` 与 `codex/gemini` 窄化类型；3. 将 `CommunicationChannelsPane` 从独立 `features/channels` 迁回 `features/tool-adapter`，删除空的 `features/channels` 与 `shell/layout/channel-management` 遗留目录。`cd apps/desktop-web && npx tsc -b` 通过。 | AI provider 配置链路、前端 i18n 类型安全、Settings/Channel Studio 入口、tool-adapter feature 边界、前端目录整洁度 | AI |
| 2026-03-14 | T-171 | 修复 Agent 配置向导关键交互断点：1. Claude 配置流改为按模式动态裁剪步骤，`official/custom` 在供应商模式页选择后自动进入下一可操作步骤，已安装场景默认跳过纯检查页；2. Claude 预览按钮改为按真实后端约束校验必填项与可复用 secret，避免“点了才报错”；3. 预设名称在详情/预览页统一本地化显示，不再暴露内部 i18n key；4. `AiConfigOverlay` 补齐 footer 承载能力，修复 Codex/Gemini 底部导航缺失；5. Light Agent 弹窗默认落在凭据页，并补充官方文档与本地配置路径；6. 恢复 Gemini 卡片配置入口，打通轻量 provider 全链路。`npx tsc -b` 通过。 | Claude 配置向导、LightAgent 配置链路、Gemini 配置入口、AI 配置交互体验 | AI |
| 2026-03-14 | T-171 | 落地 Agent 供应商进阶配置中心与全链路安全闭环：1. 前端实现模块化 `ai-providers/` feature，提供 Claude 深配（5步向导、预设切换、API Key 托管、预览后应用）与 Codex/Gemini 轻配（API Key 注入）；2. 采用交互式弹窗（Modal）承载配置流，设置主页保持简洁的卡片目录视图，通过“配置”按钮触发；3. 移除审计日志 UI，保持界面聚焦；4. 引入 Agent 供应商官方 Logo，并实现全链路“永不过时”的中英双语介绍与模型说明（后端下发 Key，前端动态翻译）；5. 后端新增 `vb-ai-config` 领域服务与 `vb-storage` 审计存储，`terminal.create` 已接入全量 Agent 运行时环境变量注入；6. 优化 `install_agent` 链路，安装 CLI 后自动触发 MCP bridge 脚本配置。全链路通过 `tsc` 类型检查与 `cargo` 静态检查。 | Agent 配置中心、Claude/Codex/Gemini 配置、交互式弹窗、环境变量注入、MCP 安装、Logo 映射、永不过时 i18n | AI |
| 2026-03-14 | T-171 | 任务中心 UI/UX 体验深度优化：1. 实现文本输入框高度自适应，随输入自动伸缩并保持极致输入体验；2. 修复发送逻辑，确保任务发送成功后自动清空输入框并重置高度；3. 优化界面简洁度，移除目标 Agent 下拉框中的 ID 显示，仅展示名称；4. 重塑按钮布局，将发送按钮从文本域浮层移至独立 Footer 区，避免长文本重叠；5. 精简通知机制，成功发送后不再展示冗余的计数摘要，仅保留错误提示。同步已在 ShellRoot 建立真实的数据库 Agent 列表同步，确保派发目标实时准确。 | 任务中心输入体验、Agent 选择列表、任务派发可靠性、SCSS 样式治理 | AI |
| 2026-03-14 | T-184 | 继续分析 Windows 安装包启动时命令行窗口闪现问题，确认除 Telegram/Feishu `curl` 轮询外，`ShellRoot` 在窗口绑定/打开 workspace 时会主动触发 `git_status`，而 `vb-git` 的 system git fallback 之前直接从 GUI 进程 `Command::new("git")`，在 Windows 下同样会为控制台子进程分配新窗口。现已为 `crates/vb-git` 增加统一的 Windows `CREATE_NO_WINDOW` 配置，覆盖 `status/log/branch/pull/push/stash` 等全部 system git 命令路径；非 Windows 仍保持 no-op。 | Windows 生产包启动体验、workspace 绑定后的 Git 状态刷新、system git fallback 全链路 | AI |
| 2026-03-13 | T-185 | 针对“应用初始化、打开 workspace、进入文件页时明显卡顿”做首轮整链路治理：后端 `commands/file_explorer` 将 `list/read/write/delete/move` 全部迁入 `tokio::task::spawn_blocking`，避免大目录枚举与大文件读写占住命令线程；`filesystem_watcher` 改为按 workspace 后端 64ms 合批并按 kind 去重后再发 `filesystem/changed`，切断大批量文件变动时的事件风暴；前端 `FileTreePane` 为目录加载增加 singleflight、防旧 workspace 回写与批量并发 reload，避免 watcher 批量刷新时同目录重复请求和串行排队；`ShellRoot` 将 workspace 会话恢复改为“只立即 hydrate 活动 tab，非活动 tab 仅恢复标签，点击时再读内容”，并把外部通道状态刷新延后到真正进入 `channels` / `Channel Studio` 时才触发，减少打开 workspace 时的无关初始化负载。 | Workspace 打开链路、文件树刷新、文件读取体验、Tauri 文件系统命令、watcher 事件频率、ShellRoot 启动初始化 | AI |
| 2026-03-13 | T-184 | 针对 Git 面板“暂存/提交后页面明显变慢”做整链路性能治理：后端 `commands/git` 改为把 `status/diff/log/branch/stash/detail` 及写操作统一放入 `tokio::task::spawn_blocking`，避免 Git 子进程与 git2 扫描阻塞 Tauri 命令线程；删除 `git_status` 命令主动发 `git/updated` 的实现，修复 `git.status -> git/updated -> 前端再调 git.status` 的刷新回环；保留 `git/updated`仅由真正变更仓库状态的命令发出。前端 `useGitWorkspaceController` 改为按操作类型精确刷新：暂存/撤销/丢弃只刷新 summary，commit/pull/checkout 才刷新 history/meta，fetch/push/stash 仅刷新必要子集；同时移除未使用的 `diff2html` 渲染链路、给 hover diff preload 加 debounce，并在写操作后失效当前 workspace 的 diff cache。另将高频 status 路径的 rename detection 默认关闭，避免大仓库实时状态扫描额外放大成本。 | Git 面板交互性能、Tauri Git command、前端 Git 控制器刷新策略、diff 预加载行为 | AI |
| 2026-03-13 | T-172 | 分析并修复 Windows 安装包运行时反复弹出命令行窗口：根因是 Tauri GUI 进程在无控制台父进程时后台调用 `curl` / CLI 会为控制台子进程分配新窗口，Telegram polling worker 每 1.5s 触发一次导致“关闭后又重开”。现已新增 `process_utils` 统一为 Windows 后台子进程注入 `CREATE_NO_WINDOW`，并接入 Telegram/Feishu `curl` 调用、结构化 reply CLI、Agent 安装命令与 `vb-tools` 的 Node.js 探测；非 Windows 路径保持 no-op，避免引入跨平台副作用。 | Windows 生产包运行稳定性、Telegram polling、Feishu/Telegram connector API、结构化 relay、Agent 安装链路 | AI |
| 2026-03-12 | T-171 | 收口 Agent 供应商安装体验：后端新增 `agent_install_status` 命令与本机可执行文件检测，页面进入即并行检查 Claude/Codex/Gemini 是否已安装；已安装项隐藏安装按钮并展示“已安装”状态，未安装项改为高对比 CTA，并对缺失 Node.js 的 npm CLI 给出前置依赖提示。 | Agent 供应商设置页、CLI 安装链路、Tauri command、`vb-tools` 安装检测 | AI |
| 2026-03-12 | T-172 | 分析并修复“Telegram 明明绑定 manager、消息却落到 build”问题：确认 Windows 持久化 `channel/state.json` 中残留了多个旧 workspace 的 `telegram/default/direct/* -> role:build` 绑定，而 `resolve_external_route()` 之前只按 `channel/account/peer` 选第一条命中；当命中旧 workspace 且该 workspace 当前不可解析时，`process_external_inbound_message()` 会把 workspace 回退到当前活动 workspace，却继续沿用旧 binding 的 `targetAgentId=role:build`，最终把 Telegram 误投到当前 workspace 的 build。现已为 `vb-task` 增加按指定 workspace 重解 route 的能力，并在 workspace fallback 后强制按已解析 workspace 重新匹配 route；若本地 workspace 不存在等价 binding，则直接报 `CHANNEL_ROUTE_WORKSPACE_MISMATCH`，不再静默误投。 | Telegram/Feishu 外部入站路由、跨 workspace 旧 binding 污染、route fallback 安全性 | AI |
| 2026-03-12 | T-172 | 修复设置中打开“外部通道”黑屏：`SettingsModal` 的“进入工作台”按钮误用了不存在的 `AppIcon name=\"external\"`，切到通道页时会触发 React 运行时异常。现已为 `shell/ui/icons.tsx` 补齐 `external -> ExternalLink` 映射，并让 `AppIcon` 对未知图标名回退到 `Info`，避免单个图标配置再把整页设置打挂。 | 设置弹窗通道页、Channel Studio 入口、前端图标兜底稳定性 | AI |
| 2026-03-12 | T-172 | 落地 Feishu connector Phase 1：后端新增 `connectors/feishu/`（账号存储、health probe、webhook 解析、WebSocket supervisor），`channel_connector_*` 命令补齐 Feishu 分支并扩展 DTO；前端 `tool-adapter` 新增 `feishu/` 向导子模块，`ChannelWizard` 改为 channel dispatcher，补充“GT Office <-> 飞书开放平台”分步引导、连接测试、Webhook callback 展示与最终 route/policy 应用。 | Feishu 外部通道配置闭环、tool-adapter 前端向导、connector 存储与 runtime、桌面端 Feishu 长连接依赖 | AI |
| 2026-03-12 | T-172 | 基于 OpenClaw 飞书插件实现补齐专题设计稿 `docs/feishu.md`：明确 Feishu connector 模块化拆分方案，要求后端新增与 `telegram/` 同级的 `connectors/feishu/`，前端在 `tool-adapter` 下拆出 Feishu 向导子模块，并把“GT Office 内配置步骤 + 飞书开放平台配置步骤 + 推荐 WebSocket / 高级 Webhook 模式”整理为可执行方案。 | Feishu 外部通道配置能力、后端 connector 目录规划、前端配置向导、平台接入文档 | AI |
| 2026-03-12 | T-175 | 修复自绘顶栏窗口控件与 Telegram 入站提示浮层：`TopControlBar` 按平台重排窗口按钮，Windows/Linux 将窗口控件固定到右侧并恢复更接近原生的 hover/close 态，macOS 改为 `close -> minimize -> maximize` 左侧 traffic lights；同时把 `telegram-debug-toast` 从普通文档流节点改为挂在 top slot 下方的绝对浮层，避免入站提示顶住标题栏导致 shell 变形。 | Shell 顶栏跨平台窗口控件、Telegram 入站提示层、标题栏布局稳定性 | AI |
| 2026-03-11 | T-175 | 继续修复工作站终端“切换布局后原有命令行内容消失”问题：在补上 sink 实例级解绑保护后，进一步引入 `@xterm/addon-serialize`，为 terminal sink 解绑 metadata 增加 `restoreState + cols + rows`；布局 remount 时先按序列化时的原始尺寸恢复官方 VT state，再 fit 到新布局尺寸，避免尺寸不一致导致的反序列化重叠与 scrollback 缺失。 | 工作站终端布局切换恢复链路、xterm state 保真度、station sink 生命周期、依赖白名单 | AI |
| 2026-03-11 | T-172 | 继续追查“agent 数据看起来消失”问题：确认并非 SQLite 自动清空，而是 `InMemoryWorkspaceService` 在每次 `workspace.open()` 时为同一路径重新生成随机 `workspace_id`，导致 agent/role/channel binding 等按旧 `workspace_id` 持久化的数据在重启后不可见；现已改为基于 canonical root 生成稳定 `workspace_id`，并补充跨 service 实例稳定性测试。 | workspace 身份稳定性、agent/role/binding 持久化可见性、重启后数据连续性 | AI |
| 2026-03-11 | T-172 | 分析并修复“channel 绑定 agent 修改后不生效”问题：确认路由绑定更新本身会覆盖 `target_agent_id`，真正的卡点在于外部入站幂等缓存会优先返回旧响应，导致同一测试消息在改绑后仍绕过新 route；现已在 `channel_binding_upsert/delete` 及准入策略变更后清空幂等缓存，并为 `vb-task` 补充回归测试。 | 外部通道路由热更新、调试体验、Tauri tool adapter 命令、`vb-task` 幂等缓存 | AI |
| 2026-03-11 | T-175 | 继续收紧 station terminal 生命周期：未启动 session 的 station 不再默认挂载 xterm，而是改为轻量 idle 占位态，直接提供“启动终端会话 / 启动 CLI Agent”入口，减少空闲卡片对终端实例和首屏稳定性的消耗。 | StationCard 空闲态、xterm 挂载时机、工作站终端性能与稳定性 | AI |
| 2026-03-11 | T-175 | 继续提升工作站终端体验：将 `focus` 布局从“active card 与侧栏 miniature card 之间切换挂载”改为“主舞台保活全部 `StationCard`，仅切换显隐”，侧栏改为轻量 station rail，避免 xterm 实例在切换 active station 时销毁重建。 | 工作站聚焦布局、终端实例保活、侧栏交互、前端 terminal 稳定性 | AI |
| 2026-03-11 | T-175 | 修复工作站聚焦布局切换 active station 时 `StationXtermTerminal` 被卸载重挂后丢失已显示终端内容的问题：xterm 在解绑 sink 前会把当前 buffer 文本快照回传给 `ShellRoot`，下次挂载时优先用最新快照恢复。 | 工作站聚焦布局、终端内容恢复、前端 station terminal sink 绑定链路 | AI |
| 2026-03-11 | T-175 | 将“角色管理”侧栏从本地写死 station seed 切到 SQLite `agents + agent_roles`：前端启动后按当前 workspace 拉取真实角色/agent 数据；侧栏列表按钮由删除改为编辑，编辑复用新增弹窗，删除仅在编辑态可用；后端补充 `agent_create/update/delete` 对 `tool/workdir/custom_workdir` 的持久化。 | 角色管理侧栏、角色弹窗、SQLite agent schema、Tauri agent commands、前端 desktop API | AI |
| 2026-03-11 | T-175 | 将工作站画布布局控制从固定 `1x1/1x2/2x1/2x2/3x2/4x2` 预设改为 `auto/focus/custom` 三态；`custom` 支持动态设置行列数，并兼容旧本地存储值自动迁移到新配置。同时顺手移除 `StationOverviewPane` 中阻塞构建的未使用参数。 | 工作站画布局控制、布局偏好持久化、前端构建稳定性 | AI |
| 2026-03-11 | T-175 | 将工作站画布“聚焦布局”预设按钮的图标从通用字母占位改为 Lucide `LayoutPanelLeft`，与布局语义保持一致，并为该按钮补齐独立图标样式。 | 工作站画布局预设、布局切换按钮图标一致性 | AI |
| 2026-03-11 | T-175 | 将文件编辑器顶部 tab 的关闭按钮与 `CodeMirror` 搜索面板按钮/选项图标统一到 Lucide 体系：`FileEditorPane` 继续走 `AppIcon`，`CodeMirrorEditor` 则改为向 CodeMirror DOM 注入基于 Lucide glyph 的 SVG；同时避免引入 `react-dom/server` 造成主包体积膨胀。 | 文件编辑器标签栏、编辑器搜索面板、图标统一、前端打包体积 | AI |
| 2026-03-11 | T-175 | 继续清理 icon 残留：移除 `src/assets/react.svg`，将 `CodeMirrorEditor.scss` 中的搜索面板 SVG mask 图形改为纯文本按钮符号，并把 `_legacy-foundations.scss` 的 select 下拉箭头改为纯 CSS 几何背景，收口剩余手写 SVG 图源。 | 编辑器搜索面板、全局表单样式、前端静态资源、图标遗留治理 | AI |
| 2026-03-11 | T-175 | 引入 `lucide-react` 作为统一图标源，将 `shell/ui/icons.tsx` 改为库包装层，并收口 `SettingsModal`、`FileEditorPane`、`GitGraphView` 中的 React 内联 SVG；同步把 icon库加入 `docs/07` 白名单。 | 前端图标体系、依赖白名单、组件一致性、后续 icon 扩展方式 | AI |
| 2026-03-11 | T-175 | 将任务中心中的“外部通道”“派发历史”展示迁移到新的 `features/channels/CommunicationChannelsPane`，`ShellRoot` 的 `channels` 导航改为展示真实通信侧栏，`TaskCenterPane` 收敛为纯任务派发编辑。 | 任务中心侧栏、通信通道侧栏、前端 feature 边界、交互分工 | AI |
| 2026-03-11 | T-181 | 继续补强 `AGENTS.md` 的前端模块化约束：新增 “`features/` 为前端业务唯一落点”“`shell/` 仅负责壳层编排”“`components/` 仅放跨 feature 通用组件”“feature 专属样式与状态不得默认提升为全局” 等规则，并将 DoD 扩展为同时检查前后端目录边界。 | 仓库级协作规范、前端目录治理、后续 Agent 落点一致性 | AI |
| 2026-03-11 | T-181 | 补强 `AGENTS.md` 的后端模块化约束：新增 “Tauri command 必须按前端 feature 对齐建目录”“禁止新增根目录 command 散点文件”“`app_state` 仅负责全局装配”“测试文件不得散落源码根目录”等可执行规则，并将其纳入 DoD 检查项。 | 仓库级协作规范、后端目录治理、后续 Agent 落点一致性 | AI |
| 2026-03-11 | T-183 | 将 `apps/desktop-tauri/src-tauri/src/commands` 从平铺文件改为按前端 feature 对齐的目录结构：`workspace/file_explorer/terminal/git/task_center/tool_adapter/settings/keybindings`；同步迁移 `tools.rs -> tool_adapter/tool_profiles.rs`、`ai_config.rs -> settings/ai_config.rs`，并修正 `lib.rs`、`mcp_bridge.rs`、`channel_adapter_runtime.rs`、`connectors/telegram/mod.rs` 的模块引用。 | Tauri command 边界、前后端 feature 对齐、后端入口层可维护性 | AI |
| 2026-03-11 | T-182 | 整理 Rust 后端目录：将 `apps/desktop-tauri/src-tauri/src` 下散落的 `*_tests.rs` 收口到 `src/tests/` 与 `src/commands/tests/`，同步修正 `#[path]`；将 `apps/desktop-tauri/src-tauri/src/split_tests.py` 与 `crates/vb-task/src/split_tests.py` 迁入 `scripts/dev/rust/`，并更新 `split_all_tests.py` 以生成到新测试目录。 | Tauri Rust 壳层测试布局、辅助脚本落点、后续测试拆分流程 | AI |
| 2026-03-11 | T-175 | 将 `shortcut-bindings` 从 `src/shell/state/` 迁入 `src/features/keybindings/`，补齐 `index.ts` 并让 `ShellRoot` 改为从 feature 引入快捷键绑定逻辑；同时确认 `change-feed` 当前仍无可归位的前端实现。 | keybindings 目录落地、shell state 进一步瘦身、剩余空目录识别 | AI |
| 2026-03-11 | T-175 | 继续清理 `shell/layout`：将 `CodeMirrorEditor` 迁入 `src/components/editor/`，将工作站卡片/画布/管理弹窗/搜索弹窗迁入 `src/features/workspace-hub/`，并把原 `shell/layout/model.ts` 拆成 `workspace-hub/station-model.ts` 与 `shell/layout/navigation-model.ts`。 | 通用组件抽离、workspace UI 归位、shell 与 feature 类型边界 | AI |
| 2026-03-11 | T-175 | 继续消化空 feature 骨架：将 `SettingsModal` / `DisplayPreferences` 迁入 `src/features/settings/`，将 `StationXtermTerminal` 迁入 `src/features/terminal/`，将 channel management UI 与其 helper 迁入 `src/features/tool-adapter/`，并让 `ShellRoot`、`StationCard`、`WorkbenchCanvas` 改为经由 feature 导入。 | 前端模块边界、shell 编排层收口、feature 目录落地率 | AI |
| 2026-03-11 | T-175 | 清理 `apps/desktop-web` 根目录杂项：将历史样式修复说明、快速验证指南与对照 HTML 迁入 `apps/desktop-web/docs/legacy/`，将 `run-wsl.sh`、`start-wsl.bat`、`replace-colors.ps1` 迁入 `apps/desktop-web/scripts/`，并将根目录 `README.md` 改写为当前工程说明。 | 前端目录整洁度、运行入口可发现性、历史资料归档 | AI |
| 2026-03-11 | T-175 | 清理前端空骨架与错位组件：将 `shell/layout` 中成熟的文件浏览、Git、任务中心、工作站总览组件迁回 `src/features/{file-explorer,git,task-center,workspace}`，补齐各 feature barrel 导出并删除对应目录下已无必要的 `.gitkeep`。 | 前端目录边界、feature 模块可发现性、后续增量开发落点 | AI |
| 2026-03-10 | T-171 | 将 `task.dispatch_batch` 与 MCP bridge 的终端投递逻辑对齐外部通道链路：写入命令后先发送 runtime `submitSequence`，再补一次硬回车兜底，避免 MCP 派发只把文本停在输入框、不真正执行；同时明确 `gto_dispatch_task` 才是“写入并执行终端”的 tool，`gto_report_status/gto_handover` 仅为协作消息记录。 | MCP 派发可靠性、agent tool 使用语义、跨 CLI 提交稳定性 | AI |
| 2026-03-10 | T-171 | 修复 Windows/WSL 混合运行下的 MCP “split-brain” 问题：runtime 与 directory 改为优先从同一 `.gtoffice/mcp` 状态根解析，按目标 workspace 命中同源 runtime；同时在 public tool 返回与描述中明确标注“Windows 桌面端 + WSL agent/MCP 是支持场景”。已在非沙箱 probe 中验证 WSL 成功通过 Windows bridge `127.0.0.1:50568` 向 `agent-02` 发送任务。 | MCP runtime 选择、跨环境通讯稳定性、tool 引导文案、健康诊断 | AI |
| 2026-03-10 | T-171 | 将 MCP 公共协作模型改成“两步式”：新增 `gto_get_agent_directory`，bridge 新增 `directory.get` 与 `.gtoffice/mcp/directory.json` 快照；目录视图合并 repo agent 与 runtime-only agent；`gto_report_status/gto_handover` 支持从 `GTO_AGENT_ID` 自动识别 sender；station terminal 创建与 headless bootstrap 均注入 `GTO_WORKSPACE_ID/GTO_AGENT_ID/GTO_ROLE_KEY/GTO_STATION_ID`；随后补上无参 `workspace_id` 自动推断、`gto_health` 离线降级诊断，以及面向 agent 的通讯引导和专门错误 `MCP_BRIDGE_SEND_UNAVAILABLE`，避免把 snapshot 目录误当成可发送在线链路。 | MCP public contract、bridge 目录缓存、agent terminal 上下文注入、离线目录容错、agent 通讯引导、headless 协作 E2E | AI |
| 2026-03-10 | T-171 | 打通 `gto-agent-bridge` 真正的 headless MCP E2E：在 bridge 内新增内部 bootstrap 能力，用于无 UI 场景下打开 workspace、创建 terminal、注册在线 runtime；同时修复 `vb-terminal` 在 Tauri async runtime 中调用 `blocking_send` 导致的 panic，最终验证 `gto_dispatch_task` 与 `gto_handover` 均通过真实 MCP tool 成功发送。 | MCP bridge headless 引导、terminal runtime 稳定性、任务中心真实派发/交接链路 | AI |
| 2026-03-10 | T-171 | 修复 `mcp:e2e` 被旧 runtime 误导的问题：改为等待 runtime 文件刷新并轮询 `gto_health`；同时将 MCP 安装入口改为“开发态 local、发布态 npx 优先”，支持桌面端一键安装写入发布友好的 `npx` 命令，并补齐 `vb-task` 对 `handover` 在线目标成功回归。 | MCP E2E 验证链路、CLI MCP 安装配置、发布安装策略、任务中心回归覆盖 | AI |
| 2026-03-10 | T-175 | 恢复 Windows 原生顶栏稳定方案：将整个 `TopControlBar` 恢复为标题栏拖拽区，双击最大化挂回 `header` 并过滤按钮/输入等交互元素；同时修复 `_layout.scss` 中滚动条选择器截断导致的 Sass 构建错误。 | Shell 顶栏拖拽、双击最大化、Windows 原生标题栏行为、Shell 样式构建稳定性 | AI |
| 2026-03-10 | T-175 | 根据二轮视觉反馈继续收敛文件工作区：移除 `FileTreePane` 顶部文案与多余层叠，搜索/刷新改为纯图标；去掉编辑器顶部装饰性 traffic lights 与 `Editor Deck` 文案；统一文件搜索弹窗与预览/编辑器背景基底。 | 文件树工具条、编辑器标签区、文件预览头部、文件搜索弹窗一致性 | AI |
| 2026-03-10 | T-175 | 重做 `FileTreePane`、`FileEditorPane`、`FileSearchModal`、`FileContentPane` 的 Apple 风格 shell 视觉：新增组件独立 SCSS，迁移 `_layout.scss` 中相关样式，强化亮/暗主题分层材质与窗口 chrome。 | 文件树、文件搜索、编辑器标签栏、文件预览面板、样式边界治理 | AI |
| 2026-03-10 | T-175 | 重做 `apps/desktop-web/src/shell/layout/StationCard.tsx` 与对应 SCSS：引入 role/state pills、window chrome、terminal frame、亮暗主题差异化材质与终端舱体视觉。 | Station 工作站卡片、终端视觉层次、主题切换体验 | AI |
| 2026-03-10 | T-181 | 重构 `AGENTS.md`，新增“Codex 中 AGENTS 作用”与强制流程清单；将 `docs/README/03/04/05/06` 重写为 token 优化版，移除重复叙述，保留可执行契约。 | 协作流程、文档读取成本、会话交接质量 | AI |
| 2026-03-08 | T-172 | 修复 Telegram 回传超时导致会话候选提前移除问题，并补齐 preview 失败回滚与重试路径。 | 外部回复可靠性 | AI |

> 说明：历史长记录已保留在 Git 历史；本文件仅保留当前执行所需最小上下文。
