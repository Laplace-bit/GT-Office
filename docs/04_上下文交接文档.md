# GT Office 上下文交接文档

## 最近交接快照

- 里程碑：M2 MVP 完整
- 任务：T-188
- 本轮目标：响应 review，修复文件管理增强里三个边界缺陷，确保后端文件系统命令与文件树键盘流都不再回归。

## 已完成

- **T-188 review 缺陷已修复**：
  - 后端 `fs_copy`：新增目录复制安全校验，禁止把目录复制到自身后代路径，避免 `copy_dir_all` 递归复制新建目标目录。
  - 后端 `fs_create_dir`：当目标路径已存在且是文件时返回 `FS_CREATE_DIR_CONFLICT`，不再把失败场景伪装成成功。
  - 前端 `FileTreePane.tsx`：快捷键处理改为从聚焦元素最近的树节点解析 `data-kind` 与 `data-path`，恢复目录行焦点下的 `Cmd/Ctrl+V`、F2 等行为。
  - 测试：为“同名文件创建目录冲突”和“目录复制到自身后代”补充了 Rust 单测回归。

## 改动文件清单

- `apps/desktop-tauri/src-tauri/src/commands/file_explorer/mod.rs`
- `apps/desktop-tauri/src-tauri/src/commands/tests/filesystem_tests.rs`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `apps/desktop-web/src/features/file-explorer/FileTreePane.tsx`
- `apps/desktop-web/src/features/file-explorer/FileTreeModals.tsx`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 验证情况

- **前端构建验证**：`cd apps/desktop-web && npm run build` 已成功通过。
- **定向 lint 验证**：`cd apps/desktop-web && npx eslint src/features/file-explorer/FileTreePane.tsx` 已通过。
- **Rust 回归测试**：
  - `cargo test -p gtoffice-desktop-tauri create_dir_rejects_existing_file_target -- --nocapture` 已通过。
  - `cargo test -p gtoffice-desktop-tauri copy_rejects_directory_target_inside_source_tree -- --nocapture` 已通过。
- **未执行验证**：未跑 `cargo test -p gtoffice-desktop-tauri` 全量或 `cargo check --workspace`，本轮只做了与修复点直接相关的定向验证。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-188；review 指出的 3 个阻塞缺陷已修复，当前进入真实桌面环境回归阶段。
- **下一步第一动作**：在 Tauri 桌面端手工验证文件树目录焦点下的快捷键流，重点检查目录行 `Cmd/Ctrl+V`、F2、Delete，以及 `fs_show_in_folder`、复制到同级/子级目录的边界提示。
- **风险/阻塞与是否需要决策**：无阻塞；后端已防住 API 直接调用，但仍建议手工确认前端错误提示文案与交互是否足够清晰。
- **已执行验证命令与结果**：
  - `cd apps/desktop-web && npm run build`：已通过。
  - `cd apps/desktop-web && npx eslint src/features/file-explorer/FileTreePane.tsx`：已通过。
  - `cargo test -p gtoffice-desktop-tauri create_dir_rejects_existing_file_target -- --nocapture`：已通过。
  - `cargo test -p gtoffice-desktop-tauri copy_rejects_directory_target_inside_source_tree -- --nocapture`：已通过。
