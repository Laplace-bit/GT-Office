# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-175
- 本轮目标：收口前端 TSX 类型安全问题，清除 AI provider 配置链路中的 `any`/未声明全局对象，并把通信通道侧栏归回合理的 feature 目录。

## 已完成

- **AI provider 类型治理**：
  - 在 `shell/i18n/ui-locale.ts` 新增 `isTranslationKey` 与 `translateMaybeKey`，替换 `ClaudeConfigModal`、`LightAgentConfigModal`、`ProviderAgentCard` 中对翻译 key 的 `as any` 强转。
  - 新增 `features/settings/ai-providers/shared/provider-utils.ts`，统一 `describeUnknownError` 与 `codex/gemini` 轻量 agent 窄化类型。
  - `LightAgentConfigModal` 改为显式使用 `LightAgentDraftInput`，`onReload` 支持 `Promise`，`catch (err: any)` 已全部清除。
- **全局窗口扩展补齐**：
  - 新增 `src/app/window-extensions.d.ts`，为 `__GTO_OPEN_CHANNEL_STUDIO__` 提供正式声明。
  - `SettingsModal` 与 `ShellRoot` 改为使用已声明的 `window.__GTO_OPEN_CHANNEL_STUDIO__`，不再依赖 `(window as any)`。
- **目录归位**：
  - 将 `CommunicationChannelsPane.tsx/.scss` 从 `src/features/channels/` 迁回 `src/features/tool-adapter/`，并更新 `tool-adapter/index.ts` 与 `ShellRoot` 导入。
  - 删除空的 `src/features/channels/` 与 `src/shell/layout/channel-management/` 遗留目录，减少 feature 重叠。

## 改动文件清单

- `apps/desktop-web/src/app/window-extensions.d.ts` (new)
- `apps/desktop-web/src/features/settings/SettingsModal.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/AiProvidersSection.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/claude/ClaudeConfigModal.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/light-agents/LightAgentConfigModal.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/shared/ProviderAgentCard.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/shared/provider-utils.ts` (new)
- `apps/desktop-web/src/features/tool-adapter/CommunicationChannelsPane.tsx` (moved)
- `apps/desktop-web/src/features/tool-adapter/CommunicationChannelsPane.scss` (moved)
- `apps/desktop-web/src/features/tool-adapter/index.ts`
- `apps/desktop-web/src/shell/i18n/ui-locale.ts`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 验证情况

- `cd apps/desktop-web && npx tsc -b`：通过。
- 手动 UI 验证：**未验证**。本轮未在桌面端逐步点击 AI provider 弹窗与 Channel Studio，仅完成静态类型校验。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-175；前端类型治理与通道目录归位已完成，下一步应做桌面端手工回归并继续处理剩余 legacy foundation 拆分。
- **已完成与未完成**：
  - 已完成：AI provider 相关 `any` 清零、窗口扩展声明、`CommunicationChannelsPane` 迁回 `tool-adapter`、空目录清理。
  - 未完成：桌面端手动验证 Claude/Codex/Gemini 弹窗与 Settings -> Channels -> Channel Studio 跳转；`features/change-feed` 仍为空骨架，legacy styles 仍待后续收口。
- **下一步第一动作**：在桌面端打开 Settings，验证 AI provider 配置弹窗和 Channel Studio 跳转各一次，确认 `window.__GTO_OPEN_CHANNEL_STUDIO__` 链路与通道页渲染无回归。
- **风险/阻塞与是否需要决策**：
  - 风险：当前仓库工作树本身存在大量其他未提交改动；继续推进前需避免把本轮类型治理与其他功能改动混在一起。
  - 阻塞：无。
- **验证命令与结果**：
  - `cd apps/desktop-web && npx tsc -b`：通过。
