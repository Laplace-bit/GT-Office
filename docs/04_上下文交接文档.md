# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-171
- 本轮目标：修复 Agent 配置向导中“模式选择/轻量 provider 配置”不可用的交互逻辑，确保 Claude、Codex、Gemini 链路都能按真实功能语义完成配置。

## 已完成

- **Claude 配置流重构**：
  - 新增 `claudeConfigFlow.ts`，把 `ClaudeConfigModal` 从 `stepIndex` 硬编码跳转改为基于模式的动态步骤流。
  - `official/custom` 在“供应商模式”页选中后会自动进入下一可操作步骤，不再停留在无输入页。
  - 已安装 CLI 的场景打开 Claude 弹窗时会跳过纯检查页，直接落到首个可编辑步骤。
  - 预览按钮现在按后端真实规则判断可用性：预设/自定义会校验必填字段与可复用 secret，减少点了才报错的情况。
  - 预设名称在详情页与预览 diff 中做了本地化显示，不再把内部翻译 key 暴露给用户。
- **Light Agent 链路修复**：
  - `AiConfigOverlay` 补齐 `footer` 能力，Codex/Gemini 弹窗底部导航恢复可见。
  - Light Agent 已安装场景默认直接进入凭据页，减少一层无输入过渡页。
  - 凭据页补充官方文档入口与本地配置路径提示。
- **入口可达性修复**：
  - `ProviderAgentCard` 恢复 Gemini 的“配置”入口，轻量 provider 链路不再被 UI 隐藏。

## 改动文件清单

- `apps/desktop-web/src/features/settings/ai-providers/claude/ClaudeConfigModal.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/claude/ClaudeProviderWorkspace.scss`
- `apps/desktop-web/src/features/settings/ai-providers/claude/claudeConfigFlow.ts` (new)
- `apps/desktop-web/src/features/settings/ai-providers/light-agents/LightAgentConfigModal.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/light-agents/LightAgentProviderWorkspace.scss`
- `apps/desktop-web/src/features/settings/ai-providers/shared/AiConfigOverlay.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/shared/AiConfigOverlay.scss`
- `apps/desktop-web/src/features/settings/ai-providers/shared/ProviderAgentCard.tsx`
- `apps/desktop-web/src/shell/i18n/messages.ts`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`
- `docs/05_高质量功能设计_核心工作流.md`

## 验证情况

- `npx tsc -b` (apps/desktop-web)：通过。
- 手动 UI 验证：**未验证**。本轮仅完成静态类型校验，未在桌面端逐步点击 Claude/Codex/Gemini 配置弹窗。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-171；Agent 配置中心的主交互链路已修正，下一步建议做桌面端手工回归。
- **已完成与未完成**：
  - 已完成：Claude 动态步骤流、官方/自定义自动推进、Light Agent footer 恢复、Gemini 配置入口恢复。
  - 未完成：桌面端真实点击回归，确认 Claude `official/preset/custom` 三种模式的预览/应用链路与返回/切步行为都符合预期。
- **下一步第一动作**：在桌面端打开 Settings -> AI Providers，分别验证 Claude 的三种模式切换，以及 Codex/Gemini 底部按钮与文档链接是否可正常工作。
- **风险/阻塞与是否需要决策**：
  - 风险：本轮未做手工 UI 回归；如果 `AppIcon` 的 `activity` 在当前主题下动效不明显，可能需要再补一个明确的 loading 态样式。
  - 阻塞：无。
- **验证命令与结果**：
  - `cd apps/desktop-web && npx tsc -b`：通过。
