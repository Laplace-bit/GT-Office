# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-175
- 本轮目标：继续收敛工作站画布交互，统一图标入口并将固定布局预设改为可动态设置的布局控制

## 已完成

- 已将工作站画布布局控制从固定 preset 改为 `auto / focus / custom`
- `custom` 模式下可直接设置列数与行数，布局偏好会与左侧面板宽度一起持久化到 `localStorage`
- 已兼容旧版 `canvasLayoutPreset` 本地值；历史的 `1*1 / 1*2 / 2*1 / 2*2 / 3*2 / 4*2` 会自动映射到新的 `custom` 布局配置
- 已将工作站画布“聚焦布局”预设的图标改为 `LayoutPanelLeft`，不再沿用 `A` 字母占位
- 已引入 `lucide-react` 作为新的统一图标源
- `apps/desktop-web/src/shell/ui/icons.tsx` 已从手写 SVG `switch` 改为 icon 库包装层，现有 `AppIcon` 调用点无需整体改签名
- 已收口现有 React 组件中的零散内联 SVG：
  - `apps/desktop-web/src/features/settings/SettingsModal.tsx`
  - `apps/desktop-web/src/features/file-explorer/FileEditorPane.tsx`
  - `apps/desktop-web/src/features/git/GitGraphView.tsx`
- 已把 `lucide-react` 加入 `docs/07_依赖选型与精简清单.md` 白名单
- 已继续清理剩余旧图标来源：
  - 删除未使用的 `apps/desktop-web/src/assets/react.svg`
  - 将 `apps/desktop-web/src/styles/foundations/_legacy-foundations.scss` 中 select 下拉箭头改为纯 CSS 几何背景
- 已将文件编辑器的图标进一步统一到 Lucide：
  - `apps/desktop-web/src/features/file-explorer/FileEditorPane.tsx` 顶部 tab 关闭按钮继续经由 `AppIcon -> lucide-react`
  - `apps/desktop-web/src/components/editor/CodeMirrorEditor.tsx` 会向 CodeMirror 搜索面板 DOM 注入基于 Lucide glyph 的 SVG
  - 新增 `apps/desktop-web/src/components/editor/lucide-icon-nodes.ts`，用于在不引入 `react-dom/server` 的前提下复用 Lucide 图形
- 已同步更新：
  - `docs/03_项目开发进度跟踪.md`
  - `docs/04_上下文交接文档.md`
- 已移除 `StationOverviewPane` 中阻塞 `tsc` 的未使用 `onOpenManageModal` 参数，恢复前端构建通过

## 未完成

- 品牌图标目前已统一走 icon 库映射，但若后续要求“官方品牌识别度”更高，可能还要单独决定是否为 Telegram/Feishu 接入专门品牌图形资源

下一步第一动作：

- 回归工作站画布在桌面端和窄宽度下的 `custom` 布局交互，确认数字输入与 focus 模式切换的视觉密度是否还需要继续收敛

## 改动文件清单

- `apps/desktop-web/src/shell/ui/icons.tsx`
- `apps/desktop-web/src/shell/i18n/messages.ts`
- `apps/desktop-web/src/features/settings/SettingsModal.tsx`
- `apps/desktop-web/src/features/file-explorer/FileEditorPane.tsx`
- `apps/desktop-web/src/features/git/GitGraphView.tsx`
- `apps/desktop-web/src/components/editor/CodeMirrorEditor.scss`
- `apps/desktop-web/src/components/editor/CodeMirrorEditor.tsx`
- `apps/desktop-web/src/components/editor/lucide-icon-nodes.ts`
- `apps/desktop-web/src/features/workspace-hub/WorkbenchCanvas.scss`
- `apps/desktop-web/src/features/workspace-hub/WorkbenchCanvas.tsx`
- `apps/desktop-web/src/features/workspace/StationOverviewPane.tsx`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `apps/desktop-web/src/styles/foundations/_legacy-foundations.scss`
- `apps/desktop-web/package.json`
- `package-lock.json`
- `docs/07_依赖选型与精简清单.md`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 风险 / 阻塞

- `npm install` 期间根目录 `node_modules` 的 `lightningcss` 临时目录曾有残留；依赖已装上，但后续若再改依赖，仍建议留意 Windows/WSL 混合环境下的 npm 清理警告
- CodeMirror 搜索面板的图标是“注入到第三方 DOM”的方案，不是普通 React 组件树；后续若升级 CodeMirror 搜索面板 DOM 结构，需要回归这段注入逻辑

## 验证命令与结果

- 已执行 `cd apps/desktop-web && npm run build`，通过
- 打包结果：`dist/assets/index-CVbxk_bK.js` 约 `1195.05 kB`，仍有 Vite 默认的大 chunk warning，但不阻塞构建

## Next Agent Starter

- 当前里程碑与本轮目标：M2 / T-175；已把前端 React 图标入口统一到 `lucide-react`
- 已完成与未完成：`AppIcon` 已改为库包装层，文件编辑器顶部 tab、搜索面板，以及工作站画布“聚焦布局”按钮图标都已切到 Lucide；同时工作站画布已从固定布局 preset 改为 `auto/focus/custom` 三态，`custom` 可动态设置行列并兼容旧偏好值。当前剩余观察点主要是 CodeMirror DOM 结构未来变更时的兼容性，以及 `custom` 布局在小屏宽度下是否还要进一步压缩头部控件
- 改动文件清单：`apps/desktop-web/src/shell/ui/icons.tsx`、`apps/desktop-web/src/shell/i18n/messages.ts`、`apps/desktop-web/src/features/settings/SettingsModal.tsx`、`apps/desktop-web/src/features/file-explorer/FileEditorPane.tsx`、`apps/desktop-web/src/features/git/GitGraphView.tsx`、`apps/desktop-web/src/components/editor/CodeMirrorEditor.tsx`、`apps/desktop-web/src/components/editor/CodeMirrorEditor.scss`、`apps/desktop-web/src/components/editor/lucide-icon-nodes.ts`、`apps/desktop-web/src/features/workspace-hub/WorkbenchCanvas.tsx`、`apps/desktop-web/src/features/workspace-hub/WorkbenchCanvas.scss`、`apps/desktop-web/src/features/workspace/StationOverviewPane.tsx`、`apps/desktop-web/src/shell/layout/ShellRoot.tsx`、`apps/desktop-web/src/styles/foundations/_legacy-foundations.scss`、`apps/desktop-web/package.json`、`package-lock.json`、`docs/07_依赖选型与精简清单.md`、`docs/03_项目开发进度跟踪.md`、`docs/04_上下文交接文档.md`
- 风险/阻塞与是否需要决策：若希望 Telegram/Feishu 必须使用官方品牌 Logo，而非统一风格图标，需要单独决策；CodeMirror 搜索面板图标注入逻辑需随上游 DOM 结构变化回归；工作站画布 `custom` 头部控件若后续继续增项，可能需要迁入弹出式设置而不是继续堆叠在 header
- 验证命令与结果：已执行 `cd apps/desktop-web && npm run build`，通过；主包约 `1195.05 KB`
