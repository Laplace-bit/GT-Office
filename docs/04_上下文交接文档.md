# GT Office 上下文交接文档

## 最近交接快照

- 里程碑：M2 MVP 完整
- 任务：T-175
- 本轮目标：删除“动态氛围灯”相关的 UI、功能、样式和设置，确保设置页、偏好持久化、Shell 背景层与样式令牌均无残留。

## 已完成

- 前端删除动态氛围灯整套实现：
  - `apps/desktop-web/src/features/settings/DisplayPreferences.tsx` 与 `SettingsModal.tsx` 已移除动态氛围灯开关、强度分段控件及相关 props。
  - `apps/desktop-web/src/shell/state/ui-preferences.ts` 已删除 `ambientLightingEnabled/ambientLightingIntensity` 字段，不再向根节点写入 ambient dataset。
  - `apps/desktop-web/src/shell/layout/ShellRoot.tsx` 已移除运行时设置同步、用户设置持久化补丁和背景层挂载；`apps/desktop-web/src/shell/layout/AmbientBackgroundLighting.tsx` 已删除。
  - `apps/desktop-web/src/styles/foundations/_legacy-foundations.scss` 与 `apps/desktop-web/src/features/settings/DisplayPreferences.scss` 已清理 ambient 设计令牌、data-attribute 样式和对应控件样式。
  - `apps/desktop-web/src/shell/i18n/messages.ts` 已删除动态氛围灯相关文案键。
- 文档：
  - 已更新 `docs/03` 与本交接快照，记录本次 T-175 的动态氛围灯移除。

## 改动文件清单

- `apps/desktop-web/src/features/settings/DisplayPreferences.tsx`
- `apps/desktop-web/src/features/settings/DisplayPreferences.scss`
- `apps/desktop-web/src/features/settings/SettingsModal.tsx`
- `apps/desktop-web/src/shell/state/ui-preferences.ts`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `apps/desktop-web/src/shell/layout/AmbientBackgroundLighting.tsx`（已删除）
- `apps/desktop-web/src/shell/i18n/messages.ts`
- `apps/desktop-web/src/styles/foundations/_legacy-foundations.scss`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 验证情况

- `cd apps/desktop-web && npm run build`：通过
- 未执行桌面端人工回归；建议下一任重点验证设置页“显示偏好”是否不再出现动态氛围灯，且 Shell 背景无空白层级或视觉回归。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-175；本轮已删除动态氛围灯的设置、偏好、背景层和样式残留，下一步是做桌面端人工回归，确认视觉层级稳定且设置页无残留入口。
- **已完成与未完成**：
  - 已完成：动态氛围灯设置项、背景组件、UI 偏好字段、i18n 文案和 ambient 设计令牌已删除；进度与交接文档已同步。
  - 未完成：未做桌面端人工回归，尚未确认删除后浅色/深色主题下 Shell 背景层级是否完全符合预期。
  - 下一步第一动作：打开桌面端设置页与主 Shell，确认“显示偏好”中已无动态氛围灯，且主界面背景无异常空层或闪烁。
- **风险/阻塞与是否需要决策**：
  - 本次删除未触及后端 settings schema；若未来希望彻底清理历史配置快照中的 `ui.ambientLighting` 脏字段，需要单独补迁移或清洗策略。
  - 当前仅完成构建验证，若视觉团队仍依赖该背景层制造分区感，需要再决定是否以更克制的静态材质替代。
- **验证命令与结果**：
  - `cd apps/desktop-web && npm run build`：通过
