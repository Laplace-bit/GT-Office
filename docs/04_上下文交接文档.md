# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读本文件（`docs/04_上下文交接文档.md`）。
4. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
5. 阅读 `docs/06_API与事件契约草案.md`。
6. 阅读 `AGENTS.md`。

## Handover Snapshot

- Date: 2026-03-08 CST
- Owner: AI
- Current Milestone: M2 MVP 完整
- Goal of Current Session: 修复 `T-172` 中 Telegram 出站超时后回复丢失（`CHANNEL_REPLY_SEND_FAILED`）问题，确保失败可重试且不丢会话
- Target Workspace: `/mnt/c/personal/vbCode`

### Completed

1. 修复外部回复 finalize 生命周期：`take_external_reply_dispatch_candidates` 不再在产出 `Finalize` 候选时提前移除 session，改为发送成功后显式 `mark_external_reply_finalize_delivered` 清理。
2. 修复 preview 失败后“卡死不再重发”：新增 `mark_external_reply_preview_delivery_failed`，在发送失败时回滚 `last_preview_text/last_preview_sent_at_ms`，允许下轮重试。
3. 修复“无输出阶段 idle 误清理”问题：无文本 session 仅在 `ended/expired` 或“已存在 rendered snapshot 且 ready prompt”时才允许清理，避免首段输出延迟导致整会话丢失。
4. `flush_external_reply_candidates` 接入新状态流转：`Finalize` 成功后清理 session；`Preview` 失败时执行回滚。
5. 新增/调整回归测试：`external_reply_finalize_candidate_retries_until_marked_delivered`、`external_reply_preview_failure_resets_throttle_for_retry`，并更新 `external_reply_flush_is_single_shot_after_idle` 以匹配“成功确认后清理”语义。
6. 更新文档：`docs/03_项目开发进度跟踪.md`、本交接文档。

### Not Completed

1. 未做 Telegram 真机端到端复测（仅完成本地单元测试 + `cargo check`）。
2. 全量 `app_state` 测试在当前分支存在既有失败项（与本轮改动无直接对应），本轮未收敛全量绿灯。

### Changed Files

1. `apps/desktop-tauri/src-tauri/src/app_state.rs`
2. `apps/desktop-tauri/src-tauri/src/app_state_tests.rs`
3. `apps/desktop-tauri/src-tauri/src/commands/channel_adapter.rs`
4. `docs/03_项目开发进度跟踪.md`
5. `docs/04_上下文交接文档.md`

### Risks / Blockers

1. Telegram 网络层若持续不可达（防火墙/代理故障）仍会失败；本轮解决的是“瞬时失败不丢会话”，不是“无网络可用”场景。
2. 当前仓库有大量并行改动，全量测试信号噪音较高，后续需要专门窗口做测试债务收敛。

### Validation

1. `cargo test -p gtoffice-desktop-tauri external_reply_finalize_candidate_retries_until_marked_delivered -- --nocapture`：通过。
2. `cargo test -p gtoffice-desktop-tauri external_reply_preview_failure_resets_throttle_for_retry -- --nocapture`：通过。
3. `cargo test -p gtoffice-desktop-tauri external_reply_flush_is_single_shot_after_idle -- --nocapture`：通过。
4. `cargo test -p gtoffice-desktop-tauri external_reply_binding_kept_when_no_output_yet -- --nocapture`：通过。
5. `cargo check -p gtoffice-desktop-tauri`：通过。
6. `cargo test -p gtoffice-desktop-tauri app_state -- --nocapture`：失败（当前分支既有多项失败，本轮未做全量收敛）。

## Next Agent Starter

- Current Milestone: M2 MVP 完整（T-172 持续收敛）
- Session Goal: 在真实 Telegram 网络环境复测“超时后自动重试且不丢回复”，确认 `CHANNEL_REPLY_SEND_FAILED` 不再导致永久丢消息
- First Action: 发送一条会触发较长 agent 回复的 Telegram 消息，观察 `external/channel_outbound_result` 是否在首次失败后仍能后续成功投递（同一 trace）
- Success Criteria:
  1. 单次网络抖动后，回复最终仍可到达 Telegram（不因首次 timeout 丢失）。
  2. Preview 发送失败后可自动再次发送，不会卡在“已有预览但不再重发”状态。
  3. 无输出等待阶段不会提前移除绑定，会话后续输出可正常回传。
- Target Workspace: `/mnt/c/personal/vbCode`
