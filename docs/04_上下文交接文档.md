# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读本文件（`docs/04_上下文交接文档.md`）。
4. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
5. 阅读 `docs/06_API与事件契约草案.md`。
6. 阅读 `AGENTS.md`。

## Handover Snapshot

- Date: 2026-03-01 CST
- Owner: AI
- Current Milestone: M2 MVP 完整
- Goal of Current Session: 完成 Telegram 调试体验增强 + 设置页 Channel 管理重构 + Windows token 持久化修复
- Target Workspace: `workspace_root=/mnt/c/personal/vbCode`

### Completed

1. Telegram 入站调试弹窗增强：
   - 后端 `external/channel_inbound` 事件新增 `senderName/text` 字段。
   - 前端弹窗新增“消息内容”展示，并对超长文本做截断保护（280 字符）。
2. 设置页 Channel 管理重构（组件抽离）：
   - 设置主页仅保留 `SettingsChannelEntryCard` 入口卡片。
   - 新增 `SettingsChannelManagerModal`，承载综合配置（机器人、路由、准入、健康检查）。
   - 入口与管理界面解耦，降低设置主页复杂度。
3. 多语言风格收敛：
   - 新增组件文案全部改为 `messages.ts` key，不使用代码内联中英文本。
4. Windows token 持久化修复：
   - `credential_store` 新增 Windows Credential Manager 实现（`CredWriteW/CredReadW`）。
   - Windows 平台移除静默内存回退，避免“显示保存成功但重启丢失”。
5. 契约与进度文档同步：
   - `docs/06` 更新 `external/channel_inbound` payload（新增 `senderName/text`）。
   - `docs/03` 新增 `T-172` 第十七轮记录。

### Changed Files

1. `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
2. `apps/desktop-web/src/shell/layout/shell-layout.css`
3. `apps/desktop-web/src/shell/i18n/messages.ts`
4. `apps/desktop-web/src/shell/integration/desktop-api.ts`
5. `apps/desktop-web/src/shell/layout/SettingsModal.tsx`
6. `apps/desktop-web/src/shell/layout/SettingsChannelEntryCard.tsx`（新增）
7. `apps/desktop-web/src/shell/layout/SettingsChannelManagerModal.tsx`（新增）
8. `apps/desktop-tauri/src-tauri/src/commands/channel_adapter.rs`
9. `apps/desktop-tauri/src-tauri/src/connectors/credential_store.rs`
10. `docs/03_项目开发进度跟踪.md`
11. `docs/04_上下文交接文档.md`
12. `docs/06_API与事件契约草案.md`

### Decisions

1. 调试弹窗继续只对 `telegram` 入站触发，避免泛化成全通道噪声。
2. 设置主页只做“入口与摘要”，复杂配置放入独立管理弹窗。
3. Windows 凭据写入失败不再静默降级内存，直接返回错误，优先保证“保存语义真实可持久”。

### Pending Next Actions

1. Feishu connector 实装（至少账户 upsert/list + health 读路径），并接入新机器人配置区块。
2. 增加 Telegram bot E2E 联调记录（polling 默认路径 + webhook 高级路径各 1 条）并沉淀到 `docs/05`。
3. 评估是否需要将 Telegram 调试弹窗做成设置项开关（当前默认开启）。
4. 评估 polling worker 的退出生命周期与 backoff 策略（当前为固定轮询周期）。

### Risks / Blockers

1. polling 目前基于 `curl` 阻塞调用 + 周期轮询，网络抖动下恢复策略仍偏基础（待加指数退避）。
2. Windows 企业网络/中间人代理场景下，Telegram TLS 仍有长尾（虽已加 schannel 重试）。
3. Feishu connector 尚未实现，当前管理弹窗里 Feishu 仍为“预留入口”状态。

### Validation

1. `npm run typecheck`（通过）
2. `cargo check --workspace`（通过）

## Next Agent Starter

- Current Milestone: M2 MVP 完整
- Session Goal: 继续 `T-172`，补齐 Feishu connector 最小闭环，并收敛 Telegram 调试体验
- First Action (must be executable): 在 `apps/desktop-tauri/src-tauri/src/connectors/` 新增 `feishu.rs`，先实现 `account_upsert/account_list/health_check`；随后在 `commands/channel_adapter.rs` 注册 `feishu` 分支并跑 `cargo check --workspace`
- Success Criteria:
  1. `channel_connector_account_upsert/list/health` 对 `feishu` 返回可用结果而非 unsupported。
  2. Telegram/Feishu connector 关键模块无阻塞性编译错误。
  3. `cargo check --workspace` 与 `npm run typecheck` 通过。
- Target Workspace (workspace_id or root path): `/mnt/c/personal/vbCode`

### Completed in Last Session

1. Telegram 入站调试弹窗支持显示消息内容（后端事件 + 前端类型 + UI 渲染）。
2. 设置页完成“入口卡片 + 管理弹窗”重构，并抽离独立组件。
3. 新增文案全部采用 i18n key（不使用内联中英文文案）。
4. Windows 凭据后端接入 Credential Manager，修复重启 token 丢失风险。
5. 更新 `docs/03`、`docs/04`、`docs/06`。

### Pending (ordered)

1. Feishu connector 最小闭环（account + health）实现。
2. 在命令入口接入 Feishu connector，并更新设置页调用路径。
3. 补联调验收记录（Telegram polling 默认路径 + webhook 可选路径）。
4. 评估 Telegram 调试弹窗开关（是否放到设置页 channels）。
5. 为 Telegram polling 补运行态指标（最近轮询时间、连续失败次数）并展示到设置页/任务中心。

### Validation Commands

1. `npm run typecheck`（通过）
2. `cargo check --workspace`（通过）

### Risks / Decisions Needed

1. Feishu connector 的凭据字段与健康策略是否对齐 openclaw（app_id/app_secret + tenant_access_token）。
2. Telegram webhook 高级路径是否仍要提供“一键隧道”能力，还是仅文档化外部隧道工具。
3. polling worker 是否需要持久化 offset 到磁盘（当前为进程内记忆，重启后可能短窗口重复拉取）。
