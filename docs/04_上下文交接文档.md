# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-171
- 本轮目标：收敛多 Agent MCP 对话噪音、把“agent 间通信默认走 GT Office MCP”固化到安装/工具层，并修复 `sender_agent_id` 缺失时的被拒绝问题。

## 已完成

- **MCP 输出降噪**：
  - `tools/gto-agent-mcp/src/mcp-server.mjs`：缩短 `tools/list` 中各 tool 的描述；`gto_get_agent_directory` 正常返回改为最小目录视图，只保留 `workspaceId/directoryVersion/updatedAtMs/agents/self`；`gto_health` 在 bridge 正常时只返回 `bridgeAvailable/self/directory`，不再附带长段 `communicationGuidance/environmentNote`。
  - `tools/gto-agent-mcp-sidecar/src/main.rs`：与 JS server 保持同样的描述和输出收敛策略，避免不同分发形态出现不一致行为。
- **workspace/sender 自动识别兜底**：
  - `gto_dispatch_task/gto_report_status/gto_handover` 现在都支持 `workspace_id` 自动解析。
  - `gto_report_status/gto_handover` 的 sender 解析扩成“显式参数 -> `GTO_AGENT_ID` -> 当前 cwd/目录自识别”，降低因 terminal env 缺失导致的 `MCP_INVALID_PARAMS`。
- **sender inbox 查询**：
  - `crates/vb-task/src/lib.rs`：新增最近 `channel/message` 内存历史与 `list_messages()` 查询能力。
  - `apps/desktop-tauri/src-tauri/src/commands/task_center/mod.rs` / `apps/desktop-tauri/src-tauri/src/mcp_bridge.rs`：新增 `channel_list_messages` command 与 `channel.list_messages` bridge 方法。
  - `tools/gto-agent-mcp/src/mcp-server.mjs` / `tools/gto-agent-mcp-sidecar/src/main.rs`：新增 `gto_list_messages`，默认按当前 agent inbox 读取最近 `status/handover` 回复正文。
- **安装器默认协作规则**：
  - `tools/gto-agent-mcp/src/installer.mjs`：安装 MCP server 时同时写入受管 communication policy 文件 `~/.gtoffice/mcp/agent-communication.md`，并把 `GTO_AGENT_COMMUNICATION_POLICY_FILE` 注入 MCP 配置。
  - 安装器还会在常见指令文件位置写入托管规则块：`~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`、`~/.gemini/GEMINI.md`、`~/.qwen/QWEN.md`。
- **桌面端安装提示与文档同步**：
  - `apps/desktop-tauri/src-tauri/src/commands/agentic_one.rs`：安装成功提示改为“已部署 MCP bridge 和默认 GT Office communication rules”。
  - 已更新 `docs/03`、`docs/05`、`docs/06`、`tools/gto-agent-mcp/README.md`。
- **agent 通讯测试完成**：
  - 2026-03-17：在 GT Office MCP 上完成当前 agent 定向回复测试，已通过 `gto_report_status` 向目标 agent 回传普通回复与四句短诗，消息可追溯任务为 `task_19cfc45d74f_2`、`task_19cfc46aed6_6`、`task_19cfc4879bd_a`。

## 改动文件清单

- `tools/gto-agent-mcp/src/mcp-server.mjs`
- `tools/gto-agent-mcp-sidecar/src/main.rs`
- `tools/gto-agent-mcp/src/installer.mjs`
- `tools/gto-agent-mcp/README.md`
- `apps/desktop-tauri/src-tauri/src/commands/agentic_one.rs`
- `apps/desktop-tauri/src-tauri/src/commands/task_center/mod.rs`
- `apps/desktop-tauri/src-tauri/src/mcp_bridge.rs`
- `apps/desktop-tauri/src-tauri/src/lib.rs`
- `apps/desktop-web/src/shell/integration/desktop-api.ts`
- `crates/vb-task/src/lib.rs`
- `crates/vb-task/tests/lib_tests.rs`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`
- `docs/05_高质量功能设计_核心工作流.md`
- `docs/06_API与事件契约草案.md`

## 验证情况

- `node --check tools/gto-agent-mcp/src/mcp-server.mjs`：通过。
- `node --check tools/gto-agent-mcp/src/installer.mjs`：通过。
- `node --input-type=module -e "... installAll(...)"`：通过；在临时 home 目录下成功写入 MCP 配置、`GTO_AGENT_COMMUNICATION_POLICY_FILE` 和各 CLI 指令文件托管块。
- `cargo check --manifest-path tools/gto-agent-mcp-sidecar/Cargo.toml`：通过。
- `cargo test -p vb-task list_messages_returns_recent_filtered_inbox -- --nocapture`：通过。
- `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`：通过。
- `gto_report_status` 定向回复测试：通过；2026-03-17 已完成当前 agent 通讯验证，并补记“agent 通讯测试完成”到交接文档。
- 真正依赖 live GT Office bridge 的 MCP 端到端派发/回传：**未验证**。本轮未连接真实在线 runtime 做 CEO/Dev 往返回归。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-171；核心静态验证已通过，下一步应补 live bridge 下的真实往返回归，确认 manager 可通过 `gto_list_messages` 拉到 worker 的 `status/handover` 正文。
- **已完成与未完成**：
  - 已完成：正常输出降噪、workspace 自动解析、sender cwd/目录兜底、安装器 communication policy 落盘、`gto_list_messages` inbox 查询、JS/Rust 静态验证。
  - 未完成：还没在真实 GT Office bridge 上跑 manager -> worker -> status/handover -> `gto_list_messages` 的 live 回归。
- **下一步第一动作**：在真实在线 workspace 下执行一次 `gto_get_agent_directory -> gto_dispatch_task -> gto_report_status -> gto_list_messages` 闭环，确认 sender 能拿到回复正文。
- **风险/阻塞与是否需要决策**：
  - 风险：当前“默认协作规则”采取的是“安装器写入受管 policy 文件 + 常见指令文件托管块”的兼容方案，不同 CLI 是否都会实际消费这些文件需要回归验证。
  - 风险：`gto_list_messages` 只能读到通过 `channel.publish` 进入消息总线的内容；如果 worker 只在自己终端里打印一句话，sender 仍然拿不到。
  - 风险：Rust sidecar 当前返回中 `senderResolvedFrom` 在 human 场景下会保留 `null` 字段，若后续要求更极致的 payload 收敛，可再做一次结构体级裁剪。
  - 阻塞：无。
- **验证命令与结果**：
  - `node --check tools/gto-agent-mcp/src/mcp-server.mjs`：通过。
  - `node --check tools/gto-agent-mcp/src/installer.mjs`：通过。
  - `node --input-type=module -e "... installAll(...)"`：通过。
  - `cargo check --manifest-path tools/gto-agent-mcp-sidecar/Cargo.toml`：通过。
  - `cargo test -p vb-task list_messages_returns_recent_filtered_inbox -- --nocapture`：通过。
  - `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`：通过。
