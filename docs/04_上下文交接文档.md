# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-175
- 本轮目标：将“通信通道”侧栏从运维信息面板改成面向普通用户的消息流，只保留内容、时间、channel、状态，并用更克制的 Apple 风格重构样式层级。

## 已完成

- **通信通道侧栏简化**：
  - `apps/desktop-web/src/features/tool-adapter/CommunicationChannelsPane.tsx`：移除运行状态、Webhook、路由/白名单/幂等等摘要与派发历史区，仅保留最近消息流；每条消息展示内容、时间、channel、状态。
  - `apps/desktop-web/src/features/tool-adapter/CommunicationChannelsPane.scss`：重做为单主面板消息流样式，采用轻玻璃 + 分隔式列表，避免卡片嵌套。
- **事件载荷补齐**：
  - `apps/desktop-tauri/src-tauri/src/commands/tool_adapter/mod.rs`：`external/channel_outbound_result` 事件新增 `channel` 字段。
  - `apps/desktop-web/src/shell/integration/desktop-api.ts` / `apps/desktop-web/src/shell/layout/ShellRoot.tsx`：前端订阅类型与状态映射同步更新，避免从展示文案反推 channel/status。
- **文档同步**：
  - 已更新 `docs/03`、`docs/05`、`docs/06`，记录本轮用户视图简化与事件契约补充。

## 改动文件清单

- `apps/desktop-tauri/src-tauri/src/commands/tool_adapter/mod.rs`
- `apps/desktop-web/src/features/tool-adapter/CommunicationChannelsPane.tsx`
- `apps/desktop-web/src/features/tool-adapter/CommunicationChannelsPane.scss`
- `apps/desktop-web/src/shell/integration/desktop-api.ts`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `apps/desktop-web/src/shell/i18n/messages.ts`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`
- `docs/05_高质量功能设计_核心工作流.md`
- `docs/06_API与事件契约草案.md`

## 验证情况

- `cd apps/desktop-web && npm run build`：通过。
- 桌面端 GUI 手动回归：**未验证**。本轮仅完成静态构建验证，尚未在真实窗口中人工检查消息流视觉与滚动体感。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-175；通信通道侧栏已切成用户视图，下一步优先做真实桌面窗口回归，确认消息流在亮暗主题、长文本、空状态和窄宽度下的观感是否符合预期。
- **已完成与未完成**：
  - 已完成：侧栏信息精简、单主面板 Apple 风格重构、出站事件 `channel` 透传。
  - 未完成：未做 GUI 手动回归；也未评估是否需要把“最近外部事件”文案进一步改成更口语化的“最近消息”。
- **下一步第一动作**：启动桌面端，进入“通信通道”侧栏，分别验证 1. Telegram/飞书有消息时列表是否只显示四项元信息；2. 长内容是否换行自然；3. 窄宽度下 meta pills 是否正确折行。
- **风险/阻塞与是否需要决策**：
  - 风险：老的出站历史事件若在本次代码生效前产生，可能仍没有 `channel`，当前 UI 会回退到首个 configured channel 或 `external`。
  - 阻塞：无。
- **验证命令与结果**：
  - `cd apps/desktop-web && npm run build`：通过。
