# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
4. 阅读 `docs/06_API与事件契约草案.md`。
5. 阅读本文件的 `Handover Snapshot` 与 `Next Agent Starter`。

## Handover Snapshot

- Date: 2026-02-11 17:05 CST
- Owner: AI
- Current Milestone: M1 MVP 内核
- Goal of Current Session: 回退到自绘顶栏并仅保留性能优化（T-129）
- Target Workspace: `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `workspace_root=/mnt/c/project/vbCode`

### Completed

1. 按产品约束撤回 Windows 原生标题栏路径，恢复统一自绘顶栏（不增加系统标题栏高度）。
2. 回退 `ShellRoot` 的 `nativeWindowTopWindows/isWindowsPlatform/windowSetDecorations(true)` 分流逻辑，恢复 `nativeWindowTop = tauriRuntime` 与 `windowSetDecorations(false)`。
3. 维持“只做性能”的优化项：窗口状态同步 `120ms` 去抖（降低 `windowIsMaximized` 高频调用）与 Windows 顶栏去模糊（降低拖动重绘开销）。
4. 清理残留链路：移除 `onWindowStartDragging` 透传，`TopControlBar` 回归声明式拖拽区（`data-tauri-drag-region`）。
5. 更新 `docs/03_项目开发进度跟踪.md`：新增 `T-129` 与 2026-02-11 变更记录。

### Changed Files

1. `apps/desktop-web/src/shell/layout/shell-layout.css`
2. `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
3. `apps/desktop-web/src/shell/layout/TopControlBar.tsx`
4. `docs/03_项目开发进度跟踪.md`
5. `docs/04_上下文交接文档.md`

### Decisions

1. 界面约束优先：不采用会增加标题栏高度的系统装饰栏方案。
2. 性能优化与交互语义解耦：保持现有拖拽语义不变，仅优化高频状态同步与重绘成本。

### Pending Next Actions

1. 第一动作：在 Windows 手动回归顶栏拖动顺滑度与双击最大化/还原。
2. 第二动作：若仍有“卡一卡”，继续做纯性能项（拖动期间临时禁用顶部 hover transform/阴影）。

### Risks / Blockers

1. 无阻塞；构建有既有 chunk size warning（非本次引入）。

### Validation

1. Command: `npm --workspace apps/desktop-web run build`
   Result: 通过（`tsc -b` + `vite build` 均通过，仅 chunk size warning）。

## Next Agent Starter

- Current Milestone: M1 MVP 内核
- Session Goal: 在不改变自绘顶栏交互语义的前提下，继续压低 Windows 拖动卡顿
- First Action (must be executable): 在 `workspace_root=/mnt/c/project/vbCode` 执行 `rg -n "windowResizeSyncTimerRef|setTimeout\\(|120\\)|windowIsMaximized|nativeWindowTop = tauriRuntime|windowSetDecorations\\(false\\)|backdrop-filter: none|data-tauri-drag-region" apps/desktop-web/src/shell/layout/ShellRoot.tsx apps/desktop-web/src/shell/layout/shell-layout.css apps/desktop-web/src/shell/layout/TopControlBar.tsx`，确认当前为“自绘顶栏 + 纯性能优化”状态。
- Success Criteria:
  1. 保持自绘顶栏形态不变（无原生标题栏高度占用）。
  2. 双击空白区最大化/还原与普通拖动行为均正常。
  3. `npm --workspace apps/desktop-web run build` 通过。
- Target Workspace (workspace_id or root path): `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `/mnt/c/project/vbCode`

### Completed in Last Session

1. `T-129` 完成：回退到自绘顶栏并保留纯性能优化项，移除原生标题栏分流。
2. `docs/03` 与 `docs/04` 已同步更新。

### Pending (ordered)

1. Windows 手动回归慢拖/快拖/连拖场景。
2. 若仍有反馈，继续做“拖动期间禁用 hover 动效”类纯性能优化。

### Validation Commands

1. `npm --workspace apps/desktop-web run build` -> 通过（含 chunk size warning）。

### Risks / Decisions Needed

1. 无阻塞决策项；仅需体验校准反馈。
