# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-171
- 本轮目标：完成 Agent 供应商 Logo 映射、中英双语介绍与模型说明的“永不过时”化改造。

## 已完成

- **图片资源迁移与组织**：
  - 将 `temp/` 目录下的 Logo 图片移至 `apps/desktop-web/public/assets/logos/` 并规范命名（`claude.png`, `openai.png`, `gemini.png`, `chatgpt.png`）。
- **Logo 全链路映射**：
  - `ProviderAgentCard.tsx` 现在根据 Agent 类型展示对应的品牌 Logo。
  - `ClaudeConfigModal.tsx` 的预设列表已支持 Logo 展示，并为未提供图片的厂商配置了美观的占位符。
- **“永不过时”的多语言方案**：
  - **后端 Key 化**：重构 `catalog.rs` 和 `service.rs`，不再下发硬编码的英文介绍，改为下发翻译 Key（如 `aiConfig.agent.claude.subtitle`）。
  - **前端动态翻译**：更新 React 组件，统一使用 `t(locale, key)` 进行内容渲染，确保介绍词、模型说明、操作步骤均支持中英实时切换。
  - **文案优化**：修订所有 Agent 介绍文案，剔除具有时效性的描述，确保内容长期有效。
- **UI/UX 细节打磨**：
  - 优化了预设卡片的样式，增加了 Logo 容器与更精致的分类标签。
  - 补齐了 Codex 和 Gemini 的详细提示（Tips）及其对应的多语言翻译。

## 改动文件清单

- `apps/desktop-tauri/src-tauri/src/commands/settings/ai_config.rs`
- `apps/desktop-web/public/assets/logos/` (New directory with images)
- `apps/desktop-web/src/features/settings/ai-providers/claude/ClaudeConfigModal.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/claude/ClaudeProviderWorkspace.scss`
- `apps/desktop-web/src/features/settings/ai-providers/light-agents/LightAgentConfigModal.tsx`
- `apps/desktop-web/src/features/settings/ai-providers/shared/ProviderAgentCard.tsx`
- `apps/desktop-web/src/shell/i18n/messages.ts`
- `crates/vb-ai-config/src/catalog.rs`
- `crates/vb-ai-config/src/service.rs`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 验证情况

- `npx tsc -b` (apps/desktop-web)：通过。
- **Logo 验证**：主页卡片与 Claude 预设列表均能正确加载图片资源。
- **i18n 验证**：切换语言后，所有 Agent 的功能描述、预设说明、向导步骤均能正确显示中文/英文。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-171；Agent 配置中心的视觉与文案国际化已闭环。
- **已完成与未完成**：
  - 已完成：图片资源归位、Logo 映射、永不过时 i18n 方案。
  - 未完成：其他小众供应商（如 DeepSeek、Kimi）的官方 Logo 收集（当前使用文字占位）。
- **下一步第一动作**：在设置页面手动切换“中文/English”，验证 Agent 卡片的副标题是否随之变动。
- **风险/阻塞与是否需要决策**：无。
- **验证命令与结果**：
  - `npx tsc -b` (apps/desktop-web)
