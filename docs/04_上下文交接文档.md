# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
4. 阅读 `docs/06_API与事件契约草案.md`。
5. 阅读本文件的 `Handover Snapshot` 与 `Next Agent Starter`。

## Handover Snapshot

- Date: 2026-02-09 03:12 CST
- Owner: AI
- Current Milestone: M1 MVP 内核
- Goal of Current Session: Git 双栏体验重整 + 超长历史分页加载 + 苹果风格收敛（T-109 增强）
- Target Workspace: `workspace_root=/mnt/c/project/vbCode`

### Completed

1. Git 页面布局重整：左侧第二列承载全部 Git 操作，右侧主区域承载 Graph/Log/Diff（信息结构清晰，避免混杂）。
2. 超长历史分页：`git.log` 新增 `skip + limit` 真分页能力，前端支持“加载更早提交 / 回到最新”。
3. Git Graph 与 Diff 继续使用成熟库：
   - `@gitgraph/react`：图谱绘制
   - `diff2html`：结构化 diff 渲染
4. 后端 Git 状态稳定性补丁：修复 unborn 分支场景分支名异常（`HEAD` / `No commits yet on main`）。
5. 设计与契约文档同步回写：`docs/03`、`docs/05`、`docs/06`、`docs/07`、`docs/04`。

### Changed Files

1. `apps/desktop-web/src/shell/layout/GitPane.tsx`
2. `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
3. `apps/desktop-web/src/shell/layout/shell-layout.css`
4. `apps/desktop-web/src/shell/i18n/messages.ts`
5. `apps/desktop-web/src/shell/integration/desktop-api.ts`
6. `apps/desktop-web/package.json`
7. `package-lock.json`
8. `crates/vb-git/src/lib.rs`
9. `apps/desktop-tauri/src-tauri/src/commands/git.rs`
10. `docs/03_项目开发进度跟踪.md`
11. `docs/04_上下文交接文档.md`
12. `docs/05_高质量功能设计_核心工作流.md`
13. `docs/06_API与事件契约草案.md`
14. `docs/07_依赖选型与精简清单.md`

### Decisions

1. 分页策略采用后端 `skip+limit`，避免前端一次拉满长历史导致卡顿与内存膨胀。
2. 布局采用“操作/信息分离”原则：左侧高频动作、右侧深度信息。
3. 视觉方向收敛为克制玻璃层次与清晰分区，保持苹果风格但减少噪声与拥挤。

### Pending Next Actions

1. 第一动作：补 Git 双栏交互 E2E（文件点击看 diff、分页加载、stage/unstage/commit/branch/stash）。
2. 若遇超大型仓库，再加“Graph 时间窗分页”与日志搜索。
3. 评估 `commands::git` 单测耗时，必要时拆分轻量测试目标。

### Risks / Blockers

1. `cargo test -p gtoffice-desktop-tauri --lib commands::git` 编译/链接耗时非常长，本轮未纳入通过集。
2. Graph 仍是基于 log 重建，对于极端复杂 merge 历史可视化精度仍有边缘差异。

### Validation

1. Command: `npm run typecheck`
   Result: 通过。
2. Command: `npm run build:web`
   Result: 通过。
3. Command: `cargo check --workspace`
   Result: 通过（含 `ashpd` future incompat warning，不影响当前）。
4. Command: `cargo test -p vb-git`
   Result: 通过（6 passed）。
5. Command: `cargo test -p gtoffice-desktop-tauri --lib commands::git`
   Result: 未执行通过（耗时过长，本轮中止）。

## Next Agent Starter

- Current Milestone: M1 MVP 内核（Git 协作体验收口）
- Session Goal: 补齐 Git 双栏 E2E 与大仓库分页性能数据
- First Action (must be executable): 在 `workspace_root=/mnt/c/project/vbCode` 执行 `rg -n "skip|loadOlderHistory|resetToLatestHistory|GitHistoryPane|GitOperationsPane" apps/desktop-web/src/shell/layout/GitPane.tsx apps/desktop-web/src/shell/integration/desktop-api.ts apps/desktop-tauri/src-tauri/src/commands/git.rs crates/vb-git/src/lib.rs`
- Success Criteria: 分页链路和双栏交互有完整验证记录，超长历史无明显卡顿
- Target Workspace (workspace_id or root path): `/mnt/c/project/vbCode`

### Completed in Last Session

1. 完成 `T-109` 增强：Git 双栏 + Graph + Diff + 真分页。
2. `git.log` 契约扩展并前后端联调完成。
3. `vb-git` 回归测试通过并修复 unborn 分支识别。

### Pending (ordered)

1. Git 双栏 E2E。
2. 超长历史性能压测。
3. 决定是否继续图谱时间窗分页。

### Files Touched

1. `apps/desktop-web/src/shell/layout/GitPane.tsx`
2. `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
3. `apps/desktop-web/src/shell/layout/shell-layout.css`
4. `apps/desktop-web/src/shell/i18n/messages.ts`
5. `apps/desktop-web/src/shell/integration/desktop-api.ts`
6. `apps/desktop-web/package.json`
7. `package-lock.json`
8. `crates/vb-git/src/lib.rs`
9. `apps/desktop-tauri/src-tauri/src/commands/git.rs`
10. `docs/03_项目开发进度跟踪.md`
11. `docs/04_上下文交接文档.md`
12. `docs/05_高质量功能设计_核心工作流.md`
13. `docs/06_API与事件契约草案.md`
14. `docs/07_依赖选型与精简清单.md`

### Validation Commands

1. `npm run typecheck` -> 通过
2. `npm run build:web` -> 通过
3. `cargo check --workspace` -> 通过
4. `cargo test -p vb-git` -> 通过（6 passed）
5. `cargo test -p gtoffice-desktop-tauri --lib commands::git` -> 未通过集（耗时过长）

### Risks / Decisions Needed

1. 是否在下一轮引入 Graph 时间窗策略（例如默认仅渲染最近 N 页）。
