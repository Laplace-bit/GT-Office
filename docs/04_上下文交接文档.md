# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-175
- 本轮目标：修复顶部栏窗口控件的跨平台布局/样式，并让 channel 入站提示不再挤压标题栏

## 已完成

- 已在 `TopControlBar` 内按平台重排窗口控件：Windows/Linux 保持 `minimize -> maximize/restore -> close` 且固定在右侧，macOS 改为左侧 `close -> minimize -> maximize`
- 已补齐标题栏剩余拖拽区 `.titlebar-drag-region` 的弹性布局，避免 Windows 自绘标题栏按钮被挤在中间而不是贴右
- 已重做窗口控件样式：Windows/Linux 使用更接近原生标题栏的拉伸按钮和关闭态；macOS traffic lights 保留彩色圆点并在 hover/focus 时显示 glyph
- 已将 `telegram-debug-toast` 从 `ShellRoot` 顶部普通流式节点移入 `shell-top-slot`，改为挂在标题栏下方的绝对浮层，避免提示窗把整个 shell 顶变形
- 已为入站提示浮层补齐玻璃层、关闭按钮、移动端宽度约束等样式
- 已同步更新：
  - `docs/03_项目开发进度跟踪.md`
  - `docs/04_上下文交接文档.md`

## 未完成

- 未执行真实桌面端手工联调，尚未分别在 Windows/macOS 原生壳层里逐项确认按钮位置与 hover 细节
- 本轮没有补自动化 UI 回归；当前验证以 TypeScript 编译与 Vite 生产构建为主

下一步第一动作：

- 在 Windows 上打开桌面端确认右上角最小化/最大化/关闭按钮位置与点击行为，再在 macOS 上确认左上角 traffic lights 顺序、hover 图标与双击标题栏最大化不冲突

## 改动文件清单

- `apps/desktop-web/src/shell/layout/TopControlBar.tsx`
- `apps/desktop-web/src/shell/layout/TopControlBar.scss`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `apps/desktop-web/src/shell/layout/ShellRoot.scss`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 风险 / 阻塞

- 当前平台判断依赖前端 `navigator.platform/userAgent`，若后续引入更细的 Linux/Windows window chrome 差异，可能还需要把平台信息改成由 Tauri 明确下发
- `telegram-debug-toast` 现在是标题栏下方浮层，不再参与布局；如果未来同一位置叠加更多顶部通知，需要补充 toast 堆叠/避让策略
- 尚未验证多语言长文案或超长消息内容下的浮层高度表现

## 验证命令与结果

- 已执行 `npm --workspace apps/desktop-web exec tsc -b`，通过
- 已执行 `npm --workspace apps/desktop-web exec vite build`，通过
- Vite 构建仍存在既有 chunk size warning，但未阻塞产物生成
- 未执行真实桌面端手工联调；原因：当前会话仅完成代码修复与构建验证

## Next Agent Starter

- 当前里程碑与本轮目标：M2 / T-175；本轮已收口顶栏窗口控件的跨平台布局和样式，并修复 Telegram 入站提示把标题栏顶变形的问题
- 已完成与未完成：已让 Windows/Linux 窗口按钮固定在右侧、macOS traffic lights 改回左侧系统顺序，并将入站提示改成标题栏下方浮层；未完成的是 Windows/macOS 真机手工回归和极端长文案视觉确认
- 改动文件清单：`apps/desktop-web/src/shell/layout/TopControlBar.tsx`、`apps/desktop-web/src/shell/layout/TopControlBar.scss`、`apps/desktop-web/src/shell/layout/ShellRoot.tsx`、`apps/desktop-web/src/shell/layout/ShellRoot.scss`、`docs/03_项目开发进度跟踪.md`、`docs/04_上下文交接文档.md`
- 风险/阻塞与是否需要决策：若 macOS 真机上仍觉得 glyph hover 太显眼，可再决定是否收窄到仅 hover 外层 group 时显示、或完全隐藏 glyph 只保留纯色圆点
- 验证命令与结果：`npm --workspace apps/desktop-web exec tsc -b` 通过；`npm --workspace apps/desktop-web exec vite build` 通过；未做真实桌面端手工联调
