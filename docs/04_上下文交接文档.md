# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
4. 阅读 `docs/06_API与事件契约草案.md`。
5. 阅读本文件的 `Handover Snapshot` 与 `Next Agent Starter`。

## Handover Snapshot

- Date: 2026-02-13 16:49 CST
- Owner: AI
- Current Milestone: M2 MVP 完整
- Goal of Current Session: 修复 Git 管理模块 UI 问题：变更文件列表与提交/分支/STASH 区块紧凑化 + diff 顶部“回到最新”入口改为 switch 语义（T-167）
- Target Workspace: `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `workspace_root=/mnt/c/project/vbCode`

### Completed

1. `GitPane.tsx`：将历史区顶部“回到最新”按钮替换为 `switch` 形态，使用 `role="switch"` + `aria-checked` 在“最新提交/文件差异”之间切换；未选中文件时开关禁用并提示原因。
2. `messages.ts`：新增 `git.history.view.*` 文案键；将 `git.history.backToLatest` 更正为“回到最新提交”/`Back To Latest Commits`。
3. `useGitWorkspaceController.ts`：将 Git 变更列表虚拟行高统一到 `ROW_HEIGHT = 30`，与 UI 样式保持一致。
4. `shell-layout.css`：修复 Git 变更虚拟列表行布局（`absolute + box-sizing:border-box`），收敛行间距与区块间距，解决“行高被挡住/间距过大”问题。
5. `git-pane.css`：同步收敛 Git 面板紧凑样式与新 switch 样式，避免不同样式注入顺序下表现不一致，并进一步压缩“提交/分支/STASH”区块的输入、按钮、列表项尺寸。
6. `shell-layout.css/git-pane.css`：跟进修复变更文件选中高亮缺失，增强 active 背景与左侧强调线，并确保 hover 不覆盖选中态。
7. 文档同步：更新 `docs/03`（T-167 任务+变更记录）与 `docs/05`（WF-03B、AC-033）。
8. 完成验证：`npm --workspace apps/desktop-web run build` 通过（仅 chunk size warning）。

### Changed Files

1. `apps/desktop-web/src/shell/layout/GitPane.tsx`
2. `apps/desktop-web/src/shell/i18n/messages.ts`
3. `apps/desktop-web/src/features/git/useGitWorkspaceController.ts`
4. `apps/desktop-web/src/shell/layout/shell-layout.css`
5. `apps/desktop-web/src/shell/layout/git-pane.css`
6. `docs/03_项目开发进度跟踪.md`
7. `docs/04_上下文交接文档.md`
8. `docs/05_高质量功能设计_核心工作流.md`

### Decisions

1. Git 历史区顶部入口不再使用“回到最新”语义，改为双态 switch（最新提交/文件差异）以匹配当前界面行为。
2. 变更文件虚拟列表使用绝对定位行渲染，且行高与 `ROW_HEIGHT` 保持一致，避免内容遮挡和视觉空隙。
3. 同时维护 `shell-layout.css` 与 `git-pane.css` 的紧凑样式，避免样式加载顺序差异导致回归。

### Pending Next Actions

1. 桌面端人工回归 `T-167`：在 Git 面板滚动 200+ 变更文件，确认行密度紧凑且状态/路径/操作按钮不遮挡。
2. 人工回归变更文件选中态：点击后高亮持续可见（含 hover 与滚动后回显），不会退化为普通悬停态。
3. 人工回归“提交/分支/STASH”三块：确认输入框、按钮与列表项压缩后仍无遮挡、无误触，键盘 Enter 提交链路正常。
4. 人工回归历史区 switch：未选中文件时禁用；选中文件后可在“最新提交/文件差异”间来回切换且状态稳定。
5. 观察窄宽度窗口下 switch 文案是否需要进一步缩写（若发生换行，再评估改为图标+短文案）。

### Risks / Blockers

1. 尚未完成真实桌面端人工回归（当前仅完成 Web 构建验证）。

### Validation

1. Command: `npm --workspace apps/desktop-web run build`
   Result: 通过（仅 chunk size warning）。

## Next Agent Starter

- Current Milestone: M2 MVP 完整
- Session Goal: 执行 `T-167` 桌面端人工回归，确认 Git 变更列表 + 提交/分支/STASH 区块紧凑化与历史/差异 switch 在真实窗口下无回退
- First Action (must be executable): 在 `/mnt/c/project/vbCode` 执行 `npm --workspace apps/desktop-tauri run dev`，进入 Git 面板后导入/制造较多变更文件并滚动观察行高与遮挡，再测试 switch 切换
- Success Criteria:
  1. Git 变更列表行间距较之前明显收敛，且每行状态/路径/操作按钮均完整可见。
  2. 提交/分支/STASH 三块高度更紧凑且不影响可点击区域与输入可读性。
  3. 变更列表选中态高亮清晰可见，hover 不覆盖 active 状态。
  4. 历史区 switch 未选中文件时禁用，选中文件后可稳定切换到“文件差异”并可切回“最新提交”。
  5. “回到最新提交”文案仅用于历史分页重置语义，不再误用于顶部视图切换入口。
  6. 如有追加修复，继续同步 `docs/03` 与 `docs/04`。
- Target Workspace (workspace_id or root path): `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `/mnt/c/project/vbCode`

### Completed in Last Session

1. Git 变更列表虚拟行渲染改为紧凑模型（行高 30 + 绝对定位 + 内容不遮挡）。
2. Git 历史区顶部入口从“回到最新”按钮重构为 switch（最新提交/文件差异）。
3. 提交/分支/STASH 三块区块完成紧凑化（输入框、按钮、列表项与间距收敛）。
4. 跟进修复变更文件选中态可视反馈（active 背景增强 + 左侧强调线 + active-hover 持续）。
5. 国际化文案补齐：新增 switch 文案并将 “Back To Latest” 更正为 “Back To Latest Commits”。
6. 构建验证通过：`npm --workspace apps/desktop-web run build`（仅 chunk size warning）。
7. 文档已同步：`docs/03`、`docs/04`、`docs/05`。

### Pending (ordered)

1. 桌面端验证 Git 大变更列表滚动时的行高、点击与 hover/active 行为。
2. 桌面端验证提交/分支/STASH 三块紧凑样式在不同缩放比下无控件遮挡。
3. 桌面端验证 switch 在不同窗口宽度与中英文下的可读性和禁用态提示。
4. 若出现文案拥挤，评估是否将 switch 文案缩写为更短标签。

### Validation Commands

1. `npm --workspace apps/desktop-web run build` -> 通过（仅 chunk size warning）。

### Risks / Decisions Needed

1. 若窄宽度场景下 switch 文案拥挤，需决定是保留文案还是改为纯图标+tooltip。
