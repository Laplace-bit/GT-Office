# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-186
- 本轮目标：修复 macOS 下全项目输入框与文本编辑器无法输入部分字符的问题。

## 已完成

- **定位根因**：
  - `apps/desktop-web/src/shell/layout/ShellRoot.tsx`：确认桌面端全局 `keydown` 捕获中，为了屏蔽 WebView 浏览器默认快捷键，错误地把裸键 `r`、`+`、`=`、`-`、`0` 也做了 `preventDefault()`，导致这些字符在输入框、textarea、CodeMirror 等场景全部无法录入。
- **完成修复**：
  - `apps/desktop-web/src/shell/layout/ShellRoot.tsx`：新增 `shouldPreventDesktopBrowserShortcut()`，将拦截条件收敛为仅在 `Cmd/Ctrl` 组合下屏蔽 `r/+/=/-/0` 这些 reload/zoom 级别快捷键；普通输入与 `Option` 参与的字符输入不再被全局吞掉。

## 改动文件清单

- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 验证情况

- `cd apps/desktop-web && npm run build`：通过。
- macOS 真实输入回归：**未验证**。本轮完成代码修复与前端构建验证，尚未在真实桌面端逐个输入框手工回归 `r/+/=/-/0` 等字符。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-186；已完成 macOS 文本输入字符吞键修复，下一步应做真实桌面端输入回归。
- **已完成与未完成**：
  - 已完成：定位 `ShellRoot` 全局键盘捕获误伤普通输入的根因；把桌面 WebView shortcut 拦截收敛为仅拦截真实 `Cmd/Ctrl` 组合；前端构建通过。
  - 未完成：尚未在 macOS 真机逐个验证普通 input、textarea、CodeMirror 编辑器、任务中心文本框等场景对 `r/+/=/-/0` 的实际输入恢复情况。
- **下一步第一动作**：在 macOS 桌面端依次验证工作区路径输入、任务中心文本框、文件搜索框、CodeMirror 文件编辑器，确认 `r`、`+`、`=`、`-`、`0` 与 `Cmd/Ctrl+R`、`Cmd/Ctrl+=` 等快捷键表现符合预期。
- **风险/阻塞与是否需要决策**：
  - 风险：当前修复针对已定位的全局 `keydown` 吞键问题；如果仍有个别特殊字符异常，需要继续排查 Tauri/WebView 或编辑器自身的组合输入链路。
  - 阻塞：无。
- **验证命令与结果**：
  - `cd apps/desktop-web && npm run build`：通过。
