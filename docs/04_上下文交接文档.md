# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
4. 阅读 `docs/06_API与事件契约草案.md`。
5. 阅读本文件的 `Handover Snapshot` 与 `Next Agent Starter`。

## Handover Snapshot

- Date: 2026-02-12 23:15 CST
- Owner: AI
- Current Milestone: M1 MVP 内核
- Goal of Current Session: 修复底栏分支下拉“双箭头”问题
- Target Workspace: `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `workspace_root=/mnt/c/project/vbCode`

### Completed

1. 定位到双箭头根因：`index.css` 中全局规则 `select:not([class*="custom"])` 会自动注入 SVG 下拉箭头，而分支控件同时有局部自定义箭头，导致重复显示。
2. 在 `StatusBar.tsx` 将分支下拉类名改为 `status-bar__branch-select custom-select`，避开全局自动箭头规则。
3. 在 `shell-layout.css` 对分支下拉增加兜底：`background-image: none !important`，并隐藏 `::-ms-expand`，保证只保留一个箭头。
4. 更新 `docs/03_项目开发进度跟踪.md`：新增双箭头修复记录。

### Changed Files

1. `apps/desktop-web/src/shell/layout/StatusBar.tsx`
2. `apps/desktop-web/src/shell/layout/shell-layout.css`
3. `docs/03_项目开发进度跟踪.md`
4. `docs/04_上下文交接文档.md`

### Decisions

1. 分支控件沿用局部自定义箭头，禁用全局 `select` 自动箭头注入。
2. 优先以“一个箭头、视觉确定性”为目标，增加局部兜底样式避免主题或平台差异回归。

### Pending Next Actions

1. 第一动作：在 Tauri 窗口实机确认分支下拉仅显示一个箭头（Windows/Linux/macOS）。
2. 第二动作：检查浅/深色主题下箭头对比度是否一致。

### Risks / Blockers

1. 本轮仅完成 Web build 验证，未做 Tauri 实机回归。

### Validation

1. Command: `npm --workspace apps/desktop-web run build`
   Result: 通过（仅 chunk size warning）。

## Next Agent Starter

- Current Milestone: M1 MVP 内核
- Session Goal: 做“分支下拉双箭头修复”的 Tauri 实机回归
- First Action (must be executable): 在 `workspace_root=/mnt/c/project/vbCode` 执行 `npm --workspace apps/desktop-tauri run dev`，观察底栏分支下拉在浅/深主题是否只显示单箭头。
- Success Criteria:
  1. 分支下拉仅保留一个向下箭头。
  2. 切换分支功能正常。
  3. 无新增重影或发虚。
  4. 底栏紧凑单行不受影响。
- Target Workspace (workspace_id or root path): `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `/mnt/c/project/vbCode`

### Completed in Last Session

1. 分支下拉双箭头问题已在代码层修复（类名隔离 + 样式兜底）。
2. 构建验证通过。
3. `docs/03` 与 `docs/04` 已同步更新。

### Pending (ordered)

1. Tauri 实机验证单箭头效果。
2. 视回归结果再微调箭头颜色与位置。

### Validation Commands

1. `npm --workspace apps/desktop-web run build` -> 通过（仅 chunk size warning）。

### Risks / Decisions Needed

1. 待确认：是否保留当前局部自定义箭头样式，还是完全跟随系统原生箭头风格。
