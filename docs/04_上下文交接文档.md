# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-175
- 本轮目标：把任务中心中的“外部通道”“派发历史”展示迁移到“通信通道”侧栏，并保持前端 feature 边界清晰

## 已完成

- 新增 `apps/desktop-web/src/features/channels/CommunicationChannelsPane.tsx` 与对应 SCSS，接管通信通道侧栏内容
- `ShellRoot` 的 `channels` 导航已改为渲染真实的通信通道面板，不再落回 `LeftBusinessPane` 占位
- 新面板已承接：
  - 外部通道运行状态与刷新入口
  - 已配置通道、运行摘要、最近外部事件
  - 派发历史与失败任务重发入口
- `TaskCenterPane` 已去除“外部通道”“派发历史”区块，收敛为任务目标选择、Markdown 编辑、发送状态提示
- 已补充 i18n 文案：
  - `taskCenter.external.configuredEmpty`
- 已同步更新：
  - `docs/03_项目开发进度跟踪.md`
  - `docs/04_上下文交接文档.md`

## 未完成

- 尚未做完整视觉回归，需确认 `tasks` 与 `channels` 之间切换时的高度、滚动和密度是否完全符合预期
- 尚未确认是否还需要为通信通道侧栏补充更多路由/绑定明细信息

下一步第一动作：

- 运行前端类型检查并手动查看 `tasks/channels` 两个侧栏在亮暗主题下的显示与折叠行为

## 改动文件清单

- `apps/desktop-web/src/features/channels/CommunicationChannelsPane.tsx`
- `apps/desktop-web/src/features/channels/CommunicationChannelsPane.scss`
- `apps/desktop-web/src/features/channels/index.ts`
- `apps/desktop-web/src/features/task-center/TaskCenterPane.tsx`
- `apps/desktop-web/src/features/task-center/TaskCenterPane.scss`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `apps/desktop-web/src/shell/i18n/messages.ts`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 风险 / 阻塞

- 当前通信通道面板复用了任务中心既有数据源，展示迁移完成，但如果后续要做更细粒度的通道管理，可能仍需再拆分更细的子组件
- 工作树可能存在并行改动，后续若继续改动 `ShellRoot` 需留意合并冲突

## 验证命令与结果

- 未验证；代码已完成，待执行前端类型检查与界面回归

## Next Agent Starter

- 当前里程碑与本轮目标：M2 / T-175；已将任务中心中的外部通道与派发历史展示迁移到通信通道侧栏
- 已完成与未完成：`channels` 导航已有真实 pane，`TaskCenterPane` 已收敛；仍待类型检查与实际 UI 回归
- 改动文件清单：`apps/desktop-web/src/features/channels/CommunicationChannelsPane.tsx`、`apps/desktop-web/src/features/channels/CommunicationChannelsPane.scss`、`apps/desktop-web/src/features/channels/index.ts`、`apps/desktop-web/src/features/task-center/TaskCenterPane.tsx`、`apps/desktop-web/src/features/task-center/TaskCenterPane.scss`、`apps/desktop-web/src/shell/layout/ShellRoot.tsx`、`apps/desktop-web/src/shell/i18n/messages.ts`、`docs/03_项目开发进度跟踪.md`、`docs/04_上下文交接文档.md`
- 风险/阻塞与是否需要决策：是否继续把通信通道侧栏细分为“状态卡 + 历史卡 + 路由绑定卡”尚未决策；当前实现已满足展示迁移
- 验证命令与结果：未验证；待执行前端类型检查与界面回归
