# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
4. 阅读 `docs/06_API与事件契约草案.md`。
5. 阅读本文件的 `Handover Snapshot` 与 `Next Agent Starter`。

## Handover Snapshot

- Date: 2026-02-13 15:58 CST
- Owner: AI
- Current Milestone: M2 MVP 完整
- Goal of Current Session: 修复 `StationCard` 小窗命令行渲染不稳定与任务中心命令“到达但未自动执行”问题（T-166）
- Target Workspace: `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `workspace_root=/mnt/c/project/vbCode`

### Completed

1. `StationCard` 增加基于卡片尺寸的 compact 布局阈值（宽/高过小时自动收敛间距与终端最小高度），缓解小窗命令行渲染抖动与不可见问题。
2. `shell-layout.css` 新增 `station-window-compact` 样式分支，统一下调 terminal host 最小高度，保证小尺寸卡片仍可稳定可见。
3. `StationXtermTerminal.submit` 改为 xterm 原生输入链路：`terminal.input('\r', true)`，不再依赖 DOM 键盘事件模拟。
4. `ShellRoot` 任务派发/重发提交顺序改为“命令写入成功 -> xterm submit”，并移除“回车字符 fallback 写入”路径；submit 失败明确标记 `XTERM_SUBMIT_FAILED`。
5. Tauri `task_dispatch_batch` 改为仅写入命令文本，不再由后端追加提交序列，避免与前端 xterm submit 双写竞态。
6. 同步文档：更新 `docs/05`（US-23 主流程）、`docs/06`（Task Center 契约语义）、`docs/03`（新增 T-166 任务与变更记录）。
7. 完成验证：`npm --workspace apps/desktop-web run build`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml` 通过。

### Changed Files

1. `apps/desktop-web/src/shell/layout/StationCard.tsx`
2. `apps/desktop-web/src/shell/layout/StationXtermTerminal.tsx`
3. `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
4. `apps/desktop-web/src/shell/layout/shell-layout.css`
5. `apps/desktop-tauri/src-tauri/src/commands/tasks.rs`
6. `docs/03_项目开发进度跟踪.md`
7. `docs/04_上下文交接文档.md`
8. `docs/05_高质量功能设计_核心工作流.md`
9. `docs/06_API与事件契约草案.md`

### Decisions

1. 自动提交统一从 xterm 输入层触发，不在任务命令文本或后端派发链路中拼接/追加回车符。
2. 派发链路以“命令先落盘并写入会话，再触发 submit”为唯一顺序约束，避免并发竞态。
3. 小窗渲染稳定性优先采用 StationCard 自适应布局收敛，而不是继续提高终端重试频率。

### Pending Next Actions

1. 桌面端人工回归 `T-166`：在小尺寸工位（固定布局 `2*2` 或 `3*2`）下启动 `codex/claude`，确认终端输出稳定可见。
2. 桌面端人工回归任务中心“批量派发 + 失败重发”，确认命令写入后自动执行且无重复提交。
3. 观察是否出现 `XTERM_SUBMIT_FAILED`；若出现，评估是否需要“等待 sink 就绪后重试次数”配置化。

### Risks / Blockers

1. 仍缺真实桌面端人工回归（当前仅完成构建与编译验证）。

### Validation

1. Command: `npm --workspace apps/desktop-web run build`
   Result: 通过（仅 chunk size warning）。
2. Command: `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`
   Result: 通过（含 `ashpd v0.8.1` future incompatibility warning）。

## Next Agent Starter

- Current Milestone: M2 MVP 完整
- Session Goal: 执行 `T-166` 桌面端人工回归，确认“小窗稳定渲染 + xterm 自动提交”在线上交互场景无回退
- First Action (must be executable): 在 `/mnt/c/project/vbCode` 执行 `npm --workspace apps/desktop-tauri run dev`，设置 `2*2` 布局后在最小可用窗口尺寸下启动工位终端并观察输出，再从任务中心派发任务验证自动执行
- Success Criteria:
  1. 小尺寸工位下终端输出连续可见，不会出现“光标移动但文本不渲染”。
  2. 任务中心派发与重发均可自动执行，且无重复回车副作用。
  3. 不通过命令文本拼接回车符即可稳定提交（submit 仅由 xterm 触发）。
  4. 如有追加修复，继续同步 `docs/03` 与 `docs/04`。
- Target Workspace (workspace_id or root path): `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `/mnt/c/project/vbCode`

### Completed in Last Session

1. `StationCard` 已加入 compact 自适应，缓解小窗终端可见区域抖动。
2. 自动提交链路已改为 xterm 原生 submit（`terminal.input('\r', true)`）并且强制“先写命令后提交”。
3. Tauri `task_dispatch_batch` 已收敛为仅写命令文本，避免后端/前端重复提交。
4. 构建验证通过：`npm --workspace apps/desktop-web run build`、`cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml`。
5. 文档已同步：`docs/03`、`docs/04`、`docs/05`、`docs/06`。

### Pending (ordered)

1. 桌面端验证小窗终端稳定渲染（含窗口缩放和工位切换）。
2. 桌面端验证任务派发/重发自动提交（包括 Codex、Claude、普通 shell）。
3. 若仍存在 submit 丢失，评估是否需要新增 submit 失败遥测与更长 sink 就绪重试窗口。

### Validation Commands

1. `npm --workspace apps/desktop-web run build` -> 通过（仅 chunk size warning）。
2. `cargo check --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml` -> 通过（含 `ashpd v0.8.1` future incompatibility warning）。

### Risks / Decisions Needed

1. 若少量机型仍出现 `XTERM_SUBMIT_FAILED`，需决定是否允许“xterm submit 失败后延迟重试策略”可配置化。
