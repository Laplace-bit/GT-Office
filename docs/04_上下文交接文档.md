# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读本文件（`docs/04_上下文交接文档.md`）。
4. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
5. 阅读 `docs/06_API与事件契约草案.md`。
6. 阅读 `AGENTS.md`。

## Handover Snapshot

- Date: 2026-03-04 CST
- Owner: AI
- Current Milestone: M2 MVP 完整
- Goal of Current Session: 标准化 Rust 测试目录结构（`T-176`）
- Target Workspace: `workspace_root=/mnt/c/personal/vbCode`

### Completed

1. 测试文件架构翻新：
   - 彻底移除了位于 `src/` 下带有内部模块特权的 `*_tests.rs`，并将它们移动到了正统的 Cargo 集成测试目录结构 `tests/` 中。
   - 包括了 `vb-workspace`, `vb-terminal`, `vb-task`, `vb-daemon`, `vb-settings`, `vb-git` 六大主体模块架构改造。
2. 模块边界暴露适配：
   - 为保证测试能在脱离 `crate` 内部作用域后仍可运行，暴漏了部分此前为私有且必要的 `pub(crate)` 及 `fn` 级别的模块 API 供集成测试使用。如 `vb-settings` 里的 `default_user_settings_path` 以及 `vb-task` 里的 `now_ms`，还有 `vb-daemon` 里的 `search` 测试接口等。
3. 清理测试声明：
   - 全局清除了对应的 `#[test] mod tests` 片段。
4. 验证通过：
   - `cargo test --workspace` 编译并验证全套工作区无异常。

### Changed Files

1. `crates/vb-workspace/tests/lib_tests.rs` (NEW / MODIFIED from `src`)
2. `crates/vb-workspace/src/lib.rs` (MODIFIED)
3. `crates/vb-terminal/tests/lib_tests.rs` (NEW)
4. `crates/vb-terminal/src/lib.rs` (MODIFIED)
5. `crates/vb-task/tests/lib_tests.rs` (NEW)
6. `crates/vb-task/src/lib.rs` (MODIFIED)
7. `crates/vb-settings/tests/lib_tests.rs` (NEW)
8. `crates/vb-settings/src/lib.rs` (MODIFIED)
9. `crates/vb-git/tests/lib_tests.rs` (NEW)
10. `crates/vb-git/src/lib.rs` (MODIFIED)
11. `crates/vb-daemon/tests/ring_buffer_tests.rs` (NEW)
12. `crates/vb-daemon/src/util/ring_buffer.rs` (MODIFIED)
13. `crates/vb-daemon/tests/codec_tests.rs` (NEW)
14. `crates/vb-daemon/src/protocol/codec.rs` (MODIFIED)
15. `crates/vb-daemon/tests/fileio_tests.rs` (NEW)
16. `crates/vb-daemon/src/fileio/service.rs` (MODIFIED)
17. `crates/vb-daemon/tests/search_tests.rs` (NEW)
18. `crates/vb-daemon/src/search/mod.rs` & `service.rs` (MODIFIED)
19. `docs/03_项目开发进度跟踪.md`

### Decisions

1. The test boundaries align strictly with Rust's integration testing (`tests/` directory) where files test solely against the crate's exported API, reducing internal coupling and adhering to project strict guidelines.

### Pending Next Actions

1. 继续解决尚未完成的 UI 层重构或 Agent 对标与 MCP 验证工作 (参考 `T-175` 与 `T-172`)。

### Risks / Blockers

None.

### Validation

1. `cargo test --workspace` 已全部执行完毕并校验通过。

## Next Agent Starter

- Current Milestone: M2 MVP 完整
- Session Goal: 延续历史开发，选择推进 SCSS 样式工程化或是修复 Agent/MCP 相关基础设施。
- First Action (must be executable): 阅读 `docs/03_项目开发进度跟踪.md` 确认下一个未闭环任务并开始认领。
- Success Criteria: 无报错与回归问题。
- Target Workspace (workspace_id or root path): `/mnt/c/personal/vbCode`
