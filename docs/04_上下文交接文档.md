# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-175 样式系统工程化重构中的 shell 文件工作区视觉重塑
- 本轮目标：把文件树、编辑器、文件搜索、文件预览从集中式 legacy SCSS 中拆出，并升级为更高级、偏 Apple 风格、明暗主题都成立的窗口器件语言

## 已完成

- 使用 `ui-ux-pro-max` 收敛设计方向：选择克制的 liquid glass / dimensional layering，而不是廉价霓虹或过度动效
- 为 `FileTreePane`、`FileEditorPane`、`FileSearchModal`、`FileContentPane` 补充更稳定的 header / chrome / stage / status 结构钩子
- 新增组件独立样式文件：
  - `apps/desktop-web/src/shell/layout/FileTreePane.scss`
  - `apps/desktop-web/src/shell/layout/FileEditorPane.scss`
  - `apps/desktop-web/src/shell/layout/FileSearchModal.scss`
  - `apps/desktop-web/src/shell/layout/FileContentPane.scss`
- 从 `apps/desktop-web/src/styles/features/shell/_layout.scss` 移除上述组件相关旧样式，保留 shell 级公共布局
- 修复 `_layout.scss` 中 `station-grid` 缺失闭合花括号导致的 Sass 构建错误
- 根据二轮反馈继续去装饰化：删除 `FileTreePane` 顶部标题文案、搜索/刷新改纯图标、统一 row hover/selected 作用域；删除编辑器顶部 traffic lights 与 `Editor Deck` 文案；收窄 `FileContentPane` / `FileSearchModal` 的卡片层级与背景偏差
- 恢复 Windows 原生顶栏稳定方案：把整个 `TopControlBar` 恢复为标题栏拖拽区，双击最大化挂回 `header`，并过滤 `button/input/textarea/select/a/[role='button']/[contenteditable='true']` 等交互元素，避免误触
- 修复 `_layout.scss` 中 shell 通用滚动条选择器被截断的问题，恢复 Sass 构建通过
- 修正 `FileEditorPane.tsx` 的本轮新增 lint 问题；全量 lint 仍被仓库其他既有文件阻塞
- 更新 `docs/03_项目开发进度跟踪.md`

## 未完成

- 未做浏览器级视觉回归截图核对
- 未继续处理 T-175 的其他 legacy foundations / shell 组件拆分
- 全量 `eslint` 仍失败，阻塞项来自仓库既有文件：`GitPane.tsx`、`StationCard.tsx`、`StationManageModal.tsx`

下一步第一动作：
- 在 Windows 原生顶栏模式下实机确认：整个顶部非交互区域拖拽/双击最大化正常，按钮与输入控件不会误触标题栏行为；随后继续做 `files` 导航亮/暗主题走查

## 改动文件清单

- `apps/desktop-web/src/shell/layout/FileTreePane.tsx`
- `apps/desktop-web/src/shell/layout/FileTreePane.scss`
- `apps/desktop-web/src/shell/layout/FileEditorPane.tsx`
- `apps/desktop-web/src/shell/layout/FileEditorPane.scss`
- `apps/desktop-web/src/shell/layout/FileSearchModal.tsx`
- `apps/desktop-web/src/shell/layout/FileSearchModal.scss`
- `apps/desktop-web/src/shell/layout/FileContentPane.tsx`
- `apps/desktop-web/src/shell/layout/FileContentPane.scss`
- `apps/desktop-web/src/styles/features/shell/_layout.scss`
- `apps/desktop-web/src/shell/layout/TopControlBar.tsx`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 风险 / 阻塞

- 当前仓库存在大量与本任务无关的未提交改动；后续 Agent 合并时不要误回滚
- 视觉改造主要通过 SCSS 与轻量结构钩子完成，尚未做浏览器截图比对；若小尺寸窗口下 header / chip / tab 挤压，需要继续微调
- Windows 原生标题栏行为依赖 Tauri + WebView 命中规则；当前已回到“整个 header 为拖拽区 + 交互元素过滤”的稳定分支，仍建议实机确认 Win10/Win11 行为一致
- 全量 lint 失败并非本轮新引入，但会影响“lint 全绿”类交付要求

## 验证命令与结果

- `npm run build:web`
  结果：通过；本轮包含顶栏拖拽命中收窄修复；Vite 仍提示主包 chunk 超过 500 kB，为既有告警，非本轮新增阻塞
- `npm --workspace apps/desktop-web run lint`
  结果：未通过；当前阻塞为既有文件问题：`GitPane.tsx`、`StationCard.tsx`、`StationManageModal.tsx`，另有若干 warnings
- `npm run typecheck`
  结果：未执行；`apps/desktop-web` workspace 未提供 `typecheck` script

## Next Agent Starter

- 当前里程碑与本轮目标：M2 / T-175，已完成 shell 文件工作区视觉拆分，并把 Windows 原生顶栏恢复到稳定拖拽/双击实现
- 已完成与未完成：组件 SCSS、文件工作区收敛、CodeMirror 搜索面板、`TopControlBar` 整体 header 拖拽区与交互元素过滤双击切换已落地，`build:web` 通过；下一步先实机确认 Win10/Win11 顶栏行为，再做浏览器级亮/暗主题人工走查
- 改动文件清单：`FileTreePane.tsx/.scss`、`FileEditorPane.tsx/.scss`、`FileSearchModal.tsx/.scss`、`FileContentPane.tsx/.scss`、`CodeMirrorEditor.tsx/.scss`、`TopControlBar.tsx`、`_layout.scss`、`docs/03`、`docs/04`
- 风险/阻塞与是否需要决策：暂无产品决策阻塞；注意仓库脏工作树；Windows 原生标题栏交互需实机确认；全量 lint 仍被既有文件错误阻塞
- 验证命令与结果：`npm run build:web` 通过；`npm --workspace apps/desktop-web run lint` 未通过，失败点为仓库既有问题；未做截图回归
