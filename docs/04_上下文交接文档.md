# GT Office 上下文交接文档

## 最近交接快照

- 里程碑：M2 MVP 完整
- 任务：T-195
- 本轮目标：补强 Git 管理界面文件变更列表的可读性，列表中直接展示文件名，hover 时展示完整路径。
- 本轮目标：补强 Git 管理界面可用性，解决文件列表可读性、全展开遮挡，以及分支管理模块层级混乱的问题。

## 已完成

- Git 变更文件列表 UI：
  - `apps/desktop-web/src/features/git/GitPane.tsx` 中的 `GitFileRow` 现在只展示 `basename`，减少长路径在窄列表里的噪音。
  - 文件行按钮新增 `title` 与 `aria-label` 为完整相对路径，hover 时可直接查看原路径信息。
- Git 操作区滚动修复：
  - `apps/desktop-web/src/styles/features/git/_pane.scss` 中的 `git-pane__content` 改为纵向滚动容器。
  - `git-section--expanded` 不再默认 `flex: 1` 抢占剩余空间，避免多个 section 同时展开时把下部内容挤出可视区域却又无法滚动访问。
- Git 分支管理区重构：
  - `apps/desktop-web/src/features/git/GitPane.tsx` 将分支管理从两条拥挤表单行改为“当前分支概览 + 两张分组卡片”的布局。
  - `apps/desktop-web/src/styles/features/git/_pane.scss` 新增分支概览、卡片、字段标签与响应式堆叠样式。
  - `apps/desktop-web/src/shell/i18n/messages.ts` 补充分支区可见标签文案，避免依赖 placeholder 传达语义。
- 文档：
  - 已更新 `docs/03` 与本交接快照，记录本次 T-195 的 Git 列表可读性优化。

## 改动文件清单

- `apps/desktop-web/src/features/git/GitPane.tsx`
- `apps/desktop-web/src/styles/features/git/_pane.scss`
- `apps/desktop-web/src/shell/i18n/messages.ts`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`

## 验证情况

- `cd apps/desktop-web && npm run build`：通过
- 未执行人工 UI 回归；建议下一任在真实仓库中验证长路径、多层目录与同名文件场景下的可读性、Changes/Commit/Branches/Stash 同时展开时的滚动体验，以及分支区在窄宽度下的按钮排布。

## Next Agent Starter

- **当前里程碑与本轮目标**：M2 / T-195；本轮已优化 Git 变更列表、操作区滚动和分支管理布局，下一步是做真实 UI 回归。
- **已完成与未完成**：
  - 已完成：Git 变更文件行直接展示文件名，hover 时展示完整路径；Git 操作区父容器支持纵向滚动；分支管理区重组为当前分支概览与分组卡片，并补齐可见 label；进度与交接文档已同步。
  - 未完成：1. 未做人工 UI 回归；2. 同目录/跨目录同名文件在当前列表中仍仅靠 tooltip 区分；3. 分支区尚未增加更强的二次确认/禁用说明文案。
  - 下一步第一动作：在真实仓库中验证分支区的切换、删除、创建三条路径，重点观察窄面板下的卡片堆叠与按钮可达性。
- **风险/阻塞与是否需要决策**：
  - 同名文件较多时，仅展示 basename 可能降低即时辨识度；若用户反馈不足，下一步应考虑在副文案中补父目录，而不是回退为完整路径主展示。
  - 父层滚动与变更文件列表内部滚动已并存；若后续出现滚轮焦点体验问题，需要进一步调整 `overscroll-behavior` 或 section 高度策略。
  - 分支删除当前仅靠 disabled 态防误删；如果后续用户仍觉得不够明确，可以补“当前分支不可删除”的 inline hint，而不是增加更重的弹窗干扰。
- **验证命令与结果**：
  - `cd apps/desktop-web && npm run build`：通过
