# vbCode 上下文交接文档

## 当前里程碑与本轮目标

- 里程碑：M2 MVP 完整
- 任务：T-175
- 本轮目标：把“角色管理”侧栏切到真实数据库数据，并把删除入口收敛到编辑弹窗

## 已完成

- 角色管理侧栏列表按钮已从“删除”改为“编辑”，点击后复用 `StationManageModal`
- `StationManageModal` 已支持新增 / 编辑双模式；仅在编辑态显示删除按钮
- 前端 `ShellRoot` 不再只依赖 `createDefaultStations()`；Tauri 环境下会按当前 `workspaceId` 真实拉取 `agentRoleList + agentList`
- 新增 `mapAgentProfileToStation()`，把 SQLite `agents + agent_roles` 映射回工作站 UI 所需的 `AgentStation`
- Rust `agents` 表已补 `tool / workdir / custom_workdir` 字段，并在 `ensure_schema()` 中兼容旧库自动补列
- Tauri 已补 `agent_update`、`agent_delete`，`agent_create` 也已改为持久化 `tool/workdir/customWorkdir`
- 前端 `desktopApi` 已同步新增 `agentUpdate`、`agentDelete` 及相关类型
- 已同步更新：
  - `docs/03_项目开发进度跟踪.md`
  - `docs/04_上下文交接文档.md`
  - `docs/05_高质量功能设计_核心工作流.md`
  - `docs/06_API与事件契约草案.md`

## 未完成

- 角色管理当前仍沿用既有 4 类 `StationRole` 映射到 UI；如果后续要开放数据库中的任意自定义 `role_key` 直接进入中央画布，还需要扩展前端 role 类型与画布布局语义

下一步第一动作：

- 在桌面端手动回归角色新增、编辑、删除各一次，确认工作目录/工具字段在重启后仍能从数据库正确回填

## 改动文件清单

- `apps/desktop-tauri/src-tauri/src/commands/agent.rs`
- `apps/desktop-tauri/src-tauri/src/lib.rs`
- `apps/desktop-web/src/features/workspace-hub/station-model.ts`
- `apps/desktop-web/src/features/workspace-hub/StationManageModal.tsx`
- `apps/desktop-web/src/features/workspace-hub/StationManageModal.scss`
- `apps/desktop-web/src/features/workspace/StationOverviewPane.tsx`
- `apps/desktop-web/src/shell/integration/desktop-api.ts`
- `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
- `crates/vb-agent/src/models.rs`
- `crates/vb-agent/src/repository.rs`
- `crates/vb-storage/src/agent_repository.rs`
- `docs/03_项目开发进度跟踪.md`
- `docs/04_上下文交接文档.md`
- `docs/05_高质量功能设计_核心工作流.md`
- `docs/06_API与事件契约草案.md`

## 风险 / 阻塞

- 当前删除仍是物理删除 `agents` 记录；若后续需要审计恢复能力，建议改为软删除或状态机
- 旧数据库会自动补列，但不会回填历史 `tool/workdir`；历史记录会按默认 `codex cli` 和派生目录显示

## 验证命令与结果

- 已执行 `cd apps/desktop-web && npm run build`，通过
- 已执行 `cargo check --workspace`，通过
- 前端打包结果：`dist/assets/index-C31Gvt97.js` 约 `1199.32 kB`，仍有 Vite 默认的大 chunk warning，但不阻塞构建

## Next Agent Starter

- 当前里程碑与本轮目标：M2 / T-175；本轮已把角色管理侧栏切到 SQLite 真数据，并把删除入口收敛到编辑弹窗
- 已完成与未完成：侧栏列表改为编辑按钮，弹窗复用新增/编辑模式，编辑态支持删除；`agents` 表已持久化 `tool/workdir/custom_workdir`，前端会按 `agentRoleList + agentList` 装载真实数据。当前未覆盖的是“任意自定义 role_key 直接进入中央画布”的扩展场景
- 改动文件清单：`apps/desktop-tauri/src-tauri/src/commands/agent.rs`、`apps/desktop-tauri/src-tauri/src/lib.rs`、`apps/desktop-web/src/features/workspace-hub/station-model.ts`、`apps/desktop-web/src/features/workspace-hub/StationManageModal.tsx`、`apps/desktop-web/src/features/workspace-hub/StationManageModal.scss`、`apps/desktop-web/src/features/workspace/StationOverviewPane.tsx`、`apps/desktop-web/src/shell/integration/desktop-api.ts`、`apps/desktop-web/src/shell/layout/ShellRoot.tsx`、`crates/vb-agent/src/models.rs`、`crates/vb-agent/src/repository.rs`、`crates/vb-storage/src/agent_repository.rs`、`docs/03_项目开发进度跟踪.md`、`docs/04_上下文交接文档.md`、`docs/05_高质量功能设计_核心工作流.md`、`docs/06_API与事件契约草案.md`
- 风险/阻塞与是否需要决策：若产品希望角色删除可恢复，需要决定是否改成软删除；若要开放更多自定义角色类型进入中央画布，需要先扩展前端 `StationRole` 与部门映射
- 验证命令与结果：已执行 `cd apps/desktop-web && npm run build`、`cargo check --workspace`，均通过；前端仍有大 chunk warning
