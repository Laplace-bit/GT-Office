# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读本文件（`docs/04_上下文交接文档.md`）。
4. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
5. 阅读 `docs/06_API与事件契约草案.md`。
6. 阅读 `AGENTS.md`。

## Handover Snapshot

- Date: 2026-03-03 CST
- Owner: AI
- Current Milestone: M2 MVP 完整
- Goal of Current Session: 修复任务中心容器滚动与侧栏宽度变化下的自适应布局（`T-175` 增量）
- Target Workspace: `workspace_root=/mnt/c/personal/vbCode`

### Completed

1. SCSS 工程骨架落地：
   - `apps/desktop-web/src/styles/index.scss` 成为统一样式入口。
   - 建立 `styles/tokens`、`styles/foundations`、`styles/features/{shell,git}`、`styles/utilities` 分层目录。
2. 样式文件迁移：
   - `src/index.css` -> `src/styles/foundations/_legacy-foundations.scss`。
   - `shell-layout.css` -> `src/styles/features/shell/_layout.scss`。
   - `git-pane.css` -> `src/styles/features/git/_pane.scss`。
   - `diff-viewer.css` -> `src/styles/features/git/_diff-viewer.scss`。
3. 组件导入收敛：
   - `main.tsx` 改为导入 `./styles/index.scss`。
   - `ShellRoot.tsx`、`GitPane.tsx` 移除局部 `.css` 直接导入。
4. 兼容令牌补齐：
   - 新增 `styles/tokens/_theme-compat.scss`，补齐历史缺失 `--vb-*`（如 `--vb-primary`、`--vb-bg-secondary`、`--vb-surface-elevated` 等）。
   - 校验结果：样式代码中不再存在未定义 `--vb-*` 变量。
5. 单一来源收敛（首批）：
   - 从 shell 样式中移除重复 `.git-*` 大段规则，统一由 `features/git` 承担主定义。
6. 其他样式链一致性：
   - `apps/desktop-tauri/src/styles.css` -> `styles.scss`，并更新 `apps/desktop-tauri/index.html` 引用。
7. 依赖治理与文档同步：
   - `apps/desktop-web` 新增 devDependency：`sass`。
   - 已更新 `docs/03`、`docs/05`、`docs/07`。
8. Git 管理模块图标语义修复（增量）：
   - 将“丢弃变更（`git.action.discard`）”按钮图标从 `trash` 改为 `x-mark`。
   - 保留“删除分支（`git.action.deleteBranch`）”的 `trash`，维持删除类操作一致语义。
   - 构建验证通过：`npm --workspace apps/desktop-web run build`。
9. Git 管理模块图标语义修复（增量二）：
   - 新增 `undo` 撤销箭头图标并将“丢弃变更（`git.action.discard`）”按钮从 `x-mark` 切换为 `undo`。
   - 构建验证通过：`npm --workspace apps/desktop-web run build`。
10. Git 文件行操作约束修复（增量三）：
   - 已暂存文件：仅展示“取消暂存（`git.action.unstage`）”，图标为 `undo`。
   - 未暂存文件：展示“暂存（`git.action.stage`） + 丢弃（`git.action.discard`）”，其中丢弃图标为 `trash`。
   - 构建验证通过：`npm --workspace apps/desktop-web run build`。
11. 任务中心滚动与自适应修复（增量四）：
   - `apps/desktop-web/src/styles/features/shell/_layout.scss` 中将 `.task-center-pane` 调整为纵向 `flex` 容器，开启 `overflow-y: auto` + `overflow-x: hidden`，并为子节点补齐 `min-width: 0`，确保最外层容器可纵向滚动。
   - 对 `.task-center-target-actions`、`.task-center-external-header`、`.task-center-external-controls` 增加 `flex-wrap`，并为任务历史文本增加 `overflow-wrap: anywhere`，保证侧栏宽度变化时内容自适应不溢出。
   - 验证通过：`npm run typecheck`。
12. 任务中心外层滚动二次收敛（增量五）：
   - 为 `.task-center-pane` 增加 `height: 100%` 与 `max-height: 100%`，确保其在 `shell-left-pane` 内形成稳定滚动视口。
   - 将任务历史区改为外层统一滚动：`.task-center-history` 与 `.task-center-history ul` 改为 `overflow: visible`，取消内部双滚动竞争。
   - 将任务中心内部列表/弹层（历史、外部事件、目标列表、mention）`overscroll-behavior` 从阻断链路改为 `auto`，恢复滚轮向外层容器的滚动传递。
   - 验证通过：`npm run typecheck`。
13. 任务中心滚动宿主重构（增量六）：
   - `ShellRoot.tsx` 在 tasks 分支新增 `.task-center-scroll-host` 包裹 `TaskCenterPane`，将滚动高度约束从内容面板迁移到稳定宿主层。
   - `.task-center-scroll-host` 使用 `grid-row: 1 / -1`、`height: 100%`、`min-height: 0`、`overflow: hidden`、`display: flex`，确保在左侧 grid 容器中稳定占满可用高度。
   - `.task-center-pane` 移除 `grid-row/height/max-height` 跨上下文耦合，改为 `flex: 1 1 auto` 挂在宿主内滚动，解决“文本框拉高后历史被顶出且不可滚动”。
   - 验证通过：`npm run typecheck`。
14. 派发历史高度策略修复（增量七）：
   - `task-center-history` 改为明确双行栅格：`grid-template-rows: auto minmax(132px, clamp(180px, 28vh, 320px))`，确保标题固定、列表区高度可预期。
   - `task-center-history ul` 恢复 `overflow: auto`，保证历史条目在固定可视高度内滚动，不再出现“高度不正确”导致的塌陷或无限撑高。
   - 验证通过：`npm run typecheck`。
15. 任务中心 UI 重叠收敛（增量八）：
   - `task-center-editor` 增加 `overflow: hidden`，避免编辑区内部元素越界后覆盖后续模块。
   - 任务描述 textarea 新增 `max-height: clamp(220px, 34vh, 360px)` 与 `overflow: auto`，将超高内容收敛为框内滚动，避免继续挤压导致派发历史区与其它模块重叠。
   - 验证通过：`npm run typecheck`。
16. 任务中心重叠硬约束修复（增量九）：
   - `task-center-scroll-host` 从 `grid-row: 1 / -1` 调整为 `grid-row: 3`，对齐左侧面板 `auto auto 1fr` 的主内容行，避免跨行导致的高度计算异常。
   - 滚动职责收敛到宿主层：`task-center-scroll-host` 负责 `overflow-y: auto`；`task-center-pane` 改为 `overflow: visible`，移除双滚动竞争。
   - 任务描述 textarea 改为固定高度 `clamp(182px, 28vh, 320px)`、`resize: none`、`overflow: auto`，禁止继续拖拽破坏布局，确保不再与“外部通道/派发历史”重叠。
   - 验证通过：`npm run typecheck`。
17. 任务中心小高度响应式修复（增量十）：
   - `ShellRoot.tsx` 在 tasks 场景为左侧容器追加 `is-task-center` 类；`shell-left-pane.is-task-center` 使用单行网格 `minmax(0,1fr)`，避免低高度窗口下三行 grid 约束导致的重叠。
   - 任务中心区块高度改为 `svh` 维度自适应：描述框、历史列表区、外部事件区均按窗口高度缩放。
   - 新增 `@media (max-height: 860px)` 与 `@media (max-height: 720px)` 分级收敛规则，小高度下自动减小间距/文本区高度并隐藏次级说明文案，优先保证主要交互区不重叠。
   - 验证通过：`npm run typecheck`。
18. 任务中心无 px 单位改造（增量十一）：
   - 按用户约束将任务中心核心样式段（`task-center-pane` 至 `task-center-history-actions`）中的 `px` 全量替换为 `rem/svh/%`，包括字体、内外边距、圆角、边框厚度、列表高度与断点阈值。
   - 滚动/高度策略统一为 `svh + clamp/minmax`，并将任务中心相关滚动条尺寸单位同步改为 `rem`，避免绝对像素在小高度窗口触发堆叠。
   - 验证命令：`sed -n '640,1430p' apps/desktop-web/src/styles/features/shell/_layout.scss | rg -n "px"`（无输出）、`npm run typecheck`（通过）。
19. 任务中心 Flex 主布局重构（增量十二）：
   - 按用户建议将任务中心主流程从“高度栅格分配”改为“Flex 纵向流”：`task-center-scroll-host` 与 `task-center-pane` 均采用 `display:flex; flex-direction:column`，滚动统一留在宿主层。
   - 结构约束：`header/target/editor/send/external` 固定 `flex:0 0 auto`，`task-center-history` 改为 `flex:1 1 auto`，其 `ul` 再次 `flex:1 1 auto + overflow:auto`；历史区不再使用 `grid-template-rows` 固定分配。
   - 目标：在低窗口高度下优先保证主流程纵向排列，超出部分走宿主滚动，不再出现区块重叠。
   - 验证命令：`sed -n '640,1430p' ... | rg -n "px"`（无输出）、`npm run typecheck`（通过）。

### Changed Files

1. `apps/desktop-web/src/main.tsx`
2. `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
3. `apps/desktop-web/src/shell/layout/GitPane.tsx`
4. `apps/desktop-web/src/styles/index.scss`
5. `apps/desktop-web/src/styles/tokens/_theme-compat.scss`
6. `apps/desktop-web/src/styles/utilities/_helpers.scss`
7. `apps/desktop-web/src/styles/foundations/_legacy-foundations.scss`
8. `apps/desktop-web/src/styles/features/shell/_layout.scss`
9. `apps/desktop-web/src/styles/features/git/_pane.scss`
10. `apps/desktop-web/src/styles/features/git/_diff-viewer.scss`
11. `apps/desktop-tauri/src/styles.scss`
12. `apps/desktop-tauri/index.html`
13. `apps/desktop-web/package.json`
14. `package-lock.json`
15. `docs/03_项目开发进度跟踪.md`
16. `docs/05_高质量功能设计_核心工作流.md`
17. `docs/07_依赖选型与精简清单.md`
18. `docs/04_上下文交接文档.md`
19. `apps/desktop-web/src/shell/ui/icons.tsx`

### Decisions

1. 采用 SCSS 分层架构（非 Tailwind-first），并保留 Tailwind 作为辅助能力。
2. 视觉策略为“等价迁移优先”，本轮不做大规模视觉重设计。
3. 迁移方式为分阶段增量；允许先保留 `_legacy-foundations.scss`，后续继续按 feature 细拆。
4. 通过 token 兼容层消除历史变量漂移，避免迁移期 UI 断样。

### Pending Next Actions

1. 第一执行动作：继续拆分 `apps/desktop-web/src/styles/foundations/_legacy-foundations.scss`，优先拆出 `reset/base/inputs/buttons/scrollbar/motion` 五个模块并逐步删除遗留块。
2. 将 `styles/features/shell/_layout.scss` 按子域进一步拆分（`_top-control.scss`、`_activity-rail.scss`、`_task-center.scss`、`_station.scss`、`_settings.scss`）。
3. 对 `ShellRoot.tsx` 中少量 Tailwind utility class（如容器布局类）进行语义类收敛，减少样式入口分散。
4. 复测并记录迁移后 CSS 体积优化目标（当前构建 CSS `237.04 kB`，较基线偏大）。

### Risks / Blockers

1. `_legacy-foundations.scss` 仍承载较多历史规则，继续拆分时存在层叠回归风险。
2. 当前仓库处于 dirty worktree，且存在与本任务无关的既有改动，提交时需严格筛选。
3. 本轮为结构迁移，尚未覆盖完整视觉回归截图对比链路。

### Validation

1. `npm run typecheck`（通过）
2. `npm run build:web`（通过）
3. `cargo check --workspace`（通过）

## Next Agent Starter

- Current Milestone: M2 MVP 完整
- Session Goal: 继续推进 `T-175`，把 legacy 样式进一步模块化并降低 CSS 体积
- First Action (must be executable): 执行 `rg -n "^/\* ===|^/\* ========================================" apps/desktop-web/src/styles/foundations/_legacy-foundations.scss`，按分组创建 `foundations` 子文件并迁移首批规则
- Success Criteria:
  1. `_legacy-foundations.scss` 体积显著下降，核心基础规则分离完成。
  2. `npm run typecheck`、`npm run build:web`、`cargo check --workspace` 持续通过。
  3. Shell/Git/Settings 页面视觉无结构性回归。
- Target Workspace (workspace_id or root path): `/mnt/c/personal/vbCode`
