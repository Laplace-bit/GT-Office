# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
4. 阅读 `docs/06_API与事件契约草案.md`。
5. 阅读本文件的 `Handover Snapshot` 与 `Next Agent Starter`。

## Handover Snapshot

- Date: 2026-02-12 22:10 CST
- Owner: AI
- Current Milestone: M1 MVP 内核
- Goal of Current Session: 优化 Agent IDE 底栏信息架构（T-141）
- Target Workspace: `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `workspace_root=/mnt/c/project/vbCode`

### Completed

1. 重构 `StatusBar.tsx` 为单行状态带，不再分左右两栏；仅保留 5 项核心信息：`激活工作区`、`Agent 在线`、`终端会话数`、`Git 分支`、`Git 变更文件`。
2. 在 `StatusBar.tsx` 增加文本稳健性处理：工作区和分支字段统一 `trim` + fallback（`-`），并为长文本添加 `title` 全量提示。
3. 在 `ShellRoot.tsx` 接入 `gitChangedFiles={gitSummary?.files.length ?? 0}`，让底栏显示实时 Git 变更文件数。
4. 在 `shell-layout.css` 重写 `.status-bar` 样式：强制单行、禁止换行、字段分隔、工作区/分支可省略；并新增窄宽度降级规则（隐藏 label、再按优先级隐藏变更数/终端数）确保始终单行。
5. 在 `messages.ts` 新增 `statusBar.gitChanges` 中英文文案。
6. 更新 `docs/03_项目开发进度跟踪.md`：新增 `T-141` 任务与 2026-02-12 变更记录。

### Changed Files

1. `apps/desktop-web/src/shell/layout/StatusBar.tsx`
2. `apps/desktop-web/src/shell/layout/ShellRoot.tsx`
3. `apps/desktop-web/src/shell/layout/shell-layout.css`
4. `apps/desktop-web/src/shell/i18n/messages.ts`
5. `docs/03_项目开发进度跟踪.md`
6. `docs/04_上下文交接文档.md`

### Decisions

1. 底栏只保留可直接支撑 Agent 协作执行的状态信息，不展示低价值静态占位指标（如固定队列延迟/策略模式/资源占用示例值）。
2. 单行信息优先于字段完整显示，超长内容以省略和 tooltip 补偿，不允许换行挤压主工作区。

### Pending Next Actions

1. 第一动作：在桌面壳层执行 `npm --workspace apps/desktop-tauri run dev`，确认底栏在真实窗口宽度变化下仍保持单行且 tooltip 可用。
2. 第二动作：若产品需要更高信息密度，可将 `Git 变更文件` 拆为 `M/A/D` 分项并维持当前单行策略。

### Risks / Blockers

1. 本轮仅完成 Web 构建验证，未执行 Tauri 实机视觉回归；极窄窗口下字段隐藏阈值仍需产品侧确认。

### Validation

1. Command: `npm --workspace apps/desktop-web run build`
   Result: 通过（仅 chunk size warning）。

## Next Agent Starter

- Current Milestone: M1 MVP 内核
- Session Goal: 做 `T-141` 桌面端实机回归，确认底栏单行与超长截断策略符合产品预期
- First Action (must be executable): 在 `workspace_root=/mnt/c/project/vbCode` 执行 `npm --workspace apps/desktop-tauri run dev`，进入工作台后横向缩放窗口并检查底栏字段显示/隐藏与 tooltip 行为。
- Success Criteria:
  1. 底栏全程保持单行，不出现换行或挤压抖动。
  2. 长工作区标识与长分支名可省略显示，hover 可看到完整文本。
  3. 在窄宽度下按优先级隐藏次要项（先 `Git 变更文件`，后 `终端会话数`），核心项仍可见。
  4. 与 Git 刷新联动后 `Git 变更文件` 能正确更新。
- Target Workspace (workspace_id or root path): `workspace_id=ws:1b106531-bc6d-412e-b1d3-a7c50fdad2c9`, `/mnt/c/project/vbCode`

### Completed in Last Session

1. `T-141` 已完成实现（底栏单行重构 + 高价值字段收敛 + 超长文本截断）。
2. `StatusBar` 已接入 `gitChangedFiles` 实时数据。
3. `docs/03` 与 `docs/04` 已同步更新。

### Pending (ordered)

1. Tauri 桌面窗口实机回归（宽度压缩、超长工作区名/分支名）。
2. 视产品反馈决定是否继续压缩标签文案（如 `Agent`/`Term`/`Git` 短标签）。

### Validation Commands

1. `npm --workspace apps/desktop-web run build` -> 通过（仅 chunk size warning）。

### Risks / Decisions Needed

1. 待确认：移动到极窄窗口时是否继续保留 `终端会话数`，还是改为只保留工作区 + Agent + Git 分支。
