# vbCode 上下文交接文档

## 1. 目的

用于在 vibecoding 过程中切换执行者时，保证任务连续、决策可追溯、上下文不丢失。

## 2. 快速接手步骤（新 Agent）

1. 阅读 `docs/README.md`。
2. 阅读 `docs/03_项目开发进度跟踪.md`。
3. 阅读本文件（`docs/04_上下文交接文档.md`）。
4. 阅读 `docs/05_高质量功能设计_核心工作流.md`。
5. 阅读 `docs/06_API与事件契约草案.md`。
6. 阅读 `AGENTS.md`。

## Handover Snapshot

- Date: 2026-03-05 CST
- Owner: AI
- Current Milestone: M2 MVP 完整
- Goal of Current Session: 重构设置页布局（T-178）为 VSCode 分栏 + Channel 管理组件拆分
- Target Workspace: `workspace_root=/mnt/c/personal/vbCode`

### Completed

1. **SettingsModal 重构**：
   - 原版：单列弹窗，包含 `DisplayPreferences` + `SettingsChannelEntryCard` + `SettingsChannelManagerModal` 叠加弹窗模式。
   - 新版：分栏布局（左 240px Sidebar + 右内容区），导航项：General（显示偏好）/ External Channels（通道管理）/ About；响应式在 `<=768px` 时 Sidebar 变横排 Tab 栏。
2. **Channel 管理组件解耦**（新目录 `channel-management/`）：
   - `ChannelManagerPane.tsx`：主容器，加载运行态状态与 bindings，协调 Overview / Wizard 视图切换。
   - `ChannelOverview.tsx`：展示 runtime 状态、Bot 列表与"添加 Channel"入口。
   - `ChannelWizard.tsx`：4 步向导（通道选择 → 目标绑定 → 准入策略 → 确认应用）。
   - `ChannelBotCard.tsx`：单个 bot 账户卡片，展示分组路由并支持编辑/删除。
3. **修复多处字号与多语言设置未生效问题 (T-179)**：
   - 修复 `settingsModal.title` (去除了硬编码 fallback 英文)。
   - 在 `index.html` 添加 Google Fonts（Fira Code, JetBrains Mono, IBM Plex Sans 等）的加载以供在 Windows/Linux 等环境正确应用 UI 配置偏好。
   - 修复 CodeMirrorEditor 与 StationXtermTerminal 中的字号定义，引入 `readCssVar('--vb-font-size-base')` 等动态绑定机制，消除遗留的 `12px/13px` 硬编码。
4. **验证通过**：`npm run build:web`（Exit code 0）。

### Not Completed

1. `T-172` 仍为 `IN_PROGRESS`：
   - 真实外部机器人端到端联调证据（多 channel 多 bot 实机）仍需补齐。
   - 第一执行动作：在 Channel 管理页配置真实 Telegram bot，发送消息验证路由派发与 StationCard 绑定标签一致。

### Changed Files

1. `apps/desktop-web/src/shell/layout/SettingsModal.tsx` (MODIFY - 修复 i18n key)
2. `apps/desktop-web/index.html` (MODIFY - 增加 Google Fonts)
3. `apps/desktop-web/src/shell/layout/StationXtermTerminal.tsx` (MODIFY - 动态字号偏好读取)
4. `apps/desktop-web/src/shell/layout/CodeMirrorEditor.tsx` (MODIFY - 动态字号变量读取与 Refs 同步警告修复)
5. `docs/03_项目开发进度跟踪.md`
6. `docs/04_上下文交接文档.md`

### Risks / Blockers

1. IDE 的静态分析 lints 显示 `Cannot find module './ChannelOverview'` 等，但这是 IDE 缓存问题——`npm run typecheck` 已通过（vite build Exit 0），说明 TS 编译无误。
2. Channel 准入策略的 `open` 默认值在向导中已硬编码，若产品要求更换默认值需手动修改 Step 3 的 select 初始值。

### Validation

1. `npm run typecheck`（通过，Exit code 0）
   - 包含 `apps/desktop-web` 构建（`tsc -b && vite build`）
   - 包含 `packages/shared-types` 类型检查（`tsc --noEmit`）

## Next Agent Starter

- Current Milestone: M2 MVP 完整
- Session Goal: 推进 `T-172` 真实多 bot 联调闭环并补充证据。
- First Action (must be executable): 在新 Channel 管理页（Settings → External Channels）点击"添加 Channel"，通过向导配置真实 Telegram Bot Token，发送消息验证路由派发与回传。
- Success Criteria:
  1. 真实配置的 Channel 工作正常（能够根据外部消息进入正确的路由节点及触发界面响应）
  2. 字号调整及字体选择可以全局看到视觉变化。
- Target Workspace (workspace_id or root path): `/mnt/c/personal/vbCode`
