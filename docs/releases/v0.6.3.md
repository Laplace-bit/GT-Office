# v0.6.3

## Startup and production fixes

- Fix production white screen by using relative Vite asset paths and expanding CSP for Monaco workers.
- Show an instant HTML startup skeleton before JavaScript loads, then hydrate with a lightweight React splash.
- Defer Monaco, full app CSS, channel connectors, and Feishu/Telegram/WeChat workers until after first paint via `system_signal_ui_ready`.
- Split the shell into lazy-loaded chunks to shrink the initial bundle (~5.6MB → ~200KB entry).
- Add `scripts/measure-startup.mjs` for local/CI startup verification.

## Commits

- a6be855 fix(startup): production white screen and faster first paint
