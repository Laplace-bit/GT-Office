{
  "name": "gto",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "gto": "./bin/gto.mjs"
  },
  "scripts": {
    "test": "node --input-type=module -e \"import { existsSync } from 'node:fs'; import { spawnSync } from 'node:child_process'; const args = process.argv.slice(1); const runIndex = args.indexOf('--run'); const requested = runIndex >= 0 && args[runIndex + 1] ? [args[runIndex + 1]] : process.env.npm_config_run ? [process.env.npm_config_run] : ['tests/test_e2e.ts', 'tests/test_core.ts']; const targets = requested.filter((file) => existsSync(file)); if (targets.length === 0) { console.error('No test files found'); process.exit(1); } const result = spawnSync(process.execPath, ['--test', ...targets], { stdio: 'inherit' }); process.exit(result.status ?? 1);\" --"
  }
}
